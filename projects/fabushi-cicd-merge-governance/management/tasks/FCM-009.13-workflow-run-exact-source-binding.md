# FCM-009.13 — workflow_run exact-source binding

- **Project ID:** FAB-P0003
- **Project Key:** FCM
- **Parent task:** FCM-009
- **Atomic task:** FCM-009.13
- **Status:** in-progress
- **Current base:** `main@1989e928618a395d47e64012e4df4ccfb3aee62c`
- **PR:** #2460

## Trigger / requirement

The user explicitly resumed the control-plane repair and lifted the earlier `43ce...` evidence-window merge hold. The required behavior is to stop a stale or cancelled accepted-main Electron run from being misrepresented under a newer default-branch/controller SHA in `workflow_run` post-main delivery.

Observed historical failure remains immutable evidence: post-main run `34055670967` displayed then-current default-branch SHA `43ce998fd5fbcae032c179a8814de9ec08d03f4c`, while the actual triggering Electron run was `34055345371` for source `cf80b3a0b6ddc0670ddd58deb9c8d2c30aeb2075`, conclusion `cancelled`.

## Reused upstream semantics

- GitHub Actions `workflow_run`: downstream `GITHUB_SHA` / `GITHUB_REF` represent the default branch; triggering run identity comes from `github.event.workflow_run`.
- GitHub Actions REST workflow-runs API independently exposes triggering `head_sha`, `event`, `head_branch`, and `conclusion`.

No external code is copied and no new delivery protocol is invented.

## Atomic implementation

1. Bind source of record to `github.event.workflow_run.head_sha` and upstream run ID.
2. Re-read the upstream run through the GitHub Actions API and require exact source SHA, `event=push`, `head_branch=main`, and matching conclusion.
3. Distinguish controller/default-branch SHA from upstream Electron source SHA in run name and summary.
4. Always upload `post-main-source-binding.json` before the fail-closed conclusion gate.
5. Keep failed/cancelled upstream Electron results failed; never rewrite them as success for a newer SHA.
6. Rebase/squash #2460 onto the latest canonical main as one atomic commit and use required CI + protected merge queue only.

## Acceptance

- [ ] #2460 current-head required PR checks succeed.
- [ ] #2460 enters the protected `main` merge queue; no bypass/direct merge.
- [ ] Canonical main readback contains the exact-source binding repair.
- [ ] A post-main `workflow_run` on the new canonical SHA retains a real `post-main-source-binding-<electron-run>.json` artifact.
- [ ] Binding JSON and run summary identify controller SHA separately from upstream Electron source/run/conclusion.
- [ ] New canonical-main Electron + Native mobile required gates are exact-SHA green before post-main publication.
- [ ] Marketplace install -> Bot -> natural-language WebMCP -> Open App shared revision -> bounded Fabushi auto-login projection -> test-mode CNY 1080 lifetime purchase/restore user journey is retained with complete video, step screenshots, trace, report, and logs under that same SHA.
- [ ] Any missing platform or evidence item remains `PENDING`; no local build/test substitutes GitHub Actions evidence.
