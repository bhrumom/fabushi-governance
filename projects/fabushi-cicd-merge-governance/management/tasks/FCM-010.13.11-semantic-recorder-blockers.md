# FCM-010.13.11 — semantic projection + truthful macOS recorder blockers

Status: blocked
Parent: FCM-010.13
Project: FAB-P0003 / FCM
Source round: user directive `[Fabushi:8fed0a73-0a7f-4588-9e87-dea00c0e4c75]`, 2026-09-12
Current execution round: user directive `[Fabushi:86e0d1c4-4716-42c9-8050-dc87d90e6380]`, 2026-09-12
Original method goal: continue the Shopify-like GitHub Actions automation loop from `https://mp.weixin.qq.com/s/DEg20nFKfrd2urMc3BjntA`, keeping core/runtime validation decoupled from UI validation and using truthful, fast automated evidence.

## Frozen scope

The already verified generation-race repair is frozen. Do not change generation-sensitive retry semantics, retry budget, stale-generation re-resolution, device identity, immutable-source binding, the 27 journey categories, or the READY -> finish -> fresh snapshot -> logout gate except where needed to assert existing acceptance evidence.

This round fixes only two production blockers:

1. Restore the canonical semantic projection `test:peer-legacy:conversation:mahayana-ai:agent:assistant` on the correct visible App Surface without loosening the controller query. Production evidence showed channel creation intentionally switches the UI to the `channels` section, where the chat-only assistant peer is not projected into the semantic DOM. The journey must explicitly return to `聊天` before resolving the exact assistant id. Add a deterministic real Electron/App-Surface regression that proves the failing channel-surface state is `count=0`, then returns to `聊天`, resolves exactly one canonical assistant peer, and invokes it.
2. Replace the GitHub-hosted macOS whole-session recorder's false-positive startup contract with a recorder that actually yields a playable `macos-session.mov`. Detect the GitHub-hosted Apple Silicon display environment, including `AppleM2ScalerParavirtDriver`, record the native recorder probe result, and fail closed unless the selected recorder path passes capture and video-playability validation.

## Starting production evidence

- Canonical main before this task: `0ae214df5d2ca4b593253b5b9395f871b194473e`.
- Generation-race repair remains frozen from canonical `26348bde9091a8bb8cffa864bb911ad70046953e` / PR #2547 and the follow-up production evidence already recorded on main.
- Production controller: run `34673949330`.
- Fresh frozen-source target: run `34673991774`, attempt 1.
- Exact App-owned device: `gha-34673991774-1-macos-app`.
- Protected-account preflight, exact-device discovery, and the first journey categories succeeded.
- Production semantic failure: `semantic target not found: {"agentId":"test:peer-legacy:conversation:mahayana-ai:agent:assistant"}` immediately after the two self-hosted channel creations when the journey reopened the assistant conversation.
- Controller failure artifact: `fcm-010-13-11-external-controller-34673991774-1`, artifact id `10291836275`, digest `sha256:c28a9857212a7516cd401c25be32e328652f6e91e8d65a03729cdef37a10ddf0`.
- The target workflow's `Start whole-session macOS recording` step reported success because the background `screencapture -v` PID was alive after three seconds, but the final evidence set contained no playable `macos-session.mov`. This is an infrastructure/evidence failure and must remain fail-closed.

## Current production round evidence

- Semantic/navigation repair PR: #2551, merged only through the protected merge queue.
- New exact canonical main/source SHA: `c6183ebc1fbe93807d8bb1252171a4d5247a9b27`.
- Required PR gates passed before queue entry; merge-group CI then passed on the queued SHA.
- Same-source Electron desktop gate: run `34680351945`, success across Linux/Windows/macOS packaged-user validation.
- Same-source Native Android/iOS gate: run `34680351950`, success including Android emulator and iOS SwiftUI simulated-user validation.
- Post-main delivery: run `34680856596`; immutable provenance-scoped Release `desktop-1.2.56-c6183ebc1fbe` targets exact source `c6183ebc1fbe93807d8bb1252171a4d5247a9b27`.
- Release-chain run `34680936263` automatically dispatched production FCM controller `34680943569`; no manual target dispatch was used.
- Controller automatically dispatched target run `34680986090/1`; exact App-owned device `gha-34680986090-1-macos-app` was discovered and exclusively controlled.
- The repaired semantic flow is proven in the production controller log: exact assistant `test:peer-legacy:conversation:mahayana-ai:agent:assistant` resolved successfully at generations 77, 195, and 253; the non-empty global-search close path also completed and the `search` category passed.
- Fresh target artifact `fabushi-macos-interactive-evidence-34680986090-1`: artifact id `10293629316`, size `172149708`, digest `sha256:c00159c9f1e16833529372cbd233b90367da9105fa66b7f552c747c1362158cc`.
- Fresh whole-session movie was independently decoded from that artifact: H.264, 1024x768, 74.198333 seconds, 62 frames, 424357 bytes. `recorder-final.json` records `mode=frame-avassetwriter`, `appleParavirtDisplay=true`, `hostedParavirtDisplay=true`, `playable=true`, `firstSampleDecoded=true`, and `failClosed=true`. The evidence archive also contains Playwright traces, App/system/device-call logs, and 126 screenshots.
- The truthful report records `remoteActionCount=29`, proving non-zero remote App actions, but `controlStatus=failed` and evidence-gate failure are retained rather than hidden.

## Newly exposed blocker — intentionally not repaired in this frozen round

The fresh run advanced past both originally scoped blockers and then failed in the `send` category because `messageRowIds()` called `fabushi.app.find` with `limit=200`, while the production MCP schema enforces `limit <= 100`. The exact failure is MCP `-32602` / `too_big` on path `limit`. Controller run `34680943569` and target run `34680986090` therefore both concluded `failure`.

This is a new semantic-query contract defect, not the original missing assistant projection and not the recorder implementation. Because the current user round explicitly limits implementation to the two original blockers, it is recorded fail-closed for the next atomic round rather than silently expanding scope. FCM-010.13.11 and FCM-010.13 remain blocked, and the CI latency observer must not run yet.

## Atomic acceptance

Implementation and narrow regressions:

- [x] The production journey explicitly returns to the `聊天` section before every assistant-peer resolution; the exact assistant semantic id is not broadened or replaced.
- [x] Deterministic real Electron regression reproduces the two-channel state, proves the assistant is absent from the channel-only surface, then switches to `聊天` and proves exactly one canonical assistant projection.
- [x] The same real Electron App Surface regression resolves exact `test:peer-legacy:conversation:mahayana-ai:agent:assistant` through `fabushi.app.find`, then invokes it through generation-bound `fabushi.app.action`.
- [x] macOS recorder records environment/driver evidence, rejects false-positive native recorder startup, and has a verified fallback for the current GitHub-hosted runner.
- [x] Recorder preflight and final validation decode at least one video sample and require positive duration; file existence/size alone is insufficient.

Merge/release gates:

- [x] Required PR CI succeeds.
- [x] PR merges only through the protected merge queue.
- [x] New exact canonical main SHA is read back from GitHub.
- [x] Same-source desktop/native/release chain produces a new immutable macOS release and automatically launches a fresh FCM-010.13.11 production controller/target chain.

Production acceptance — all must hold in the same fresh target attempt:

- [x] Exact device id is `gha-34680986090-1-macos-app`.
- [ ] All 27 journey categories PASS. Fresh target passed `startup`, `login`, `main`, `conversations`, `agent`, and `search`, then failed at the start of `send` on the new `find limit=200` contract defect.
- [x] Remote App actions are real and non-zero (`remoteActionCount=29`).
- [ ] `READY_FOR_LOGOUT PASS` precedes `ci_session_finish`.
- [ ] After finish, a fresh App snapshot succeeds and the exact `settings-logout` semantic action succeeds.
- [ ] Target workflow conclusion is success (`34680986090` is failure).
- [ ] Truthful evidence gate is success (it correctly failed because the external journey failed).
- [x] Evidence preserves a playable `macos-session.mov`, device-call trace, logs, step screenshots, workflow/report metadata, uploaded artifact, and artifact digest.

Parent gate:

- FCM-010.13.11 and FCM-010.13 remain blocked until every production acceptance item above is verified from GitHub/Actions evidence. No partial pass.

## After parent acceptance

Immediately run the dedicated CI latency observer on the new canonical state. Use measured data, not estimates, to identify `macos-packaged-interactive` or the next largest over-budget validation surface; create/follow the next atomic optimization task from that evidence rather than broadening this repair. This remains gated because the current fresh production run is not accepted.

## Round log

- 2026-09-12: task created from the live production failure. Scope explicitly freezes the completed generation-race work and limits implementation to semantic projection and truthful whole-session recording.
- 2026-09-12: real Electron/Rust-host regression disproved the speculative preload-payload hypothesis. Source inspection and two failing real-host runs showed channel creation intentionally sets `section=channels`; `fabushi.app.find` correctly exposes only the current semantic DOM, so the assistant chat peer is absent until the journey returns to `聊天`. The speculative preload changes were reverted; the retained semantic fix is the surface transition plus exact-id resolution evidence.
- 2026-09-12: PR #2549 merged through protected merge queue to canonical `7dc6c809f37827032e2901ebb09a40e5576014ad`; exact-source Native Android/iOS and Electron Windows/Linux/macOS all passed, and Post-main delivery published immutable `desktop-1.2.56-7dc6c809f378`. The existing release-chain then auto-dispatched target `34677285321/1` directly. No FCM external-controller run was created because the release was published by `GITHUB_TOKEN`, whose release event does not recursively start another workflow. The target therefore entered its external-control hold with no controller and is explicitly not valid acceptance evidence. Follow-up was limited to rewiring the already-existing `workflow_run` release-chain to dispatch the exact-source FCM external controller, which alone creates and controls the fresh target; generation and product semantics remained frozen.
- 2026-09-12: PR #2551 fixed the remaining search/profile semantic ambiguity without changing generation-race semantics: non-empty global search is cleared then explicitly closed, the search surface must become absent, and profile navigation uses stable `test:profile-navigation-*` agent ids. The real packaged regression now covers two-channel creation plus non-empty search and exact assistant resolve/invoke. PR #2551 passed required CI and protected merge queue, producing canonical `c6183ebc1fbe93807d8bb1252171a4d5247a9b27`.
- 2026-09-12: exact-source desktop/native/release automation succeeded and auto-dispatched controller `34680943569` and target `34680986090/1`. The original semantic projection blocker is proven repaired in production and the recorder produced a verified playable whole-session MOV. The run then fail-closed on newly exposed `fabushi.app.find limit=200` versus schema maximum 100. No pass was recorded and no latency observer was started.
