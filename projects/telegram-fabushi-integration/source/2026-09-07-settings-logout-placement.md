# 2026-09-07 — Settings 退出登录入口位置

用户要求：将桌面端 Settings → General 中现有“退出登录”入口移动到截图所示的账户资料区域附近，完成后合并并发布新版。

约束：

- 复用现有退出登录行为和稳定自动化标识，不新建第二套账户/会话状态机。
- 入口应在账户资料卡片之后、Theme 等通用偏好设置之前直接可见。
- 退出后继续撤销当前会话并清除账户作用域的快速启动缓存，设备通用偏好设置保留。
- 必须用真实 E2E 固定入口顺序和既有退出行为。
- 必须走受治理 PR、canonical main、exact-main GitHub Actions 与严格递增新版发布。
