name: FAB-P0001 M3 canonical patch kick v2 (disabled by FCM-024)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Record retired patch-driver policy
        run: |
          echo 'Disabled by FCM-024.'
          echo 'This historical workflow no longer patches test files or writes to legacy branches automatically.'
          echo 'Current release validation uses only the manual Fabushi official MCP runner path.'
