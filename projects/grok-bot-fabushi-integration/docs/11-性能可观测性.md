# 11 性能与可观测性

M1/M2 建立当前 main 基线，再设置量化预算。统一 correlation id 贯穿 renderer -> IPC -> host -> Mahayana -> tool result。

关键指标：启动、Agent first event、tool round-trip、host crash/restart、IPC failure、CPU/RAM、动画 frame time、E2E duration。日志必须结构化且不含秘密。
