# OBF-001 Visual regression specification

Reference input: user-supplied OpenBot screenshot `2048x1280`, SHA-256 `555f8ae43517b2f6b2a1e545a16c0eb95e596585a1c9adfd211cad6a217c475b`. The browser/account chrome is not an app-controlled surface and is excluded. The app-controlled OpenBot crop is `(x=176,y=196,w=1671,h=937)`, crop SHA-256 `0a94bcf48630f4d872bab2e765df8df396026ac08d2b6c3b5fe64b112c1d268d`. The crop itself is the comparison surface; it is **not** stretched or normalized. The exact packaged Fabushi BrowserWindow content area is therefore fixed to `1671x937`, dark color scheme, reduced motion.

The production acceptance test is fail-closed. It requires `OBF_REAL_ACCEPTANCE=1`, a real `FABUSHI_ELECTRON_EXECUTABLE`, the exact crop above, and an exact 40-character `OBF_SOURCE_SHA` or `GITHUB_SHA` that equals the canonical `main` source used to build the package. It rejects inherited `FABUSHI_FEATURE_HOST_MODE=test|mock|stub` and `FABUSHI_E2E=1`, removes FeatureHost/test-host binary overrides from the packaged child process, and therefore cannot use a source-mode or mocked host run as evidence.

Visual gate for the original user requirement "一模一样":

- This independent OBF completion gate is **zero-tolerance**. `OBF_PIXEL_COLOR_THRESHOLD` and `OBF_MAX_DIFF_PIXEL_RATIO` must both be `0`; the acceptance test rejects looser values.
- Playwright `toHaveScreenshot` is an authoritative byte-level visual-regression gate with `threshold=0` and `maxDiffPixelRatio=0`.
- The same-run `visual-diff-report.json` must report `global.differingPixels == 0`, `global.differingPixelRatio == 0`, `global.zeroDiff == true`, and no residual region. Any single differing pixel keeps OBF-001 `IN_PROGRESS`; the measured gap must be fixed and the full fresh packaged run repeated.
- `geometry.json` records actual bounding boxes for navigation column, roster column, header, transcript, four coworker rows and avatar boxes, peer-row gaps, final assistant card, result table, source-file chips, real attachment card, hover operations, composer, and composer input. Roster BotMark boxes must stay within `34px +/-2px` in both dimensions.
- Semantic regions are audited for `column-navigation`, `column-roster`, `header`, `transcript`, `avatar-chief`, `avatar-research`, `avatar-builder`, `avatar-launch`, `final-message-card`, `table`, `source-files`, `attachment-card`, `hover-actions`, and `composer`. With the zero-tolerance gate, all region differing-pixel counts must also be zero.
- Identity is exact: `identity.json` must prove the same canonical Chief Bot `data-shape` in roster, header, final assistant transcript, and after closing/relaunching the same packaged app-data directory.
- Lifecycle is runtime-derived: `lifecycle.json` must contain `thinking`, `running`, `completed`, and at least one genuine `failed` tool/action state followed by recovery and an ordinary assistant completion from the real Mahayana workbench path.
- Runtime-capability evidence must be produced in that same fresh run for computer control, browser, files, tools/MCP, live screen/snapshot, human take-over/control, policy allow and policy deny, and audit. Source tests or static capability declarations cannot substitute for runtime observations.

Same-run evidence bundle:

- `openbot-reference.png` — exact reference crop, SHA-256 verified before the journey;
- `fabushi-openbot-comparison.png` — same-size packaged Fabushi workspace screenshot;
- `visual-diff-report.json` and `geometry.json` — zero-difference global/region audit and layout measurements;
- `identity.json` — roster/header/transcript/restart identity continuity;
- `lifecycle.json` and `mahayana-agent-workbench.json` — Mahayana lifecycle/workbench evidence;
- `runtime-capabilities.json` — real computer/browser/files/tools/live-screen/take-over/policy allow-deny/audit observations from the same run;
- `evidence-manifest.json` — exact canonical source SHA, packaged app version, zero-threshold values and reference hash;
- `runtime.log`, Playwright `trace.zip`, and the full `video/` output for the same fresh packaged run.

PR source/test-host runs, old-base PR checks, and a skipped `OBF_REAL_ACCEPTANCE` test are never substitutes for this evidence. Merge authorization is based on the protected merge queue's current merge-group/exact candidate and its required `CI result`, never a stale PR-head check. Until the protected merge is complete and the exact-main fresh packaged evidence bundle exists with all runtime capabilities and zero differing pixels, OBF-001 remains `IN_PROGRESS` and cannot be called 1:1 or complete.
