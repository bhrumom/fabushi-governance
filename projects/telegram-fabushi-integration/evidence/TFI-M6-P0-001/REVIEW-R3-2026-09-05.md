# TFI-M6-P0-001 independent code review — R3

- Project: `FAB-P0001 / TFI`
- Task: `TFI-M6-P0-001`
- Review round: `R3`
- Review date (UTC+08): `2026-09-05`
- Verdict: **`REVIEW-PASS` — code/record review only**
- Delivery state: **`CI-BLOCKED / CLOSURE-BLOCKED`**
- Target repository: `bhrumom/fabushi`
- Target PR: `#2323`
- Exact target base: `codex/tfi-m6-repair@9e88a2e9c030fe05147460dfa580366cf9aa433d`
- Exact target head: `fix/tfi-m6-p0-001-community-create-boundary@c32a0bd80922a2be6e62c2722fbbd3b14a18a252`
- PR state at R3 readback: `OPEN / UNMERGED`, 10 commits, 19 changed files
- Reviewer branch: `review/pr-2323-r3-record-20260905`, created directly from canonical `main@688465e94647d4c866f6b1d7b4884145b2f4a9da`
- Reviewer scope: independent source/test/record/CI review and `projects/telegram-fabushi-integration/**` review write-back only
- Local build/test/E2E: **not run**; repository source, GitHub PR state, official upstream source, and GitHub Actions are the verification sources

## Verdict boundary

`REVIEW-PASS` here means the exact `c32a0bd...` application/test/record state has no remaining R1/R2 review finding and no new R3 code/record blocker was found. It does **not** mean required CI is green and does not authorize merge, test release, protected-main closure, packaged E2E, or Release.

Required Rust workflows on the exact R3 head still fail at formatter steps before later Rust tests/clippy, so the task remains **`CI-BLOCKED / CLOSURE-BLOCKED`** even though independent code/record review passes.

## Sources read and facts locked

R3 independently read/verified:

- canonical root `AGENTS.md` and `projects/PORTFOLIO.json`;
- TFI `PROJECT.yaml`, `SOURCE_OF_TRUTH.md`, `README.md`;
- active P0 task/WBS/milestone/acceptance/risk/dependency/status/changelog/evidence and `TEST-RELEASE-HANDOFF` records;
- live PR #2323 metadata, body, commit list, changed files, comments/reviews and exact-head Actions;
- R1 reviewer PR #2325 and its three changed review records;
- R2 reviewer PR #2326 and its three changed review records;
- exact application/test/workflow diffs for PR #2323;
- official TDLib source at the exact recorded revision/license.

Live PR metadata at review time matches the requested identity: base `9e88a2e9...`, head `c32a0bd...`, open and unmerged. No head drift was observed during the substantive R3 read.

## R1/R2 history preserved

R1 remains historical `REVIEW-REJECTED` on execution head `73a46d3089c4f12dfb2f5659b232d51c674ed5a6`; reviewer PR #2325 head `7f594f10570822dcf23a4c3c02ddb0583ea94f14` contains exactly three new TFI review records and no application/workflow files.

R2 remains historical `REVIEW-REJECTED` on execution head `1dc165489498889504a61b7e07d5164f25188cef`; reviewer PR #2326 head `dfbae8a16531f325ab482e7dc4bdf6940b6f5f87` likewise contains exactly three TFI review records. R2 disposition was B1 `CLOSED`, B2 `CLOSED`, B3 `OPEN / BLOCKING` solely because generic durable TFI records and the then-current PR description still carried unsupported inherited formatter cause attribution.

R3 does not rewrite or retroactively relabel either verdict.

## R3-B3 — CLOSED

R3 rechecked every durable location identified by R2 and the current PR body/handoff. The current active conclusion is now consistently evidence-bounded:

- required PR-head Rust workflows fail at formatter steps before subsequent Rust checks;
- no independent exact base-vs-head formatter comparison exists in the evidence set;
- formatter provenance therefore remains **undetermined: base, PR, or mixed**;
- atomic/Electron success is additive evidence only and cannot waive required Rust CI.

Disposition by record family:

- `management/02-里程碑.md`: prior inherited formatter attribution explicitly withdrawn; current milestone says provenance unknown and T1 open/blocked.
- `management/03-验收追踪矩阵.md`: prior inherited wording explicitly withdrawn/superseded; overall acceptance remains blocked.
- `management/04-风险登记.md`: R-16 now states formatter failure with no exact base-vs-head attribution; former inherited formatting-debt wording is expressly withdrawn.
- `management/05-状态报告.md`: the two old `inherited rustfmt` statements remain only as historical chronology and are immediately covered by a dedicated supersession notice withdrawing cause attribution.
- `management/06-依赖与阻塞.md`: the earlier `inherited audited M6 drift` cause is explicitly withdrawn as unproven; BLK-007 remains active.
- `management/07-变更日志.md`: append-only historical wording is retained, followed by an explicit same-record supersession entry that withdraws the cause attribution.
- P0 WBS/milestone/acceptance/risk/changelog overlays, task record, M6 WBS, evidence README and `TEST-RELEASE-HANDOFF` all use the same conservative provenance rule and keep CI/review/delivery gates separate.
- current PR #2323 body points to exact current head `c32a0bd...`, identifies `8610d0f...`/`c32a0bd...` as records-only B3 repair/write-back, expressly withdraws `inherited rustfmt` / `inherited audited-M6 drift`, and does not claim a base-vs-head formatter diagnosis.
- current R3 handoff comment `5543277743` asks for review of exact `c32a0bd...`, states formatter provenance is undetermined, and keeps required Rust CI blocked.

The only remaining literal old inherited-cause wording is in append-only historical context with explicit withdrawal/supersession. It is not a current conclusion. Therefore R2-B3 is closed for R3 review purposes.

## Records-only repair scope — VERIFIED

`1dc165489498889504a61b7e07d5164f25188cef..8610d0f6a6c143281a6ffede270ad4821dc780a9` is exactly one commit changing 15 files, all under `projects/telegram-fabushi-integration/**`.

`8610d0f6a6c143281a6ffede270ad4821dc780a9..c32a0bd80922a2be6e62c2722fbbd3b14a18a252` is exactly one commit changing only `projects/telegram-fabushi-integration/evidence/TFI-M6-P0-001/README.md`.

Thus the R2-B3 repair/write-back did not modify application source, regression tests, workflow files, root governance, or another project.

## Application source continuity after `726b421...` — VERIFIED

The application repair remains contained through `726b4210ddd4d9a967778193a8d374b5f8bad206`.

R3 fetched `native/mahayana-messaging/src/engine.rs` and `src/service.rs` at both `726b421...` and current `c32a0bd...`; the blob SHA for each file is identical across those refs. The comparison after `726b421...` also contains no later `engine.rs` or `service.rs` change. Therefore the production code previously reviewed in R2 is unchanged at R3 head.

Later delta after `726b421...` is limited to the focused P0-001 regression, additive atomic workflow, and TFI project records. No later M6-P0-002/003/004/005, M7/M8, MSR, or GBF runtime implementation leaked into this PR.

## B1/runtime semantics — still accepted

No new application blocker was found.

- `RespondCommunityJoin` still builds `Option<Event>` explicitly.
- Approval requires community admin `InviteMembers`, calls `approve_join(&requester_id, &actor_id, ...)`, records audit actor as the acting owner/admin and target as requester, emits `CommunityChanged`, then emits a participant upsert only when approved and the conversation is Group/Channel.
- Rejection removes the pending request, records owner/admin -> requester audit attribution, does not add the requester to members, and emits only `CommunityChanged`.
- The focused regression directly asserts approved `Member` + `invited_by` + pending removal + audit actor/target and rejected non-member + pending removal + audit actor/target.
- Community-backed generic `CreateConversation` remains an idempotent service-level no-op before generic `UpsertConversation`, preventing forged retype/re-owner/re-member projection.
- Community-backed `UpdateConversation` remains metadata-only (`title` / `description`) after community authorization; caller-supplied kind/owner/participants do not become authority.
- Missing-Community `RequestCommunityJoin` still returns `CommunityNotFound`; there is no `CommunityState::new()` synthesis.
- Ordinary non-Community create still follows generic `UpsertConversation` compatibility path.

## B2/open-source-first provenance — independently reproduced

The execution record is accurate and sufficiently exact:

- official repository/revision: `tdlib/td@d1085f9cebc5a62379991ae1652673954f229c1f`;
- file: `td/telegram/Requests.h`;
- symbols at that revision: `td_api::createNewSupergroupChat` and `td_api::processChatJoinRequest` request handlers;
- license file: `LICENSE_1_0.txt`, Boost Software License 1.0.

R3 independently reproduced those symbols and license from the official repository. The task record correctly says only the create-vs-join-request boundary principle was learned and no TDLib implementation source was copied, translated, ported, or adapted.

## Exact current-head GitHub Actions — R3

The user-supplied `338933...` runs are valid **repair-head `8610d0f...` evidence**, and the current records label them that way. They are not the latest `c32a0bd...` run set.

Exact current R3 head `c32a0bd80922a2be6e62c2722fbbd3b14a18a252` triggered a newer validation round:

- `TFI M6 P0-001 atomic gate` run `33893624154`, job `101090979544`: **SUCCESS**. `Compile M6 contract test binary` and `Run TFI-M6-P0-001 regressions` both passed.
- `Mahayana fast checks` run `33893624176`, job `101090979748`: **FAILURE** at `Verify formatting before native package setup`; all later native/Rust test/check steps are skipped.
- `Messaging Product Gate` run `33893624204`: Rust job `101090979954` **FAILURE** at `Rustfmt self-hosted messaging`; subsequent messaging tests, clippy, media and bridge checks are skipped. Electron Messenger job `101090980317` is **SUCCESS**.
- `Developer Fiat Commerce` run `33893624183`: **SUCCESS**.
- `Explicit automerge` run `33893624211`: **SUCCESS**.

This yields two simultaneous facts: task-specific atomic/Electron evidence passes, while required Rust CI remains red. Therefore the R3 review verdict is `REVIEW-PASS` but delivery state remains `CI-BLOCKED / CLOSURE-BLOCKED`.

## Six-category R3 result

| Category | R3 result | Evidence / boundary |
|---|---|---|
| Unit | **PASS task-specific** | exact-head atomic compile + three named P0-001 regressions succeed |
| Contract | **PASS task-specific** | create no-op, metadata-only update, join-decision ownership/audit invariants, ordinary create and CommunityNotFound guards are directly covered/reviewed |
| Integration | **CI-BLOCKED** | atomic task compile/regressions pass, but required Mahayana/Product Rust jobs stop at formatter; later Rust integration/tests/clippy are skipped |
| E2E | **PENDING** | PR is unmerged; no exact accepted-main installable/package journey exists |
| Security | **PASS for P0-001 review scope** | forged authority negatives and owner/admin -> requester audit/member invariants are directly asserted; no new R3 authority regression found |
| Performance | **static only / packaged PENDING** | no network/poll/timer/retry/wait/unbounded traversal introduced by this atomic patch; exact-main packaged regression remains downstream |

## R3 conclusion and downstream gates

**`REVIEW-PASS` for code/records at exact head `c32a0bd...`.** R1-B1, R1-B2 and R2-B3 are closed for independent review. No new R3 source/security/record-scope blocker was found.

Separately, **`CI-BLOCKED / CLOSURE-BLOCKED` remains mandatory** because required Rust workflows fail at formatter on the exact reviewed head and later Rust checks are skipped.

Do not treat this review as any of the following:

- required CI pass;
- authorization to merge #2323;
- protected canonical-main acceptance/readback;
- exact accepted-main packaged Electron/mobile E2E;
- required screenshot/video/trace/report/log evidence bundle;
- test Release or formal Release.

`TFI-M6-P0-002` remains blocked on `FULL-CLOSE(TFI-M6-P0-001)`. No merge, release, or downstream implementation is authorized by this reviewer record.
