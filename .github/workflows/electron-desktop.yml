name: Electron desktop quality gate
on:
  workflow_dispatch:
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic desktop quality gate paused for 2026-09-05 Mac test release.'
