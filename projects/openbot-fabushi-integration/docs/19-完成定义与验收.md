# 完成定义与验收

OBF-001 may become `complete` only when **all** of the following are simultaneously true:

1. The OpenBot feature -> Fabushi implementation -> evidence -> gap matrix has no unclassified fusible feature, and existing Fabushi equivalents remain the single source of truth rather than a second OpenBot runtime/control plane.
2. Project identity is valid under the canonical portfolio policy: the PR allocates the live `PORTFOLIO.next_sequence`; a newly registering project uses `PENDING_CANONICAL_MAIN`; after its protected registration merge a records-only finalization binds `first_canonical_main_commit` exactly once to the earliest canonical-main commit that introduced the project folder, after which it is immutable.
3. The product PR enters the repository's protected merge queue only after re-reading then-current canonical `main`, PR head/base/mergeability, portfolio allocation, ruleset, and required checks. The required `CI result` must pass on the merge queue's current merge-group/exact candidate; stale PR-head or old-base checks are inadmissible.
4. After protected merge (and any required records-only canonical-commit finalization), a new exact canonical `main` SHA is read. Only a fresh production packaged Fabushi Desktop built from that exact SHA is accepted; no source-mode, mock/test FeatureHost, old package, sibling build, or previous CI run may substitute.
5. On one real macOS or Windows Fabushi Desktop, one fresh packaged run fixes the app-controlled viewport to `1671x937`, dark theme, reduced motion and the pinned reference crop SHA-256 `0a94bcf48630f4d872bab2e765df8df396026ac08d2b6c3b5fe64b112c1d268d`.
6. That same run creates Chief/Research/Builder/Launch through the real runtime and proves Mahayana `thinking -> running -> completed`, at least one genuine `failed -> recovered` path, and a final ordinary assistant completion.
7. That same run proves the requested Final launch brief structure: `Workstream | Owner | Status` three-column table, Research/Builder/Launch owner chips, source-file chips, real attached files, hover reply/copy/more controls, and the normal composer.
8. `identity.json` proves Chief is the same canonical Bot identity in roster, header, final transcript and after restart with the same packaged app-data directory.
9. `runtime-capabilities.json` plus runtime/workbench logs prove real computer, browser, files, tools/MCP, live screen/snapshot, human take-over/control, policy allow, policy deny, and audit behavior in the same run. Static source declarations and unit/source tests are not runtime evidence.
10. The same fresh run preserves the complete evidence bundle: exact reference and comparison screenshots, full playable video, Playwright trace, runtime log, lifecycle/workbench journals, runtime-capabilities evidence, identity, geometry, visual-diff report and evidence manifest bound to the exact canonical source SHA and packaged app version.
11. The original user's literal 1:1 requirement is the final visual gate: `differingPixels == 0`, `differingPixelRatio == 0`, and no non-zero semantic region. Project-level tolerance for quantified residuals may describe intermediate progress, but **any non-zero pixel difference is incomplete for OBF-001** and requires fixing the measured gap and rerunning the whole fresh packaged acceptance.

If any item above is missing, stale, skipped, synthetic, from another run, from another source SHA, or visually non-zero, the status remains `IN_PROGRESS`.
