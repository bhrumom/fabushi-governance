# 依赖与阻塞

| ID | Dependency | Role | Final state |
|---|---|---|---|
| FCM-D001 | GitHub protected `main` + merge queue | final merge safety | available / verified historically; every new repair still requires fresh exact-head review/CI + queue evidence |
| FCM-D002 | GitHub Actions metadata APIs | FCM-002 telemetry | available / verified |
| FCM-D003 | `CI result` | merge and release-source safety | available / verified |
| FCM-D004 | `Electron desktop result` | macOS release evidence | release gate enforced |
| FCM-D005 | direct `Native Android` + `Native iOS` checks | mobile release evidence | available / verified on exact source |
| FCM-D006 | GitHub-hosted runners | CI execution/latency | available; queue delay measured |
| FCM-D007 | Store credentials/signing material | actual store upload only | external operational dependency; not stored here |
| FCM-D008 | authenticated protected CI test-account Fabushi dynamic-device MCP | FCM-010.13 App-owned complete macOS journey | **available / verified** — fresh target `34696426684/1` registered exact App-owned device `gha-34696426684-1-macos-app` and completed 39 accepted real remote actions before the unread-positive failure |
| FCM-D009 | legacy assistant peer semantic projection | FCM-010.13.11 27-category journey | **closed / verified** — exact assistant row remains resolvable on the fresh target; current defect is its positive unread value, not missing identity |
| FCM-D010 | macOS GitHub-hosted whole-session recorder output | FCM-010.13.11 mandatory visual evidence | **closed / verified** — artifact `10299211290` records `recorderPlayable=true` and retains the fresh whole-session MOV |
| FCM-D011 | `fabushi.app.find` production schema maximum `limit<=100` | FCM-010.13.11 semantic queries | **closed / guarded** — bounded finds remain valid; fresh unread-positive failure was a zero-match semantic timeout, not schema rejection |
| FCM-D012 | assistant-active -> non-assistant peer -> chat-list transition before exact `unread-none` | FCM-010.13.11 pre-send baseline | **closed / production verified** — canonical `b8fb73466899adc912db67823a8e429987070a6b`, target `34696426684` found exact `unread-none` at generation `295` after the real non-assistant/list transition |
| FCM-D013 | exact-head independent no-finding review + protected merge queue for PR #2558 | prior repair governance | **closed / verified** — exact-head REVIEW-PASS comment `5646040319` bound head `9aee5e73fe053ca57a29fab08ad934eb200fade5` with zero blocking findings before protected merge; records-only PR #2559 retains the review record |
| FCM-D014 | stateful Mahayana assistant unread count and authoritative list refresh after reply/open | current FCM-010.13.11 production blocker | **blocking / repair in progress** — canonical `KernelConversationProvider::list_conversations()` always constructed assistant `unread_count=0`, so fresh target `34696426684` could never expose exact `unread-positive`; branch `fix/fcm-010-13-11-runtime-unread-transition` adds read-boundary state plus post-message/open `conversation.list` refresh |
| FCM-D015 | exact-head required CI + independent no-finding review + protected merge for runtime-unread repair | current repair governance | **blocking / repair re-review required** — independent review of #2561 head `2e65a0a6...` returned `REVIEW-FAIL/BLOCKED` (`P0=0`, `P1=1`, `P2=1`, `other=0`). The branch stays draft while P1 per-conversation read state and P2 real production-adapter regression are repaired; only a later unchanged green head may receive a fresh independent REVIEW-PASS |

## FCM-010.13.11 current closure state

The historical generation race, legacy assistant identity, whole-session recorder, `find limit<=100`, semantic unread naming, and assistant-active list-state baseline defects are all closed by prior exact-source evidence. PR #2558 head `9aee5e73fe053ca57a29fab08ad934eb200fade5` obtained a traceable independent REVIEW-PASS before protected merge, producing canonical source `b8fb73466899adc912db67823a8e429987070a6b`.

That source produced immutable Release `desktop-1.2.56-b8fb73466899` (Release `387578625`), production controller `34696372043`, and fresh frozen-source target `34696426684/1`. The target controlled only exact App-owned device `gha-34696426684-1-macos-app`, recorded 39 accepted real remote actions, passed packaged Playwright, and retained playable whole-session recording, trace, screenshots and logs. It also proved the previous repair: exact assistant `unread-none` was found at generation `295` after the required `Channels -> known non-assistant -> Chats` transition.

The new deterministic blocker is later. After the assistant was reopened, a real probe was sent, and the UI switched away again, exact `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-positive` never became findable and timed out at App Surface generation `1674`. Canonical source explains this: production `KernelConversationProvider::list_conversations()` returned `Conversation::mahayana_assistant()`, whose helper initializes `unread_count=0`, with no stateful unread transition. Electron transport also did not request an authoritative conversation-list refresh after assistant completion.

The failure remained truthful and fail-closed: no 27/27 result, no `READY_FOR_LOGOUT PASS`, cleanup `ci_session_note -> ci_session_finish`, and no accepted fresh-snapshot/exact-logout success chain. Target evidence artifact `10299211290` has digest `sha256:ac43ba8829a5f8bb22d30bf3339fed2b87172c4ccdfc1103a8dcdc75950735d2`; controller evidence artifact `10298692332` has digest `sha256:4d6cc9ac0e6c2f4390a0ad6cb5546bffa9694735695c2e15f34340a3df558d4a`.

The current atomic repair is recorded in `management/tasks/FCM-010.13.11-unread-deselect-governance-repair.md` and branch `fix/fcm-010-13-11-runtime-unread-transition`. It must pass its own exact-head CI and independent review, protected merge, then generate a new exact-source immutable Release and a brand-new controller-dispatched target. `34696426684`, `34691502192`, and older targets/artifacts remain diagnostic only.

FCM-010.13.11 and FCM-010.13 remain `in-progress / fail-closed`. CI latency observer remains deferred until one same new production run proves all 27 categories PASS, non-zero real remote actions, exact `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller/target success, truthful evidence, and complete playable video/trace/screenshots/logs/artifact digest/source lineage.
