# 风险登记

| ID | Risk | Probability | Impact | Owner | Mitigation / trigger | Status |
|---|---|---|---|---|---|---|
| FCM-RSK-001 | path classifier misses runtime file | medium | unsafe green merge | FCM | unknown non-doc paths force all canonical checks | controlled |
| FCM-RSK-002 | merge-group diff classification wrong | low | stale/incompatible merge | FCM | official merge-group base/head + required `CI result` | controlled |
| FCM-RSK-003 | CD impact gate false-negative | low | required deploy skipped | FCM | fail safe on uncertainty; manual dispatch retained | controlled |
| FCM-RSK-004 | sensitive CI change auto-merges | low | delivery-policy regression | FCM | explicit authorization + merge queue + CODEOWNERS | controlled |
| FCM-RSK-005 | optimizing for speed reduces coverage | medium | regressions | FCM | risk-tiered selection, not test removal | controlled |
| FCM-RSK-006 | telemetry becomes a new CI blocker | low | availability/merge latency | FCM-002 | observer is soft-SLO/non-required | controlled |
| FCM-RSK-007 | store workflow publishes non-main/non-green SHA | medium | unverified release | FCM-004 | protected-main ancestry + exact-SHA required gates | controlled |
| FCM-RSK-008 | CODEOWNERS becomes too broad | medium | unnecessary review latency | FCM-005 | no catch-all; governance contract rejects broad pattern | controlled |
| FCM-RSK-009 | historical release source predates gate script | low | old release blocked | FCM-004 | supported source is post-policy protected-main history | accepted |
| FCM-RSK-010 | external controller cannot connect the protected CI test-account Fabushi device namespace, leaving the transient App-owned macOS device unreachable | low | exact-source install/smoke can succeed while the required full semantic journey times out and the truthful evidence gate fails | FCM-010.13 | preserve protected-account preflight, exact `gha-<run>-<attempt>-macos-app` binding, and nonzero remote device-call trace; never substitute local/historical/KRIS devices | closed / historical — fresh production targets reached exact App-owned devices; target `34696426684` completed 39 accepted real remote actions |
| FCM-RSK-011 | assistant remains active or list-only unread baseline is queried before remount/fresh generation | low | `send` can fail before the positive unread transition is tested | FCM-010.13.11 | select known non-assistant peer before Chats, poll exact bounded `unread-none`, and re-resolve generation before reopen | closed / production verified — target `34696426684` found exact baseline at generation `295` after the repaired list-state transition |
| FCM-RSK-012 | a repair merges after a finding-bearing/absent exact-head independent review or without the protected queue owning the final merge | medium | governance can accept a known-bad source and produce misleading production evidence | FCM-010.13.11 | keep each new repair draft until required CI and a genuinely independent no-finding review pass the same exact head; any commit invalidates review; then use protected merge and re-read canonical main | open / controlled — #2558 itself satisfied this gate via exact-head REVIEW-PASS comment `5646040319`; the new runtime-unread repair must prove it again on its own final head |
| FCM-RSK-013 | production Mahayana assistant conversation provider never increments unread state, or the renderer never refreshes the authoritative list after completion | high | a real assistant reply can occur while exact `unread-positive` remains impossible, blocking receive/unread and the remaining 27-category journey | FCM-010.13.11 | maintain provider read-boundary/unread state, count assistant completions only, refresh official `conversation.list` after assistant message/open, preserve exact semantic identity and fail-closed production polling | open / blocking — canonical `b8fb73466899...` target `34696426684` deterministically timed out on exact `unread-positive`; source returns `Conversation::mahayana_assistant()` with default `unread_count=0` |
| FCM-RSK-014 | shared-provider conversation reads or non-open history reads could clear another conversation's unread boundary | high | opening `codex:agent:research` or background history could suppress a legitimate assistant unread indicator | FCM-010.13.11 | keep read acknowledgement per conversation; explicit open `200` marks only that conversation; Runtime-clamped background `500` remains read-only; cover the real FeatureHost -> Runtime -> Kernel/provider adapter path | repair implemented / CI + independent re-review pending — exact-head review `2e65a0a6...` found this as P1 and the mock-only regression gap as P2; repair commit `25f95c1f...` scopes the boundary and cross-layer regression is added on the same draft branch |

## Live trigger

PR #2558 exact head `9aee5e73fe053ca57a29fab08ad934eb200fade5` received a traceable independent REVIEW-PASS with zero blocking findings at issue comment `5646040319`, then merged through the protected flow to canonical `b8fb73466899adc912db67823a8e429987070a6b`. Its exact-source Release `387578625` / tag `desktop-1.2.56-b8fb73466899` produced production controller `34696372043` and auto-dispatched fresh target `34696426684/1`.

The target reached exact App-owned device `gha-34696426684-1-macos-app`, completed 39 accepted real remote actions, passed packaged Playwright, and retained a playable whole-session recorder. It also closed FCM-RSK-011 in production: after the assistant-active -> known non-assistant -> Chats transition it found exact `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-none` at generation `295`.

The current live trigger is FCM-RSK-013. After reopening the assistant, sending the real probe, resolving the sent row, and switching away again, exact `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-positive` never became findable and timed out at App Surface generation `1674`. Source inspection shows the production kernel conversation provider had no unread state at all: `list_conversations()` returned the default Mahayana assistant record whose `unread_count` is always zero. Electron transport also did not refresh `conversation.list` after assistant completion.

The failure was truthful: 27/27 was not reached, `READY_FOR_LOGOUT PASS` was not reached, cleanup ran `ci_session_note -> ci_session_finish`, and no accepted fresh-snapshot/exact-logout success chain occurred. Target artifact `10299211290` digest `sha256:ac43ba8829a5f8bb22d30bf3339fed2b87172c4ccdfc1103a8dcdc75950735d2` and controller artifact `10298692332` digest `sha256:4d6cc9ac0e6c2f4390a0ad6cb5546bffa9694735695c2e15f34340a3df558d4a` retain the failure evidence.

Current branch `fix/fcm-010-13-11-runtime-unread-transition` repairs the provider state and authoritative projection refresh, with deterministic Rust/transport regressions. FCM-RSK-012 and FCM-RSK-014 require exact-head review discipline before merge. No old target, artifact, #2558 review, or prior green PR-side CI can satisfy the new repair's acceptance gates.
