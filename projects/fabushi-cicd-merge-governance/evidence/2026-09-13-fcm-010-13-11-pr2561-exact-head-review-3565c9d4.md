# FCM-010.13.11 independent exact-head review — PR #2561 @ 3565c9d4

- Repository: `bhrumom/fabushi`
- Project/task: `FAB-P0003 / FCM-010.13.11`
- Product PR: `#2561` — `fix(fcm): restore production assistant unread transition`
- Exact reviewed product head: `3565c9d483cb29e7729e3003e2b844b46124e18f`
- Exact reviewed PR base: `9785d8e1b71e0d61cf31541af28b910d5be58bd9`
- Product PR state at first and pre-verdict readback: `open`, `draft=true`, `merged=false`, `mergeable=true`
- Review date: `2026-09-13`
- Disposition: **REVIEW-PASS**
- Blocking findings: **P0=0 / P1=0 / P2=0 / other-blocking=0**
- Reviewer provenance: independent ChatGPT code-review-project session, `GPT-5.6 Sol`, Chat mode, Extra High reasoning; GitHub connector facts were re-read from the repository and PR; no product/test/workflow/dependency/version mutation, no CI rerun, no ready/merge/release/dispatch action.

This record was produced from a fresh independent read of GitHub. It does not reuse an unfinished browser-chat fragment or an older-head verdict. It is records-only evidence and does not modify PR #2561's product head.

## Immutable specification binding

`spec_digest` is SHA-256 over the following UTF-8 manifest, sorted lexicographically by path and serialized exactly as `<path>\t<git_blob_sha>\n`. Every blob SHA was re-read from GitHub at exact product head `3565c9d483cb29e7729e3003e2b844b46124e18f`.

```text
projects/fabushi-cicd-merge-governance/SOURCE_OF_TRUTH.md	a0f02dc787d9005e4b2ea781766243a962ab6872
projects/fabushi-cicd-merge-governance/docs/02-需求与成功指标.md	98b9fbbe00bdeafd87a3b767eb730405213a1bae
projects/fabushi-cicd-merge-governance/docs/19-完成定义与验收.md	16255a2e702da2a8b04519aa46f0425da5e861af
projects/fabushi-cicd-merge-governance/management/01-WBS原子任务.md	7e5068102bf72a61c9e2dfe7884d90f71d6774db
projects/fabushi-cicd-merge-governance/management/03-验收追踪矩阵.md	fad9e0d4384bd960015bd8b66692f3c07f0ee6b2
projects/fabushi-cicd-merge-governance/management/04-风险登记.md	8ee79e9fb39f6ca03531a0329ba97f8ae24c2845
projects/fabushi-cicd-merge-governance/management/05-状态报告.md	9d1e21fa81f3016f4e80393c68cb5c1d30701637
projects/fabushi-cicd-merge-governance/management/06-依赖与阻塞.md	03f9c7dea1a13b16bea819030d3b70ea017f64f6
projects/fabushi-cicd-merge-governance/management/07-变更日志.md	9d238fca52b459410090a6302e1b22c3acbb0086
projects/fabushi-cicd-merge-governance/management/tasks/FCM-010.13.11-unread-deselect-governance-repair.md	85c4fd7bf2325fc69e0002b5bcde9f0feb775d86
```

- `spec_digest = 391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307`

`review_key` is SHA-256 over this exact UTF-8 canonical binding, including the final newline:

```text
repository=bhrumom/fabushi
pr_number=2561
exact_head_sha=3565c9d483cb29e7729e3003e2b844b46124e18f
exact_base_sha=9785d8e1b71e0d61cf31541af28b910d5be58bd9
atomic_task_id=FCM-010.13.11
spec_digest=391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307
```

- `review_key = 037fe69d1c84279ffc971208c3454ba7bb5bf9c68a3bc63d9093a3ba0b5d8918`

Any change to the product PR's reported base/head or any bound specification blob invalidates this review and requires a fresh exact-head review.

## Live GitHub truth, commits, diff, reviews, threads, and CI

The product PR was live-read before review and again immediately before the verdict. Both reads returned exact head `3565c9d483cb29e7729e3003e2b844b46124e18f`, exact base `9785d8e1b71e0d61cf31541af28b910d5be58bd9`, `open`, `draft=true`, `merged=false`.

The full PR changed-file set and per-file patches were read. The exact base/head comparison contains 17 product-branch commits and 13 changed files: the kernel conversation provider, Mahayana Host/FeatureHost test-support bridge, Electron transport plus deterministic E2E regression, Cargo manifests/lockfile, and FCM task/evidence records. There is no product version, Release version, protected-merge rule, or workflow change in PR #2561.

The product branch diverged from merge base `b8fb73466899adc912db67823a8e429987070a6b`; the frozen review base contributes two non-overlapping commits, primarily Chrome/Web Store delivery work plus the fresh-production-failure evidence record. Current canonical `main` was separately re-read as `6b77ac34060e1733dd8d0a900987a5dd2471a0bb`; its three commits after the frozen review base do not overlap the reviewed Mahayana/Electron product paths. Final integration remains the responsibility of the existing protected merge queue and its merge-group gates.

Existing exact-head PR-side Actions were read only; none was rerun. All 16 pull-request workflow runs associated with exact head `3565c9d4...` are `completed/success`, including:

- CI `34704136588` — required aggregate `CI result` success;
- Delivery governance contract `34704136617`;
- Explicit automerge `34704136577`;
- CI latency observability `34704136704`;
- Native mobile quality gate `34704136679`;
- GBF security closure `34704136578`;
- Project portfolio governance `34704136652`;
- Host fast E2E `34704136558`;
- Mahayana Vendor Isolation `34704136654`;
- Platform Control Plane `34704136512`;
- Fabushi self-hosted messaging `34704136645`;
- Electron desktop quality gate `34704136680`;
- Mahayana iOS test-driver contract `34704136537`;
- Mahayana fast checks `34704136567`;
- Computer control security gate `34704136551`;
- Global Dharma Rust `34704136681`.

The active `main-merge-queue` ruleset requires `CI result`; the exact product head has that required context completed/success. Skipped PR-inapplicable post-main jobs are not treated as production acceptance.

All review submissions, top-level comments, and inline review threads were re-read. Historical unresolved/outdated threads remain provenance for earlier heads; they are not counted as a current-head PASS. The relevant earlier findings were independently re-evaluated against the exact current code rather than accepted from replies.

## Independent re-review of the prior `2e65a0a6...` blockers

### P1 — conversation-scoped Mahayana read boundary: CLOSED

The exact head replaces a single global boundary with `read_through_by_conversation: BTreeMap<String, usize>` in the real `KernelConversationProvider` state.

- Persisted bootstrap history is counted independently per conversation in `ConversationState::new`, so every persisted conversation starts read without carrying a stale cross-conversation boundary.
- `unread_count(conversation_id)` filters by the addressed conversation and counts only assistant messages after that conversation's boundary; user messages never increment unread.
- Explicit read acknowledgement updates only the addressed conversation.
- Opening unrelated `codex:agent:research` therefore cannot clear `mahayana-ai:agent:assistant` unread.
- Production FeatureHost `conversation.open` issues `ConversationHistory(limit=200)`; Runtime keeps it at `200`; provider `history_request_marks_read` acknowledges only that explicit-open contract.
- Production background indexing issues `ConversationHistory(limit=2000)`; Runtime clamps it to `500`; provider does not acknowledge at `500`.
- `reset_session` clears visible history and the entire per-conversation boundary map and persists the reset.

Exact-head unit regressions lock conversation-scoped boundaries, persisted initial-read behavior, explicit `200` read acknowledgement, background `500` non-acknowledgement, and reset semantics.

### P2 — true cross-layer production regression: CLOSED

The new deterministic Rust regression is not a state-helper-only test. It constructs a real `MahayanaHost` using the production `RuntimeBuilder` and `KernelConversationProvider`, wraps it in a Production `FeatureHostController`, then drives the real FeatureHost production conversation list/open adapters.

The test proves the path `FeatureHost list/open adapter -> MahayanaHost -> Runtime command + 1..500 history clamp -> KernelConversationProvider` by:

1. producing a visible assistant completion through the runtime-backed deterministic `EngineBackend` and waiting until production `conversation.list` reports assistant unread `1`;
2. opening unrelated `codex:agent:research` through the production open adapter and proving assistant unread remains `1`;
3. issuing runtime history `2000`, which reaches the provider as clamped `500`, and proving unread remains `1`;
4. opening the assistant through the production adapter (`200`) and proving assistant unread becomes `0`.

That directly closes the earlier missing-cross-layer finding. The test-support feature only exposes construction of the same Host/Runtime/kernel path; it does not substitute a mock provider for the production state owner.

## Additional semantic and integration review

### Hidden completion, visible completion, persistence, and unread semantics

`SendMessageRequest.hidden` is propagated into `RuntimeKernelEventBridge`. On `KernelEvent::MessageCompleted`, hidden assistant completion still emits the runtime completion required by background/teach/workflow/group callers, but `record_assistant_completion(..., hidden=true)` does not append it to provider-visible history and it is not persisted; consequently it cannot increment unread. FeatureHost routes tracked background/group completions to background/group events rather than an ordinary visible chat message. A deterministic provider regression verifies hidden completion leaves visible history/unread unchanged, while a subsequent visible assistant completion is stored/persisted and increments unread.

Visible user sends may be stored in visible history but unread counting filters strictly to assistant role, so user messages do not increase unread. Visible assistant completion is recorded before the runtime completion event is emitted, so downstream list refresh observes the authoritative incremented provider state.

### Electron authoritative `conversation.list` refresh and EventTarget regression

The exact Electron transport refreshes authoritative `conversation.list` after both:

- accepted non-Mini-App `conversation.open` (`after-open`); and
- assistant `chat.message` completion (`assistant-message`).

The regression instantiates the real `ElectronMahayanaHostTransport` with a real `EventTarget`-backed fake window. `initialize()` attaches the real `MAHAYANA_COMMAND_EVENT_NAME` listener. `transport.execute(...)` dispatches the actual CustomEvent through that EventTarget, so the attached command observer runs and updates request/operation/conversation mapping; the runtime event is then delivered through the real subscribed observer. The test does not call `observeCommand` directly or replace it with test-only mapping logic. It asserts both authoritative list refreshes and therefore exercises the command-observer lifecycle rather than merely inspecting source text.

### Cargo.lock and dependency surface

The manifest delta adds only test-support plumbing and dev-dependency references (`async-trait` workspace dependency and Mahayana path crates) needed by the cross-layer regression. `Cargo.lock` is synchronized to those manifest references; the lock diff adds dependency names to the existing `mahayana-feature-host` package and reorders/moves an existing package block, but introduces no new external package version/source. Exact-head Mahayana/Global Dharma Rust gates completed successfully, including locked dependency validation.

### Race, deadlock, reentrancy, bootstrap, and persistence review

The new unread boundary state remains behind the existing state mutex. The changed send/completion paths release the state lock before any follow-on `persist_history` lock acquisition; no new state mutex is held across an async `.await`. The explicit-open/completion race has coherent ordering: completion before open is acknowledged by that open; completion after open becomes unread. Per-conversation read boundaries prevent unrelated open/history operations from cross-clearing unread.

Persistence continues to store only visible message history. Read boundaries intentionally do not persist; reboot reconstructs per-conversation boundaries at each persisted conversation's visible message count, so persisted history starts read as specified. Hidden completions are excluded before persistence. Reset clears all boundaries and persists empty visible history. No changed path introduces a new reentrant callback while a state lock is held.

The existing persistence writer snapshots under the state mutex and writes via a unique temporary file + replacement. Multi-operation persistence serialization remains the pre-existing runtime contract; this PR does not weaken that contract or add a new asynchronous write path.

### FCM fail-closed gates and governance truth

No production FCM controller, Release/version workflow, device-source binding, or evidence gate is weakened. The task/WBS/risk/blocker records continue to require the post-merge new canonical SHA, immutable same-source Release, fresh controller-dispatched target, exact App-owned macOS device, all 27 categories, nonzero real remote actions, `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, truthful target/evidence success, playable whole-session video, trace/screenshots/logs, artifact digest, and source lineage. Historical failed targets are not promoted to acceptance.

The acceptance matrix/status files contain conservative historical failure snapshots that lag the latest runtime-unread failure record, while the exact task/WBS/risk/blocker records and live GitHub evidence identify the current repair truth. They do not falsely claim FCM-010.13.11 passed or waive a gate; therefore the lag is non-blocking for this exact-head code review and is not treated as production acceptance.

## Verdict and authorization boundary

**REVIEW-PASS** for repository `bhrumom/fabushi`, product PR `#2561`, task `FAB-P0003 / FCM-010.13.11`, exact head `3565c9d483cb29e7729e3003e2b844b46124e18f`, exact review base `9785d8e1b71e0d61cf31541af28b910d5be58bd9`, `spec_digest=391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307`, `review_key=037fe69d1c84279ffc971208c3454ba7bb5bf9c68a3bc63d9093a3ba0b5d8918`.

- `P0=0`
- `P1=0`
- `P2=0`
- `other blocking=0`

This review authorizes **only** the existing protected merge queue to evaluate and integrate exactly the unchanged product binding above. It does not mark PR #2561 ready, enqueue it, merge it, release anything, dispatch production work, or satisfy post-merge acceptance. Any product head/base/spec binding change invalidates this review. The protected merge queue must still independently pass its own current-main merge-group checks before any merge can occur.
