# TFI-MACOS-INTERACTIVE-001 — macOS 1.2.38 retest candidate

- Protected release base at branch creation: `main@b23026de7cd2bfb2e7da13c60616cd4532898cae`.
- Canonical prior version: `1.2.37`; no macOS `v1.2.37` release exists because Native Electron run `34016277781` was cancelled.
- Last published macOS test release: `v1.2.35` -> `3f633e07cae0b022cce1ff3e6aeb8bfa92aa463d`.
- Last macOS interactive run: `34014501293`, failed truthfully at the external hold; artifact `9983789161`, SHA-256 `d310b348e8857e9702310eb767b8ae15aeb024001d06e9843722155143c2f9b4`.
- Session-lifetime repair: PR #2401 merged as `9a41713f592ef21842c68e36bdc647720af8954d`.
- Late contract-regex repair: PR #2404 merged as `b23026de7cd2bfb2e7da13c60616cd4532898cae`; exact `linux-managed-semantic-desktop` retry job `101440734999` passed.
- Candidate governed macOS test version: `1.2.38`.
- Android `androidVersionCode=29` and iOS `iosBuildNumber=29` remain unchanged.

## Why a new immutable macOS package is required

The v1.2.35 App-owned device stopped after its refresh-token-free CI access token expired during the 25-minute external-control hold. The merged repair keeps the ordinary refresh token private to the GitHub Actions runner, rotates it through the existing account-session store every 240 seconds, and atomically reprojects a fresh bounded refresh-token-free session for the packaged App. The App remains the only gateway/device-registration owner.

The final truth contract now also requires all six `fabushi.app.status/snapshot/find/action/wait/assert` tools plus `ci_session_status`, `ci_session_note`, and `ci_session_finish`, then exact production `settings-logout` only after the READY note and finish request.

## Release / retest contract

Only the exact protected-main merge SHA of this version PR may publish immutable prerelease `v1.2.38`. Whole-session recording must start before installation. The exact v1.2.38 App must log in to the protected CI account and self-register a brand-new `gha-<run>-<attempt>-macos-app` device. KRIS, old devices, pre-existing devices and runner-owned gateways remain forbidden.

The live matrix must cover startup, login, main, conversations, global/current search, send/receive/reply/edit/delete/forward, draft, pin/mute/unread, contacts/groups/bot/agent, actual MiniApp install/open/WebMCP, media/file, notifications/sync/settings/update, then write the exact READY_FOR_LOGOUT PASS note, call `ci_session_finish`, take a fresh App snapshot and invoke `settings-logout`. Always retain full video, screenshots, redacted device-call trace, session-renewal log, Playwright report/trace/video/results, app/system logs, release metadata/digests and final report on pass or fail.