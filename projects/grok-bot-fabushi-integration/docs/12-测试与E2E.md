# 12 测试与 E2E

每个能力提供 contract tests；Electron IPC 提供 allow/deny schema tests；host 提供 mock platform tests；Mahayana 提供 agent/tool integration；桌面通过 Playwright/Electron E2E 覆盖用户路径。

电脑控制 E2E 至少验证：授权成功、明确拒绝、授权过期、目标变化、重复动作、崩溃后恢复、错误可见。来源分支旧 E2E 只能作为输入，需适配 main 架构。
