name: Retired automatic test - standalone commerce site

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024 because this legacy workflow executed product tests automatically.'
