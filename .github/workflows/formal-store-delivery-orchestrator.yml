name: Legacy formal store orchestrator (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Combined Apple + Google automatic store fan-out is disabled by FCM-023.'
          echo 'Use the platform-specific formal release workflow after its E2E gate is calibrated and enabled.'
