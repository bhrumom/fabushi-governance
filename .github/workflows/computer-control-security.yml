name: Computer control security gate
on:
  workflow_dispatch:
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic computer-control security gate paused for 2026-09-05 Mac test release.'
