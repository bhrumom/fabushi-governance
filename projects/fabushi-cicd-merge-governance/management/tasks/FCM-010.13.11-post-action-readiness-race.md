# FCM-010.13.11 — Post-action App Surface semantic readiness

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010.13
- **Status:** in-progress / fail-closed
- **Intake:** `[Fabushi:acc40c75-64fe-440a-939c-bfcfa9c09aac]`; generation-race follow-up `[Fabushi:d3d20158-2ddd-46be-8233-a39cfe7ffada]`
- **Generation-race repair canonical main:** `26348bde9091a8bb8cffa864bb911ad70046953e`
- **Frozen product source:** `7ee12b790e18049d2b9509b0c29128dd2ace690b`
- **Immutable Release tag:** `desktop-1.2.56-7ee12b790e18`

## Proven before this round

- FCM-010.13.8-.10 remain passed and are out of scope.
- PR #2542 proved stable-agent stale-generation rebasing in production.
- PR #2543 proved query-only semantic re-resolution in production.
- PR #2544 added an explicit post-`新建频道` fail-closed wait rather than a blind sleep.
- PR #2545 corrected channel-form App Surface names to `名称` and `描述`.
- PR #2546 added created-channel projection readiness.
- The controller already contained the deterministic `find=95 -> action requires 96` contract and failed closed rather than reusing stale generation/ref.

## Sustained generation-race diagnosis

Production controller run `34673090393` on `main@a0bdf707b24ac4b9e8b9d66046269b3d3503611c` passed its controller contract and protected-account production MCP preflight, then automatically dispatched exact frozen-source target `34673135503/1`, device `gha-34673135503-1-macos-app`.

The controller repeatedly re-resolved current semantic targets (`173 -> 177 -> 178 -> 179 -> 180 -> 182 -> 186 -> 189`) but the eighth stale attempt exhausted the old default budget and failed `stale_app_surface_generation: expected 190, received 189`. This proved stale-target reuse was already fixed; the remaining race was bounded retry-budget exhaustion under sustained App Surface generation churn.

Failure controller artifact: `fcm-010-13-11-external-controller-34673135503-1`, ID `10291612205`, digest `sha256:3f3cd0d2c1e5c464adf5bcc0ee3008816af5107ddbb0b0510605ad68af325f63`.

## Minimal repair and protected merge

PR #2547 kept the fail-closed semantic re-resolution algorithm unchanged and made only the bounded convergence repair:

1. Default generation-sensitive retry budget increased from `8` to `32`; no unbounded retry was introduced.
2. Existing deterministic `find=95 -> action requires 96` regression remained required.
3. Added a deterministic sustained-churn regression with eight consecutive stale generations. It asserts a fresh semantic `find` on every retry and proves the ninth action uses generation/ref `103/g103`, not any previous lease.
4. Added the generation helper to the external-controller workflow path trigger and syntax check.
5. Exact device ID, frozen source SHA, immutable tag, 27 categories, production-account preflight, and `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout` ordering were unchanged.

PR #2547 required CI and `External controller contract` passed. GitHub auto-merge entered the protected merge queue and merged as canonical `main@26348bde9091a8bb8cffa864bb911ad70046953e`.

## Fresh production verification after #2547

Canonical production controller run `34673949330` was automatically started from `main@26348bde9091a8bb8cffa864bb911ad70046953e`.

- `External controller contract`: success.
- `Protected account production MCP preflight`: success. The historical HTTP-400 / disconnected-account diagnosis is obsolete; production MCP connectivity is restored.
- Immutable Release provenance check: success.
- Automatic exact-source dispatch: target `34673991774/1`.
- Target identity: `head_sha=7ee12b790e18049d2b9509b0c29128dd2ace690b`, `head_branch=desktop-1.2.56-7ee12b790e18`.
- Exact App-owned device discovered: `gha-34673991774-1-macos-app`.
- Release install, protected-account login, App-owned registration, Action-owned semantic smoke and secondary packaged Playwright: success.

The repaired retry path crossed the previous generation-race failure point in live production. Multiple stale actions were freshly re-found/rebased and the journey continued far enough to create both self-hosted test channels. No retry-budget exhaustion or stale-generation reuse occurred. The target evidence compiler recorded **56 successful remote device actions**.

## New fail-closed blocker, outside this round's code scope

After crossing the repaired generation race, the journey exposed a distinct semantic-projection failure:

`semantic target not found: {"agentId":"test:peer-legacy:conversation:mahayana-ai:agent:assistant"}`

The controller correctly wrote a FAIL note and called `ci_session_finish`. Because all 27 categories had not passed, it did not emit `READY_FOR_LOGOUT PASS`, did not perform the success-path fresh snapshot, and did not invoke exact `settings-logout`.

Target run `34673991774` therefore completed `failure`, and `Enforce truthful macOS external journey and evidence gate` also completed `failure`, as required by fail-closed acceptance.

Failure evidence is retained as:

- artifact `fabushi-macos-interactive-evidence-34673991774-1`
- artifact ID `10291553529`
- size `160229449` bytes
- digest `sha256:fde3ea98c320218c4716a049bffeeb48a5a27020e23b5b3e0cf155414bf5b0e8`
- 112 uploaded files including device-call trace, generated regression, application/system logs, step screenshots, Playwright report and Playwright trace.

The whole-session recorder step itself started successfully, but the artifact does **not** contain `macos-session.mov`; `screencapture.log` reports `IOServiceMatchingfailed for: AppleM2ScalerParavirtDriver`. This is a second evidence deficiency and must not be represented as a retained full-session video.

## Acceptance decision

- The **generation race repair requested by this round is implemented, protected-merged, regression-tested and verified in production**.
- FCM-010.13.11 remains **in-progress / fail-closed**, because the complete 27-category journey, `READY_FOR_LOGOUT PASS`, success-path `ci_session_finish -> fresh snapshot -> settings-logout`, target-run success, truthful evidence-gate success, and full-session video are not all present.
- FCM-010.13 remains **in-progress**. FCM-010.13.8-.10 remain passed and were not redone.
- The CI latency observer remains intentionally deferred until FCM-010.13.11 is objectively green, preserving the requested sequence.

## Next atomic round

Do not reopen the generation-race implementation unless new stale-generation evidence appears. The next independent round should diagnose the missing legacy assistant semantic projection and the macOS whole-session recorder output, then run a new production controller target with the unchanged frozen source/tag and require the complete original acceptance gate before marking FCM-010.13.11 or FCM-010.13 passed.
