# GBF-403 Evidence — Windows adapter

Added Rust portable adapter using native cross-platform input/capture backends for pointer, drag, text, key chords, scroll and PNG capture. Clean-room Windows UI Automation helper is retained for semantic application surfaces and key alias compatibility. Windows Actions must compile/test the Rust crates and Node semantic contracts before release.
