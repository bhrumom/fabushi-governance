# FCM-010.13.11 — Unread baseline list-state repair

- **Portfolio Project ID / Project Key:** `FAB-P0003` / `FCM`
- **Task ID:** `FCM-010.13.11-unread-baseline-list-state-repair`
- **Parent:** `FCM-010.13` / `FCM-010.13.11`
- **Objective:** Repair the fresh production failure where the FCM-010.13.11 controller verifies the assistant `unread-none` semantic marker after selecting the assistant conversation, even though the marker only exists on the chat-list avatar surface; preserve all existing fail-closed production evidence gates.
- **Source requirements / references:** `[Fabushi:4514cdd1-e2ac-4143-be27-b7885e9468dc]`; original automation-testing objective `https://mp.weixin.qq.com/s/DEg20nFKfrd2urMc3BjntA`; parent `FCM-010.13` / `FCM-010.13.11` acceptance chain.
- **Status:** `IN_PROGRESS / FAIL_CLOSED`
- **Started:** `2026-09-12T10:50:12Z` (fresh target `34689484659` dispatch/start)
- **Updated:** `2026-09-12T11:13:42Z` (independent review identified the mandatory task-record metadata gap)
- **Completed:** `N/A — task remains blocked until a post-merge fresh production run satisfies the full acceptance gate.`

## Scope

### In scope

1. Preserve the exact assistant unread semantic contract: `peer-unread:legacy:conversation:mahayana-ai:agent:assistant` with exact bounded names `unread-none` / `unread-positive`.
2. Move the false unread baseline check to the chat-list surface before the assistant conversation is selected.
3. Keep the positive unread transition check after send and after returning to the chat list.
4. Add a deterministic controller ordering regression and wire it into the production controller CI path filter and `node --test` gate.
5. Extend the packaged Electron/App Surface regression to prove the exact unread node exists as `unread-none` on the chat list and is absent after the assistant conversation is opened.
6. Preserve source SHA, immutable Release, exact App-owned device, <=100 find limits, generation retry, 27-category, READY_FOR_LOGOUT, `ci_session_finish`, fresh snapshot, exact `settings-logout`, controller/target success, truthful-evidence, and whole-session evidence gates.
7. Carry the repair through independent exact-head review, protected merge, exact canonical-main re-read, immutable Release, and one brand-new controller-dispatched frozen-source production target.

### Out of scope

- Reworking already-verified generation-race, find-limit, semantic-name, OAuth, recorder, packaging, or evidence-gate semantics except where required to preserve their existing contract.
- Reusing diagnostic targets `34680986090`, `34681709910`, `34688641913`, or `34689484659`, or any artifact from those runs as final acceptance evidence.
- Weakening exact semantic identity/name matching, reading private message text/unread counts, screenshot/OCR control, credential handling, or bypassing protected merge/review requirements.
- Marking `FCM-010.13.11` or `FCM-010.13` passed before one same fresh production run satisfies every acceptance criterion below.

## Dependencies

- Canonical source before repair: `73b9d70bb951cdbfe7f10e26dc4db3a15b4ef4e8`.
- Existing production FCM-010.13.11 controller and target workflow contracts.
- Existing App Surface semantic projection and exact unread-name repair from the immediately preceding FCM-010.13.11 work.
- GitHub protected `main`, required CI, independent Codex review, immutable desktop Release delivery, GitHub-hosted macOS target, production Fabushi MCP protected account, and App-owned device registration.
- Existing Playwright packaged Electron verification layer; no new runtime dependency is introduced by this repair.

## Fresh failure evidence that opened this task

- **Canonical source before repair:** `73b9d70bb951cdbfe7f10e26dc4db3a15b4ef4e8`
- **Immutable Release:** `desktop-1.2.56-73b9d70bb951`
- **Fresh controller:** `34688597522` attempt 2
- **Fresh target:** `34689484659`
- **Exact failed App-owned device:** `gha-34689484659-1-macos-app`
- **Evidence artifact:** `fabushi-macos-interactive-evidence-34689484659-1`
- **Evidence digest:** `sha256:01a603d5ae6d85c3943170bf05ce2daf62c93312ab575044c1aa91d8ba69b527`

The fresh target was not accepted. Exact Release/source lineage, packaged App Agent smoke, protected login, App-owned registration, Playwright packaged evidence, evidence collection, artifact upload, and playable whole-session recording succeeded, but the production full journey failed in the `send` category before the probe was sent.

The controller repeatedly queried the exact unread semantic contract:

`fabushi.app.find({ agentId: "peer-unread:legacy:conversation:mahayana-ai:agent:assistant", name: "unread-none", limit: 1 })`

and received zero matches until timeout. The controller then wrote a FAIL note and called `ci_session_finish`; the target correctly rejected the run because the required `READY_FOR_LOGOUT PASS` note had never been emitted. Therefore FCM-010.13.11 and FCM-010.13 remain blocked.

## Root cause

The dedicated `peer-unread:*` semantic node is a chat-list avatar semantic. Fresh generated-regression snapshots prove the exact node exists while the chat list is rendered. The pre-repair `send` sequence called `openAssistantConversation()` before verifying the false unread baseline; selecting the assistant conversation unmounted the list-only unread node. The subsequent exact `agentId + name=unread-none` lookup therefore could not succeed even though the semantic projection itself was correct on the list surface.

## Open-source-first survey / reuse decision

This repair inherits and reuses the open-source survey already recorded by the immediately preceding FCM-010.13.11 semantic-name repair. That survey evaluated Radix Primitives (`radix-ui/primitives`, MIT), Microsoft Playwright (`microsoft/playwright`, Apache-2.0), and DOM Testing Library (`testing-library/dom-testing-library`, MIT) for deterministic UI-state exposure and semantic-query testing.

For this newly exposed blocker, the production semantic model is already correct; the defect is controller ordering across two real UI surfaces. No surveyed project provides or is needed as a replacement for Fabushi's production controller/App Surface transport. The relevant reusable upstream pattern is Playwright's stable semantic locator model, and Fabushi already carries Playwright in the packaged Electron E2E chain. The repair therefore **reuses the existing Playwright verification layer without adding or copying third-party runtime code** and keeps the production controller on Fabushi's existing `data-agent-id`/App Surface protocol. Radix's explicit finite-state design remains the provenance for the two-state unread semantic signal; DOM Testing Library remains useful philosophy but is not adopted as a runtime transport.

Security/licensing decision: no new dependency, source copy, binary, credential access, screenshot parsing, unread count, or message-text exposure is introduced. The exact bounded semantic name and production redaction remain unchanged. Existing MIT/Apache-2.0 upstream projects are referenced for architecture/testing patterns only, so this repair creates no new attribution or copyleft obligation.

## Branch / commit / PR

- **Branch:** `fix/fcm-010-13-11-unread-baseline-list-state`
- **PR:** `#2557` — `fix(fcm): verify unread baseline on chat-list surface`
- **Implementation / governance commits represented before this metadata-completion update:**
  - `83243e8fe664352cc9527d8480cad220825dcdfa` — initial minimal controller/E2E/task-record repair head.
  - `7e09247908e8aaaba18d83aa44ff973eee2df63d` — head reviewed independently; review found the missing CI wiring P2 and therefore was not accepted.
  - `934e28252969086d57ad09fee1ce5789f81d6cb7` — wires the unread ordering regression into the FCM controller workflow; CI proved the new test executes and passes. Independent re-review then identified only this task-record metadata gap as a P1.
- **Metadata-completion commit:** authoritative SHA is the Git commit containing this file update in PR #2557; it must receive a fresh exact-head independent review before merge. This avoids pretending a pre-commit SHA is the SHA of its own commit.

## Implementation summary

1. Keep the exact unread agent id and exact `unread-none` / `unread-positive` name contract unchanged.
2. In the `send` category, navigate to the chat-list surface and verify `unread-none` before selecting the assistant conversation.
3. Only after the baseline is recorded may the controller open the assistant conversation and send the probe.
4. Add a controller ordering contract proving `chat list -> unread-none baseline -> open assistant -> send`.
5. Wire that ordering contract into `.github/workflows/fcm-010-13-11-macos-external-controller.yml` both as a PR path trigger and as an executed `node --test` input.
6. Extend the packaged Electron/App Surface regression to prove the exact unread node is findable as `unread-none` on the chat list and absent after the assistant conversation is opened.

## Acceptance criteria

1. Required CI and a genuinely independent review pass the **same exact PR head**; any head change invalidates the prior review for merge purposes.
2. Protected merge completes and the exact canonical `main` SHA is re-read from GitHub after merge.
3. An immutable desktop Release is published for that exact canonical SHA; `target_commitish`, tag dereference, and macOS ZIP/digest all bind to the same source.
4. The production FCM-010.13.11 controller automatically dispatches one new frozen-source target from that Release; no prohibited old target/artifact is reused.
5. That one same fresh run proves the exact App-owned macOS device, all 27 required categories PASS, and non-zero real remote actions.
6. The exact final sequence is proven: `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout invoke`.
7. Controller and target both conclude success and the truthful target evidence gate concludes success.
8. Retained evidence includes a playable whole-session video, trace, screenshots, logs, artifacts, artifact digest, exact source SHA, Release tag, target run, attempt, and device lineage.
9. Only after criteria 1–8 may FCM-010.13.11 and FCM-010.13 be marked passed; then run the CI latency observer and continue the original rapid-automation iteration objective.

## Verification method / checks

- Exact-head GitHub PR and review inspection; reject reviews that name a different commit.
- GitHub Actions exact-head required checks, including FCM external-controller contract, CI, Electron desktop quality, delivery governance, security/governance lanes, and applicable Chrome release validation.
- FCM contract log must show the ordering test executed, not merely existed in source.
- Packaged Electron/App Surface Playwright E2E must use the real App Surface client and assert exact unread list presence plus selected-conversation absence.
- After merge: GitHub branch/ref/Release API checks for canonical SHA and immutable Release/tag/asset lineage.
- Production controller/target logs and MCP evidence for exact device identity, 27 category PASS records, non-zero remote actions, READY/finish/snapshot/logout order, and controller/target success.
- Target evidence artifact inspection for report, playable `macos-session.mov`, Playwright trace, screenshots, logs, generated regression, and artifact digest.
- CI latency observer output after final acceptance to select the next measured bottleneck.

## Current CI / E2E / security / performance / release evidence

- On exact head `934e28252969086d57ad09fee1ce5789f81d6cb7`, FCM external-controller contract passed and its log explicitly executed `fcm-010-13-unread-baseline-surface-contract.test.js`; all 14 controller tests passed.
- On the same head, CI, Delivery governance, Explicit automerge contract, GBF security closure, Project portfolio governance, CI latency observability, Chrome Extension Web Store, and Electron desktop quality gate passed before this metadata-only update.
- Computer-control security cross-platform Rust jobs were still running when this metadata gap was identified; their pre-update result is diagnostic only because this metadata update creates a new exact head.
- Independent review of `7e092479...` found the missing CI wiring P2; it was fixed. Independent re-review of `934e282...` found this mandatory task-record metadata P1; therefore neither old head is treated as REVIEW-PASS.

## Post-main / build / package / required-E2E / Release evidence

`N/A — this repair has not merged yet. Canonical-main package, Release, fresh production E2E, whole-session video, trace, screenshots, reports, logs, target artifact digest, and source lineage are mandatory post-merge evidence and are explicitly blocked on exact-head REVIEW-PASS plus protected merge.`

## Blockers and risks

### Current blockers

1. A new exact-head independent REVIEW-PASS is required after this metadata update.
2. Required exact-head CI must pass again after this metadata update.
3. Protected merge, canonical-main SHA, immutable Release, and fresh production target do not exist for this repair yet.
4. FCM-010.13.11 / FCM-010.13 remain blocked until one same fresh production run satisfies the full acceptance gate.

### Risks

- UI-surface ordering can regress if the chat-list-only unread node is queried while a conversation surface is selected; the controller CI regression now guards this ordering.
- A green PR contract alone cannot prove production behavior; final acceptance remains bound to a brand-new immutable-Release production run.
- Reusing old targets/artifacts or mixing evidence across runs would produce false lineage; acceptance is deliberately single-run and exact-source.
- Runner/network flakiness can occur; retries are acceptable only when they still create a brand-new production target where required and never convert a failed target into accepted evidence.

## Next action

Obtain a fresh independent read-only review for the exact PR head produced by this metadata update and require all exact-head CI gates. Only after REVIEW-PASS may PR #2557 enter the protected merge queue; after merge, re-read canonical `main`, require an immutable exact-source Release, then let the production FCM-010.13.11 controller auto-dispatch a brand-new frozen-source target and apply the full acceptance gate above.
