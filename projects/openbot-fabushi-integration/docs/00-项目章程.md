# 项目章程

Problem: Fabushi already contains an OpenBot-style visual/reply layer, but the original objective is broader than a stylesheet and static contract. The remaining work must be proven against OpenBot's feature surface and a real packaged journey.

Deliverable: evidence-backed feature matrix, minimal gap fixes only, visual regression at a fixed viewport/theme, and same-run packaged evidence. Success is equivalence where technically appropriate, not a duplicate OpenBot runtime.
