# 领域模型与协议

- **项目**：Fabushi Telegram 全量融合
- **文档 ID**：DOC-04
- **版本**：v1.0
- **状态**：BASELINE
- **基线日期**：2026-08-22
- **源计划**：`../source/完整telegram融合进fabushi.txt`

> 本文档由源计划结构化拆分而来。源计划未明确的管理字段会标记为“项目管理补充/待确认”，避免把推导内容冒充既有事实。

## 统一数据模型

必须先稳定以下核心 ID 与实体：

1. UserId
2. DeviceId
3. ContactId
4. AgentId
5. BotId
6. ConversationId
7. MessageId
8. TopicId
9. ChannelId
10. GroupId
11. MediaId
12. MiniAppId
13. MerchantId
14. PaymentIntentId
15. OrderId
16. CallId
17. SyncCursor
18. EventId

Conversation 统一抽象：
- DirectUser
- DirectAgent
- Group
- Channel
- Topic
- System
- MiniAppSession

Participant 统一抽象：
- User
- Agent
- Bot
- Service

Message 统一 Envelope：
- message_id
- conversation_id
- sender
- timestamp
- client_sequence
- server_sequence
- reply_to
- thread/topic
- content
- entities
- attachments
- reactions
- edit_version
- delivery_state
- encryption metadata
- permissions
- metadata/extensions

消息 Content 类型至少覆盖：
- Text
- Markdown/RichText
- Image
- Video
- Audio
- Voice
- File
- Sticker
- GIF/Animation
- Contact
- Location
- Poll
- MiniApp
- Payment
- ServiceEvent
- AgentToolEvent
- CallEvent


============================================================

## 自研通信协议与网络层

目标：构建 Fabushi 自己的高性能实时通信协议，不复用 Telegram 网络。

第一优先级：
- TLS 1.3
- WebSocket 作为稳定基础通道
- HTTP/2 或 HTTP/3 作为 API/下载通道
- QUIC 作为后续高性能实时通道
- protobuf/compact binary encoding
- 请求 ID / trace ID
- ack
- retry
- timeout
- reconnect
- exponential backoff
- heartbeat
- connection migration（后续）

客户端发送状态：
Draft -> Queued -> Sending -> ServerAccepted -> Delivered -> Read
                             -> Failed -> Retrying

必须解决：
- 重复发送
- 消息幂等
- 网络断开
- 乱序
- 多端竞争
- 编辑/删除竞争
- 同一消息多设备 ack
- 大量未读增量
- 断线重连后的 gap recovery

增量同步：
- 全局 SyncCursor + conversation cursor
- 每次客户端带 last cursor
- 服务端返回 delta events
- 发生 gap 时自动补洞
- cursor 失效时支持 snapshot + delta 恢复


============================================================

## 2026-09-05 — Evidence identity is not a product protocol field

The MAINSAFE exact-head defect is explicitly a CI/evidence-orchestration concern, not a new messaging protocol or domain-model requirement.

- Do not add PR SHA, merge-group SHA, workflow identity, checkout ref or CI metadata to any product protocol/entity solely to solve `TFI-M6-MAINSAFE-001-VERSION-EXACT-HEAD-CHECKOUT-001`.
- The acceptance object is external repository evidence: PR event actual worktree HEAD must equal the final product head; merge-group event actual worktree HEAD must equal the current queue group SHA.
- Product protocol, Rust schemas and runtime state remain unchanged by this task.
- If implementation appears to require protocol/schema/application changes, stop and return to Architecture; that would exceed the frozen minimal task.
