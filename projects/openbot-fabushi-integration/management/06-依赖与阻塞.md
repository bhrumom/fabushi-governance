# 依赖与阻塞

Dependencies: Mahayana/MSR runtime (FAB-P0005), computer/live-control surfaces including RDF (FAB-P0009), protected review/merge and exact-main delivery (FAB-P0003).

Current blockers to `complete`:

1. OBF product changes are not yet merged to canonical `main`, therefore no exact-main packaged Release exists for this task.
2. The current connected device inventory exposes only the Linux control host; the macOS/Windows App devices are offline, so this chat cannot truthfully execute the required fresh packaged desktop visual/video chain yet.
3. The user-supplied reference image is available to this task and has been fingerprinted/cropped, but no durable post-main workflow artifact containing that reference has yet been established; `OBF_REFERENCE_SCREENSHOT` intentionally fails closed when absent.
4. The required real-provider run must still demonstrate live `running`, `completed`, and intentionally-safe `failed` action states plus the final assistant completion. PR test-host lifecycle is not accepted as a substitute.
