# FCM-010.13.11 — Post-action App Surface semantic readiness

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010.13
- **Status:** in-progress
- **Intake:** `[Fabushi:acc40c75-64fe-440a-939c-bfcfa9c09aac]`
- **Baseline:** `main@4d7f730aaf641f3057a2fe2a05c22a044e9e151b`
- **Branch:** `fix/fcm-010-13-11-channel-projection-readiness`

## Proven before this task

- FCM-010.13.8-.10 remain passed and are out of scope.
- Frozen source stays `7ee12b790e18049d2b9509b0c29128dd2ace690b`; immutable Release tag stays `desktop-1.2.56-7ee12b790e18`.
- PR #2542 proved stable-agent stale-generation rebasing in production.
- PR #2543 proved query-only semantic re-resolution in production.
- PR #2544 added an explicit post-`新建频道` fail-closed wait rather than a blind sleep.
- PR #2545 corrected the channel form selectors to the App Surface accessible names `名称` and `描述`.
- Production controller run `34672733193` auto-dispatched target `34672786208` attempt `1`, exact device `gha-34672786208-1-macos-app`, source/tag unchanged. Exact Release resolution, installation, protected-account login, App-owned registration and frozen Action-owned packaged semantic smoke all passed.
- The production journey proved the repaired generation behavior again: stable semantic targets were re-found with new generations, `描述` rebased `99 -> 102`, and query-only `创建频道` rebased `105 -> 110`; no stale generation-bound ref was reused.

## Current narrow blocker

The channel form now resolves and the `创建频道` action succeeds. After that action the journey correctly waits for the messenger input to become visible, but it immediately performs `find({text: name})` for the newly created channel projection. On target `34672786208`, that first post-create find ran at generation `112` before the channel row/text projection was present and failed `semantic target not found`.

This is the next post-action App Surface readiness edge in the same controller chain, not an MCP connectivity problem, not a stale-generation retry failure, and not grounds to weaken any evidence gate.

Controller failure artifact for this run: `fcm-010-13-11-external-controller-34672786208-1`, artifact ID `10291307107`, digest `sha256:5fd24c4fd048d8f3d80ea27e33d00913830ca9061a359704977fe3d46a92913f`.

## Atomic acceptance

1. Preserve the already-correct App Surface field names `名称` and `描述` and all generation retry behavior.
2. After successful `创建频道` and the existing messenger-input readiness gate, explicitly wait fail-closed for the newly created channel name/text to be present before resolving its semantic peer row.
3. Extend the controller contract to lock `创建频道 -> messenger input visible -> created channel text present -> find created channel` ordering; do not add a blind sleep.
4. Do not change the frozen source/tag, exact `gha-<run>-<attempt>-macos-app` rule, 27 required categories, or `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh snapshot -> exact settings-logout` order.
5. Merge only through required CI and the protected merge queue.
6. A new production controller run must auto-dispatch a new exact frozen-source target and produce complete green evidence before FCM-010.13.11 or FCM-010.13 can be passed.

## Completion evidence required

Exact new App-owned device, all 27 categories PASS, nonzero real remote actions, `READY_FOR_LOGOUT PASS`, successful `ci_session_finish`, fresh App snapshot, final exact `settings-logout`, target run success, truthful evidence gate success, and retained trace/log/screenshots/full-session video/artifact/digest.
