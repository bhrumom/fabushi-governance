# TFI-MACOS-INTERACTIVE-001 — macOS 1.2.47 protected retest candidate

- Project: `FAB-P0001 / TFI`
- Task: `TFI-MACOS-SEMANTIC-MESSAGE-ACTIONS-001`
- Status: `TESTING`
- Stable semantic target fix: PR `#2419`, merge `2ec0fbd5cc30a61e043e5050dff4a70e8a33e06e`.
- PR #2419 exact-head Electron desktop quality run: `34022856663` (`success`). It proved an exact stable message target remains actionable beyond the intentional 500-element snapshot cap and all required message action IDs resolve before Reply.
- Latest canonical base before this candidate: `main@c1533dadc47eca5cfe99ff6ff047d8f1d2e1fb8f`.
- Canonical semantic version at branch cut: `1.2.46` (advanced by Android PR `#2430` after the first macOS branch cut).
- Existing immutable macOS prerelease before this candidate: `v1.2.45`, target `48bbcbbb2094e296e2fda0ed829bee68af1a12fa`.
- Candidate governed macOS test version: `1.2.47`.
- Android `androidVersionCode=29` and iOS `iosBuildNumber=29` remain unchanged.

## Why a new version is required

The macOS semantic stabilization is already merged, but canonical `main` advanced through independent Windows/Android/gateway work after that merge. While this macOS PR was being prepared, Android PR `#2430` protected-merged and advanced the canonical global semantic version from `1.2.45` to `1.2.46` at `c1533dadc47eca5cfe99ff6ff047d8f1d2e1fb8f`. A valid macOS retest must therefore resync to that exact canonical main and use strictly newer `1.2.47` instead of colliding with Android 1.2.46 or mutating any existing Release.

## Release / acceptance contract

After protected merge, the exact accepted main SHA must build, sign, notarize, staple, hash and publish immutable prerelease `v1.2.47` in GitHub Actions. The macOS interactive Actions run must install that Release, authenticate the protected test account, and let the installed Fabushi App self-register a brand-new `gha-<run>-<attempt>-macos-app` device. KRIS, historical devices and runner-owned gateways are forbidden.

External acceptance must genuinely execute `fabushi.app.status`, `fabushi.app.snapshot`, `fabushi.app.find`, `fabushi.app.action`, `fabushi.app.wait`, `fabushi.app.assert`, `ci_session_status`, `ci_session_note`, and `ci_session_finish`; cover messaging, search, navigation/settings, Agent, attachments/material, sync and final logout; and retain whole-session video, step-labelled screenshots, device-call trace, Playwright trace/report/results, app/system logs and final report through an always-upload path on pass or fail.
