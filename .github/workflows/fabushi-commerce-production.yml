name: Retired automatic test - Fabushi commerce production

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024. Stable production promotion requires Fabushi official MCP validation first.'
