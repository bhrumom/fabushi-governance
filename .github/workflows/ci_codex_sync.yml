name: Retired automatic test - Codex sync

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024. Formal validation must use interactive-runner-mcp.yml and Fabushi official MCP on an App-owned Action Runner device.'
