# FCM-010.13.11 — semantic projection + truthful macOS recorder blockers

Status: in-progress
Parent: FCM-010.13
Project: FAB-P0003 / FCM
Source round: user directive `[Fabushi:8fed0a73-0a7f-4588-9e87-dea00c0e4c75]`, 2026-09-12
Original method goal: continue the Shopify-like GitHub Actions automation loop from `https://mp.weixin.qq.com/s/DEg20nFKfrd2urMc3BjntA`, keeping core/runtime validation decoupled from UI validation and using truthful, fast automated evidence.

## Frozen scope

The already verified generation-race repair is frozen. Do not change generation-sensitive retry semantics, retry budget, stale-generation re-resolution, device identity, immutable-source binding, the 27 journey categories, or the READY -> finish -> fresh snapshot -> logout gate except where needed to assert existing acceptance evidence.

This round fixes only two production blockers:

1. Restore the canonical semantic projection `test:peer-legacy:conversation:mahayana-ai:agent:assistant` on the correct visible App Surface without loosening the controller query. Production evidence showed channel creation intentionally switches the UI to the `channels` section, where the chat-only assistant peer is not projected into the semantic DOM. The journey must explicitly return to `聊天` before resolving the exact assistant id. Add a deterministic real Electron/App-Surface regression that proves the failing channel-surface state is `count=0`, then returns to `聊天`, resolves exactly one canonical assistant peer, and invokes it.
2. Replace the GitHub-hosted macOS whole-session recorder's false-positive startup contract with a recorder that actually yields a playable `macos-session.mov`. Detect the GitHub-hosted Apple Silicon display environment, including `AppleM2ScalerParavirtDriver`, record the native recorder probe result, and fail closed unless the selected recorder path passes capture and video-playability validation.

## Starting production evidence

- Canonical main before this round: `0ae214df5d2ca4b593253b5b9395f871b194473e`.
- Generation-race repair remains frozen from canonical `26348bde9091a8bb8cffa864bb911ad70046953e` / PR #2547 and the follow-up production evidence already recorded on main.
- Production controller: run `34673949330`.
- Fresh frozen-source target: run `34673991774`, attempt 1.
- Exact App-owned device: `gha-34673991774-1-macos-app`.
- Protected-account preflight, exact-device discovery, and the first journey categories succeeded.
- Production semantic failure: `semantic target not found: {"agentId":"test:peer-legacy:conversation:mahayana-ai:agent:assistant"}` immediately after the two self-hosted channel creations when the journey reopened the assistant conversation.
- Controller failure artifact: `fcm-010-13-11-external-controller-34673991774-1`, artifact id `10291836275`, digest `sha256:c28a9857212a7516cd401c25be32e328652f6e91e8d65a03729cdef37a10ddf0`.
- The target workflow's `Start whole-session macOS recording` step reported success because the background `screencapture -v` PID was alive after three seconds, but the final evidence set contained no playable `macos-session.mov`. This is an infrastructure/evidence failure and must remain fail-closed.

## Atomic acceptance

Implementation and narrow regressions:

- [ ] The production journey explicitly returns to the `聊天` section before every assistant-peer resolution; the exact assistant semantic id is not broadened or replaced.
- [ ] Deterministic real Electron regression reproduces the two-channel state, proves the assistant is absent from the channel-only surface, then switches to `聊天` and proves exactly one canonical assistant projection.
- [ ] The same real Electron App Surface regression resolves exact `test:peer-legacy:conversation:mahayana-ai:agent:assistant` through `fabushi.app.find`, then invokes it through generation-bound `fabushi.app.action`.
- [ ] macOS recorder records environment/driver evidence, rejects false-positive native recorder startup, and has a verified fallback for the current GitHub-hosted runner.
- [ ] Recorder preflight and final validation decode at least one video sample and require positive duration; file existence/size alone is insufficient.

Merge/release gates:

- [ ] Required PR CI succeeds.
- [ ] PR merges only through the protected merge queue.
- [ ] New exact canonical main SHA is read back from GitHub.
- [ ] Same-source desktop/native/release chain produces a new immutable macOS release and automatically launches a fresh FCM-010.13.11 production controller/target chain.

Production acceptance — all must hold in the same fresh target attempt:

- [ ] Exact device id is `gha-<target-run>-1-macos-app`.
- [ ] All 27 journey categories PASS.
- [ ] Remote App actions are real and non-zero.
- [ ] `READY_FOR_LOGOUT PASS` precedes `ci_session_finish`.
- [ ] After finish, a fresh App snapshot succeeds and the exact `settings-logout` semantic action succeeds.
- [ ] Target workflow conclusion is success.
- [ ] Truthful evidence gate is success.
- [ ] Evidence preserves a playable `macos-session.mov`, device-call trace, logs, step screenshots, workflow/report metadata, uploaded artifact, and artifact digest.

Parent gate:

- FCM-010.13.11 and FCM-010.13 remain blocked until every production acceptance item above is verified from GitHub/Actions evidence. No partial pass.

## After parent acceptance

Immediately run the dedicated CI latency observer on the new canonical state. Use measured data, not estimates, to identify `macos-packaged-interactive` or the next largest over-budget validation surface; create/follow the next atomic optimization task from that evidence rather than broadening this repair.

## Round log

- 2026-09-12: task created from the live production failure. Scope explicitly freezes the completed generation-race work and limits implementation to semantic projection and truthful whole-session recording.
- 2026-09-12: real Electron/Rust-host regression disproved the speculative preload-payload hypothesis. Source inspection and two failing real-host runs showed channel creation intentionally sets `section=channels`; `fabushi.app.find` correctly exposes only the current semantic DOM, so the assistant chat peer is absent until the journey returns to `聊天`. The speculative preload changes were reverted; the retained semantic fix is the surface transition plus exact-id resolution evidence.
