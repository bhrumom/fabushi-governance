# 10 安全权限与敏感输入

权限采用 deny-by-default。renderer 不持有系统能力。敏感输入请求必须绑定请求 id、调用能力、目标资源和到期时间；拒绝/到期后不可重用。

对 computer control、shell、浏览器、文件、麦克风/摄像头建立 threat model：提权、prompt injection、UI spoofing、replay、confused deputy、path traversal、command injection、secret leakage。
