# 依赖与阻塞

| ID | Dependency | Role | Final state |
|---|---|---|---|
| FCM-D001 | GitHub protected `main` + merge queue | final merge safety | available / verified |
| FCM-D002 | GitHub Actions metadata APIs | FCM-002 telemetry | available / verified |
| FCM-D003 | `CI result` | merge and release-source safety | available / verified |
| FCM-D004 | `Electron desktop result` | macOS release evidence | release gate enforced |
| FCM-D005 | direct `Native Android` + `Native iOS` checks | mobile release evidence | available / verified on exact source |
| FCM-D006 | GitHub-hosted runners | CI execution/latency | available; queue delay measured |
| FCM-D007 | Store credentials/signing material | actual store upload only | external operational dependency; not stored here |
| FCM-D008 | authenticated protected CI test-account Fabushi dynamic-device MCP | FCM-010.13 App-owned complete macOS journey | **available / restored** — production controller `34673949330` protected-account preflight succeeded, exact run-owned device was discovered, and 56 successful remote device actions were recorded on target `34673991774/1` |
| FCM-D009 | legacy assistant peer semantic projection | FCM-010.13.11 27-category journey | **blocking** — fresh production journey fails closed because `test:peer-legacy:conversation:mahayana-ai:agent:assistant` is not present on the current semantic surface |
| FCM-D010 | macOS GitHub-hosted whole-session recorder output | FCM-010.13.11 mandatory visual evidence | **blocking** — recorder step starts, but fresh failure artifact lacks `macos-session.mov`; `screencapture.log` reports `IOServiceMatchingfailed for: AppleM2ScalerParavirtDriver` |

## FCM-010.13.11 current closure state

The historical MCP HTTP-400 / `We couldn't connect your account` blocker is obsolete. Production connectivity was re-proven after generation-race repair #2547: controller run `34673949330` passed protected-account preflight, automatically dispatched exact frozen-source target `34673991774/1`, found exact App-owned device `gha-34673991774-1-macos-app`, and drove 56 successful real remote device actions.

The sustained App Surface generation race is also closed for this round. PR #2547 retained fresh semantic re-resolution on every stale generation, increased the bounded default retry budget from 8 to 32, kept the deterministic `find=95 -> expected 96` regression, added an eight-stale sustained-churn regression, passed required CI and merged through the protected queue as `main@26348bde9091a8bb8cffa864bb911ad70046953e`. The fresh production journey crossed the old stale-generation failure point without exhausting the new budget.

The current failure is later and semantically distinct: `semantic target not found: {"agentId":"test:peer-legacy:conversation:mahayana-ai:agent:assistant"}`. The controller correctly wrote a FAIL note and requested `ci_session_finish`; it did not emit the READY note or execute the final success-path snapshot/logout sequence. Target `34673991774` and the truthful evidence gate therefore ended `failure`.

Evidence remains retained as `fabushi-macos-interactive-evidence-34673991774-1`, artifact ID `10291553529`, size `160229449`, digest `sha256:fde3ea98c320218c4716a049bffeeb48a5a27020e23b5b3e0cf155414bf5b0e8`, 112 files. Trace/log/screenshots/Playwright report and trace are present, but the full-session MOV is not.

FCM-010.13 remains `in-progress / fail-closed`. The next CI latency observer / bottleneck-compression round stays deferred until FCM-010.13.11 reaches the original complete green acceptance; it must not run merely because the generation-race subproblem is fixed.
