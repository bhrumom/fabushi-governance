# 依赖与阻塞

| ID | Dependency | Role | Final state |
|---|---|---|---|
| FCM-D001 | GitHub protected `main` + merge queue | final merge safety | available / verified historically; every new repair still requires fresh exact-head review/CI + queue evidence |
| FCM-D002 | GitHub Actions metadata APIs | FCM-002 telemetry | available / verified |
| FCM-D003 | `CI result` | merge and release-source safety | available / verified |
| FCM-D004 | `Electron desktop result` | macOS release evidence | release gate enforced |
| FCM-D005 | direct `Native Android` + `Native iOS` checks | mobile release evidence | available / verified on exact source |
| FCM-D006 | GitHub-hosted runners | CI execution/latency | available; queue delay measured |
| FCM-D007 | Store credentials/signing material | actual store upload only | external operational dependency; not stored here |
| FCM-D008 | authenticated protected CI test-account Fabushi dynamic-device MCP | FCM-010.13 App-owned complete macOS journey | **available / verified** — latest fresh target `34691502192/1` registered exact App-owned device `gha-34691502192-1-macos-app` and recorded 26 real remote actions before the semantic list-state failure |
| FCM-D009 | legacy assistant peer semantic projection | FCM-010.13.11 27-category journey | **closed / verified** — fresh production evidence after the original `34673991774` failure resolves the exact assistant target; the latest `34691502192` failure is later and specifically concerns the list-only unread node while the assistant remains selected |
| FCM-D010 | macOS GitHub-hosted whole-session recorder output | FCM-010.13.11 mandatory visual evidence | **closed / verified** — fresh production artifacts contain a playable whole-session recording; latest target `34691502192` reports `recorderPlayable=true` |
| FCM-D011 | `fabushi.app.find` production schema maximum `limit<=100` | FCM-010.13.11 semantic queries | **closed / guarded** — the >100 production contract failure was repaired; current controller uses bounded finds and the latest failure occurred on an exact `limit:1` unread query, not schema rejection |
| FCM-D012 | assistant-active -> non-assistant peer -> chat-list transition before exact `unread-none` | FCM-010.13.11 `send` category | **blocking** — canonical `a504f50ef6c8520517e91157bcc5769527d6e30a`, controller `34691460208`, target `34691502192` timed out on exact `unread-none` because selecting Chats alone does not clear the active assistant peer; current repair explicitly selects channel A before returning to Chats |
| FCM-D013 | exact-head independent no-finding review + protected merge queue | current FCM-010.13.11 repair governance | **blocking until re-proven** — PR #2557 final head was reviewed but had two P1 findings and its automerge guard reported protected-path authorization/queue skip; the current repair must obtain a new exact-head REVIEW-PASS before queue entry and must not retroactively treat #2557 as a pass |

## FCM-010.13.11 current closure state

Historical blockers are preserved as history but are no longer the current reconstruction state. The generation race was repaired and production-verified by #2547. Later fresh production rounds independently verified the exact legacy-assistant semantic projection and a playable whole-session recorder. A subsequent round exposed and repaired the `fabushi.app.find limit=200` schema violation, and later repairs established exact `unread-none` / `unread-positive` naming plus the list-only nature of the unread semantic node.

The latest canonical production truth is `main@a504f50ef6c8520517e91157bcc5769527d6e30a` with immutable tag `desktop-1.2.56-a504f50ef6c8`, production controller `34691460208`, and automatically dispatched target `34691502192/1`. The target used exact App-owned device `gha-34691502192-1-macos-app`, executed 26 real remote actions, completed packaged Playwright successfully and retained a playable recorder, but failed in `send` while polling exact `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-none`. The query itself was valid (`limit:1`); it returned zero matches because the assistant conversation remained the active peer and the unread semantic node exists only on the unselected chat-list avatar surface.

The failure was truthful: the journey wrote a FAIL note and called `ci_session_finish`; it did not write `READY_FOR_LOGOUT PASS` and therefore did not execute the accepted success sequence `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`. Target and controller both ended failure. Evidence is retained as artifact `fabushi-macos-interactive-evidence-34691502192-1`, artifact id `10296494233`, digest `sha256:4d06e44f654f4b571e50c349d991a97f980db1125f81289ffef7dfa377747cf6`.

The current atomic repair is `management/tasks/FCM-010.13.11-unread-deselect-governance-repair.md`. It must establish the real list-state precondition, cover it in deterministic controller + packaged App Surface regressions, synchronize project control records, pass required CI and a genuinely independent no-finding review on one exact head, merge only through the protected queue, and then produce a new canonical-SHA immutable Release plus a brand-new controller-dispatched frozen-source production target. No old target or artifact can satisfy final acceptance.

FCM-010.13.11 and FCM-010.13 remain `in-progress / fail-closed`. The CI latency observer remains deferred until one same new production run proves all 27 categories PASS, non-zero real remote actions, the exact READY/finish/fresh-snapshot/logout ordering, controller/target success, truthful evidence success, and complete playable video/trace/screenshots/logs/artifact digest/source lineage.
