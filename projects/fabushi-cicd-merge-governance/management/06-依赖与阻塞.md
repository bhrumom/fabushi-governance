# 依赖与阻塞

| ID | Dependency | Blocking relation | Fallback / response | Status |
|---|---|---|---|---|
| FCM-D001 | GitHub protected `main` + merge queue | required for final merge safety | do not direct-merge around protection | active / available |
| FCM-D002 | GitHub Actions metadata APIs | required for FCM-002 telemetry | report observer failure; never weaken product CI | active / available |
| FCM-D003 | `CI result` check | required for merge and release-source gate | fix CI; no release bypass | active / available |
| FCM-D004 | `Electron desktop result` | required for macOS store delivery | fix/obtain green Electron quality gate | active / available |
| FCM-D005 | `Native mobile result` | required for iOS/Android store delivery | fix/obtain green native quality gate | active / available |
| FCM-D006 | GitHub-hosted runners | affects latency and validation availability | telemetry distinguishes queue delay; retry CI without lowering gates | active |
| FCM-D007 | Store credentials/signing material | required only for actual store upload | keep task release-blocked if missing; never persist secrets in project files | external |

## Current blockers

No implementation blocker is accepted as a reason to skip protected PR/CI/merge-queue verification. PR #1999 remains in progress until all defined checks pass and canonical main is verified.
