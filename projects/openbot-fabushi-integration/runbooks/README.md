# Runbooks

Use the repository's existing protected merge, canonical-main packaged E2E and Release runbooks. OBF adds no deployment system. For visual acceptance, run at the task-defined fixed viewport/theme, capture evidence on `always()`, and fail closed when reference/runtime evidence is missing.
