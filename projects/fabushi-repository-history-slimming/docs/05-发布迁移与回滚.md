# 发布、迁移与回滚

迁移前公告受影响的 commit/branch/tag OID 变化，保存远端 refs 和保护规则快照。迁移窗口内只允许执行已审核的 ref 更新。成功后要求重新 clone/读回审计并恢复保护；失败时停止后续更新，按原始 refs 清单恢复受影响名称，并保留失败证据。

本项目不是应用版本发布，不创建产品 Release，不改变 app version。回滚目标是恢复原始 ref 名称到迁移前 OID，而不是恢复工作树缓存。
