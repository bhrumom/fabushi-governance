name: Electron macOS hot package
on:
  workflow_dispatch:
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the single Native Electron macOS test release workflow.'
