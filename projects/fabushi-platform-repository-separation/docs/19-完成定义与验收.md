# 完成定义与验收

## Project registration

- [ ] `FAB-P0013` 在 canonical `PORTFOLIO.json` 注册，`PROJECT.yaml` 身份一致。
- [ ] `Project portfolio governance` 通过，注册 PR 合入 `main`，并回填准确的
  `first_canonical_main_commit`。

## Repository topology

- [ ] 目标矩阵中的每个 GitHub repo 已创建并可读，名称、owner、可见性和默认分支有读回证据。
- [ ] 每个 repo 只保留自己的平台源码和明确版本化的 Core 依赖；无跨平台源码混放。
- [ ] 每个 repo 有 README、LICENSE/NOTICE、CODEOWNERS、独立 CI 和 branch protection。

## Migration integrity

- [ ] 每个 repo 记录源 SHA、导出命令/版本、路径清单、源/目标 refs 映射和 checksum。
- [ ] 关键产品资产和 license/provenance 审计通过；无 secrets/个人数据泄漏。
- [ ] 源仓库在切换完成前可直接回滚，未删除历史、Release 或不可变标签。

## Delivery

- [ ] 每个平台/CLI 的构建、测试、打包和模拟用户 E2E 在目标仓库 GitHub Actions 通过。
- [ ] 必要的截图、视频、trace、平台报告、日志和 artifacts 与准确 SHA/run 绑定并保留。
- [ ] 目标仓库 Release、部署入口、updater、CLI 下载入口和商店/分发流程已验证。
- [ ] 源仓库最终变为治理/迁移控制面，且没有仍由平台构建使用的相对路径。

在上述所有证据完成前，PRS 只能标记 `in-progress`、`blocked` 或 `failed`，不能标记
`passed/completed`。
