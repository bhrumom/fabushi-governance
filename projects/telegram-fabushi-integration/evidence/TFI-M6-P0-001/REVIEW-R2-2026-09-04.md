# TFI-M6-P0-001 independent code review — R2

- Project: `FAB-P0001 / TFI`
- Task: `TFI-M6-P0-001`
- Review date: `2026-09-04`
- Verdict: **`REVIEW-REJECTED`**
- Target repository: `bhrumom/fabushi`
- Target PR: `#2323`
- Target base: `codex/tfi-m6-repair@9e88a2e9c030fe05147460dfa580366cf9aa433d`
- Exact reviewed head: `fix/tfi-m6-p0-001-community-create-boundary@1dc165489498889504a61b7e07d5164f25188cef`
- PR state at review: `OPEN / UNMERGED`, 8 commits, 19 changed files
- Reviewer branch: `review/pr-2323-r2-record-20260904`, created directly from canonical `main@688465e94647d4c866f6b1d7b4884145b2f4a9da`
- Local build/test/E2E: **not run**. Verification used repository source, exact GitHub PR state, official upstream source, and GitHub Actions only.

## R1 history preserved

Independent R1 remains historical evidence and is not overwritten:

- reviewer PR `#2325`, head `7f594f10570822dcf23a4c3c02ddb0583ea94f14`;
- PR #2323 review id `5114738170` = **REVIEW-REJECTED** on execution head `73a46d3089c4f12dfb2f5659b232d51c674ed5a6`;
- R1 blockers were B1 semantic ownership regression coverage, B2 exact upstream provenance, and B3 unsupported inherited-only formatter attribution.

This R2 rereads the repaired exact head and independently re-evaluates each blocker.

## Exact-scope review

The live PR metadata confirms base `9e88a2e9c030fe05147460dfa580366cf9aa433d`, head `1dc165489498889504a61b7e07d5164f25188cef`, open/unmerged, 8 commits, 19 changed files. The full changed-file set remains limited to the additive P0-001 workflow, `native/mahayana-messaging/src/{engine,service}.rs`, `native/mahayana-messaging/tests/m6_channels_topics_contract.rs`, and TFI project records.

Comparison from the R1-reviewed head `73a46d3089c4f12dfb2f5659b232d51c674ed5a6` to current head `1dc165489498889504a61b7e07d5164f25188cef` is exactly two commits. It changes only the existing P0-001 Rust regression, the atomic workflow's project-record path triggers, and TFI project records; `engine.rs` and `service.rs` are unchanged after R1.

## R1-B1 — CLOSED

`native/mahayana-messaging/tests/m6_channels_topics_contract.rs`, test `respond_community_join_emits_participant_projection_only_when_approved`, now directly destructures the real `Event::CommunityChanged { community }` payload instead of checking only event count/type.

The approved path directly asserts the existing production structures and semantics:

- `CommunityMember.status == MemberStatus::Member`;
- `CommunityMember.invited_by == human:owner`;
- approved requester removed from `CommunityState.pending_join_requests`;
- `CommunityAuditAction::JoinApproved` entry has `actor_id == human:owner` and `target_actor_id == human:approved`;
- the second event is `ConversationParticipantUpserted` for the approved requester with role `Member`.

The rejected path directly asserts:

- requester is absent from `CommunityState.members`;
- requester is removed from `pending_join_requests`;
- `CommunityAuditAction::JoinRejected` entry has `actor_id == human:owner` and `target_actor_id == human:rejected`;
- only the `CommunityChanged` event is emitted.

These assertions match the actual existing `CommunityState`, `CommunityMember`, `CommunityAuditEntry`, `CommunityState::approve_join`, and `RespondCommunityJoin` implementation. They are semantic structure assertions, not string/length-only or surface-shape checks.

Exact-head task gate evidence: workflow run `33890057159`, job `101079256166` = **SUCCESS** on `1dc165489498889504a61b7e07d5164f25188cef`; the full `m6_channels_topics_contract` binary compiled and the three named P0-001 regressions passed.

## R1-B2 — CLOSED

The repaired provenance record is independently reproducible and accurate:

- official repository/revision: `tdlib/td@d1085f9cebc5a62379991ae1652673954f229c1f`;
- exact upstream file: `td/telegram/Requests.h`;
- exact symbols present there: handler declarations for `td_api::createNewSupergroupChat` and `td_api::processChatJoinRequest`;
- exact license file at the same revision: `LICENSE_1_0.txt`, **Boost Software License 1.0**.

The record correctly limits adoption to the create-vs-join-request boundary principle and explicitly states that no TDLib, reconstructed Grok, Grok Bot, Codex, or other upstream implementation code was copied, translated, ported, adapted, or transplanted. No unnecessary source copying is required.

## R1-B3 — NOT CLOSED — REVIEW BLOCKER

The new task-specific records correctly withdraw the stronger statement that the required formatter failures were entirely/wholly inherited. They also correctly state that this evidence set has **no independent exact-base formatter run** for `9e88a2e9...`, so current evidence proves only that required PR-head Rust jobs fail at formatter steps.

However, the same PR still contains multiple durable TFI management records that continue making the unsupported attribution without a local correction in those records:

1. `projects/telegram-fabushi-integration/management/02-里程碑.md:47` — says required Actions fail at **"inherited rustfmt"**.
2. `projects/telegram-fabushi-integration/management/03-验收追踪矩阵.md:168` and `:170` — say failures are **"inherited rustfmt"** / **"due inherited rustfmt"**.
3. `projects/telegram-fabushi-integration/management/04-风险登记.md:43` — attributes the blocker to branch-wide audited-M6 rustfmt drift and says to **"hand inherited formatting debt"** to the M6 owner.
4. `projects/telegram-fabushi-integration/management/05-状态报告.md:255` and `:258` — continue describing both required failures as **"inherited rustfmt"**.
5. `projects/telegram-fabushi-integration/management/06-依赖与阻塞.md:69` — describes the required failure as **"inherited audited M6 drift"**.
6. `projects/telegram-fabushi-integration/management/07-变更日志.md:231` — records the initial required failure as **"inherited rustfmt"** without a same-record correction/supersession note.

The PR description is also stale: it still labels the required failures as inherited and still names `73a46d...` as the "Current PR head" even though live PR metadata is `1dc165...`.

This is not a runtime semantic defect and does not justify changing application code. It is a **blocking evidence/governance inconsistency** because B3's requested repair was specifically to stop asserting inherited-only formatter attribution without exact base/head evidence. A later specialized record saying the old wording is superseded does not make these still-current project records internally accurate or synchronized for a future reader.

### Smallest required repair

Execution group should update only the affected execution/project records (and PR description) to neutral, evidence-bounded wording, for example: required PR-head Rust workflows **fail at rustfmt before later Rust checks; exact base-vs-head formatter attribution has not been established**. Historical event chronology may be preserved, but the unsupported cause attribution must be explicitly withdrawn/superseded in each durable record that still states it. Do **not** create a fake base check, weaken/disable a required workflow, or change application semantics merely to satisfy this finding.

## Implementation/security findings accepted in R2

No new application blocker was found in the exact repaired head:

- `RespondCommunityJoin` uses explicit `Option<Event>` construction; approved Group/Channel joins emit `CommunityChanged` plus participant projection, while rejection emits no member projection.
- Approval calls `approve_join(&requester_id, &actor_id, ...)`; audit actor/target remain acting owner/admin -> requester.
- Existing Community-backed generic `CreateConversation` returns `Vec::new()` before generic upsert. `MessagingService::handle` returns immediately when no events exist, so this is a true service-level no-op with no cursor/journal mutation.
- Community-backed `UpdateConversation` remains metadata-only (`title` / `description`), and authorization requires owner or an administrator with `change_info` permission.
- Missing Community on `RequestCommunityJoin` returns `CommunityNotFound`; no `CommunityState::new()` synthesis is introduced.
- Ordinary non-Community create still projects to generic `UpsertConversation` under the existing creator authorization.
- The R1->R2 delta adds no M6-P0-002/003/004/005, M7, M8, MSR, or GBF product semantics.

## Exact-head CI facts

At exact head `1dc165489498889504a61b7e07d5164f25188cef`:

- `TFI M6 P0-001 atomic gate` run `33890057159`, job `101079256166`: **SUCCESS**; compile + three named P0-001 regressions passed.
- `Mahayana fast checks` run `33890057133`, job `101079256711`: **FAIL** at `Verify formatting before native package setup`; later native/Rust checks skipped.
- `Messaging Product Gate` run `33890057218`: Electron Messenger job `101079257046` **SUCCESS**; Rust self-hosted job `101079257348` **FAIL** at `Rustfmt self-hosted messaging`; later Rust tests/clippy skipped.
- Developer Fiat Commerce and Explicit automerge runs are successful, but neither substitutes for the failed required Rust gates.

Required Rust formatter failures therefore remain real closure blockers. The task-specific atomic success and Electron success must not be represented as overall required-CI success.

## Project-record synchronization review

The task-specific P0 WBS, milestone overlay, acceptance overlay, risk/dependency overlay, changelog overlay, task record, evidence index, M6 WBS, and TEST-RELEASE-HANDOFF correctly preserve `CI-BLOCKED / CLOSURE-BLOCKED`, exact-main package/E2E/Release pending, and R1 history. They also correctly keep M6-P0-002 blocked on full closure.

The synchronization gate still fails because the older generic management records listed in R1-B3 above were not brought into the same conservative formatter-attribution state. R1 history itself is preserved and must remain preserved.

## R2 verdict

**`REVIEW-REJECTED`**.

- B1: **CLOSED**.
- B2: **CLOSED**.
- B3: **OPEN / BLOCKING** due inconsistent unsupported inherited-rustfmt attribution in durable project records and stale PR description.
- Runtime P0-001 implementation: no new application-code blocker found in R2.
- CI/delivery: **BLOCKED** independently of this review rejection because required Rust formatter gates fail; PR is open/unmerged; protected canonical-main merge, exact-main packaged E2E/video evidence, and Release are absent.

Do not merge or release. Do not begin M6-P0-002. After the narrow record/PR-description correction, rerun/verify the exact new PR head, then request a fresh independent rereview.