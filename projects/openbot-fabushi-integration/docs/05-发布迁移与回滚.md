# 发布迁移与回滚

Normal protected PR -> canonical main -> exact-main packaged E2E -> immutable Release. No direct main push and no changed binary under an unchanged version. Rollback is the normal prior verified Release; this slice must not migrate user data or fork persistence.
