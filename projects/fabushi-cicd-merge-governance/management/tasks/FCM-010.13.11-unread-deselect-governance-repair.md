# FCM-010.13.11 — Unread deselect + governance repair

- **Portfolio Project ID / Project Key:** `FAB-P0003` / `FCM`
- **Task ID:** `FCM-010.13.11-unread-deselect-governance-repair`
- **Parent:** `FCM-010.13` / `FCM-010.13.11`
- **Objective:** Close the fresh production failure on canonical `a504f50ef6c8520517e91157bcc5769527d6e30a` by establishing a real non-assistant/list projection before the exact `unread-none` baseline probe, while keeping project governance synchronized to current production truth.
- **Source requirements / references:** `[Fabushi:720b2161-131e-42c6-b8d2-2c033cd35df1]`; original rapid GitHub-Actions automation objective `https://mp.weixin.qq.com/s/DEg20nFKfrd2urMc3BjntA`; parent FCM-010.13 acceptance chain; PR #2557 review findings `discussion_r3996054622` / `discussion_r3996054623`; PR #2558 first-head finding `discussion_r3996163409`.
- **Status:** `IN_PROGRESS / FAIL_CLOSED — two PR #2558 heads rejected; next exact head must re-pass packaged CI and independent review`
- **Started:** `2026-09-12T11:54:00Z`
- **Updated:** `2026-09-12T12:25:00Z`
- **Completed:** `N/A — blocked until exact-head review/CI, protected merge queue, exact-source Release, and one same fresh production run satisfy the full parent gate.`

## Scope

### In scope

1. Before querying exact assistant `unread-none`, explicitly select a known non-assistant peer and return to Chats so the assistant list semantic row remounts.
2. Preserve exact semantic identity `peer-unread:legacy:conversation:mahayana-ai:agent:assistant` plus exact names `unread-none` / `unread-positive`; no screenshot/OCR/private-message fallback.
3. Deterministic controller regression proves `assistant-active -> known non-assistant peer -> Chats -> exact unread-none -> reopen assistant -> send`.
4. Packaged Electron/App Surface E2E reproduces the actual assistant-active precondition, proves Chats-only navigation keeps that durable active peer, proves the non-assistant transition and eventual exact unread semantic remount, then re-resolves generation immediately before the generation-sensitive assistant invoke.
5. Keep WBS, dependency/blocker register, risk register, status report and changelog synchronized to the latest production/CI truth.
6. Preserve every existing gate: `find limit<=100`, generation retry, exact source SHA, immutable Release, exact App-owned device, 27 categories, real nonzero remote actions, `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller/target success, truthful evidence, playable whole-session video, trace/screenshots/logs/artifacts/digest/source lineage.
7. Required CI and a genuinely independent no-finding review must pass on one exact PR head before protected merge-queue entry.

### Out of scope

- Reusing targets `34680986090`, `34681709910`, `34688641913`, `34689484659`, `34691502192`, or any old artifact as final acceptance evidence.
- Weakening semantic identity/name matching, stale-generation handling, evidence truthfulness, source/device binding, final logout ordering, protected merge, or Release immutability.
- Reworking already-verified generation retry, `limit<=100`, assistant semantic identity/name, recorder implementation, OAuth/login, or package provenance unless fresh evidence shows a new defect.
- Running the final CI latency observer before FCM-010.13.11 and FCM-010.13 are accepted.

## Fresh production evidence opening this task

Canonical `a504f50ef6c8520517e91157bcc5769527d6e30a` produced immutable Release `desktop-1.2.56-a504f50ef6c8`, production controller `34691460208`, and automatically dispatched target `34691502192/1`.

- Exact App-owned device: `gha-34691502192-1-macos-app`.
- Evidence artifact: `fabushi-macos-interactive-evidence-34691502192-1`, id `10296494233`.
- Digest: `sha256:4d06e44f654f4b571e50c349d991a97f980db1125f81289ffef7dfa377747cf6`.
- `remoteActionCount=26`, packaged Playwright success, `recorderPlayable=true`.
- Failure: exact assistant `unread-none` baseline timed out through generation 464 because assistant remained active.
- Failure path truthfully emitted FAIL -> `ci_session_finish`; there was no `READY_FOR_LOGOUT PASS` and no accepted fresh-snapshot/exact-logout success sequence.

This closes the old protected-account connectivity diagnosis: the controller reached the exact run-owned device and executed real remote actions. It also confirms PR #2557's runtime P1.

## Rejected historical repair heads

### PR #2557 final head `77af66957a06c26dd17dd8971eacb94bd35564d3`

Independent review produced two P1 findings: assistant was never actually deselected before the list-only unread probe, and governance records were stale. It is not REVIEW-PASS and its merge cannot be retroactively accepted as satisfying the user's review-before-queue gate.

### PR #2558 head `c9ee0c88ced2e51f774c89ffc37063967bd498c3`

- Base `CI result` and controller contract were green, but Electron desktop quality run `34692841431` failed the new packaged App Surface regression twice because it queried exact `unread-none` immediately after row remount.
- Independent Codex exact-head review returned P1 `discussion_r3996163409`: `management/04-风险登记.md` still described historical protected-account HTTP-400 as current/open.
- Head rejected; never queued; its review cannot be reused.

### PR #2558 head `f47b95a1f85c03f5b23db348b8a5245608b1125e`

- Required `CI result` `34693225962`, controller contract `34693226018`, delivery governance and project governance passed.
- Electron desktop quality run `34693225972` failed. The repaired E2E successfully reached the non-assistant/Chats surface and polled exact `unread-none`, but then invoked assistant with an earlier `afterDeselect.generation`. The unread polling advanced App Surface generation, so both attempts failed closed with `stale_app_surface_generation` (`expected 119, received 113`; retry `expected 122, received 119`).
- Failure artifact `electron-prepackage-e2e-34693225972-1`, id `10298231944`, digest `sha256:dfcd34e6c1c8aef1c6c383ef6f4bdaf85e8e3294517df967b34ec4bd6c1ccf2e`, retains screenshots/traces/report.
- The branch fix now re-finds the stable assistant immediately before invoke, matching production `openAssistantConversation()` and preserving stale-generation failure semantics rather than retrying a known-stale ref.
- Requested Codex re-review for exact `f47...` did not run because the repository returned `You have reached your Codex usage limits for code reviews`; therefore no independent REVIEW-PASS exists for this head.
- GitHub Copilot code review was requested as a genuinely independent alternate reviewer. Its outcome may count only if it binds to the eventual unchanged final exact head and contains no blocking finding.
- Head rejected; never queue it.

## Current repair implementation

- Production `send` performs `navigateSection("频道") -> openPeer(channelAId) -> navigateSection("聊天")`, records list-state establishment, polls exact bounded `unread-none`, then reopens assistant/sends.
- Controller contract locks assistant-active -> channel A -> Chats -> baseline -> reopen -> send ordering.
- Packaged E2E reads durable `fabushi.desktop.messenger-projection.v1.activePeerKey`: Chats-only navigation leaves the assistant active; selecting a `selfhosted:` peer changes durable state; returning to Chats keeps that non-assistant peer; exact `unread-none` is polled until mounted; then the stable assistant is freshly resolved again before invoke so the action uses current generation.
- WBS, blocker register, status report, changelog and risk register distinguish historical connectivity/assistant/MOV/find-limit failures from current list-state/stale-validation/review governance risks.

## Open-source-first / reuse decision

Reuse the immediately preceding recorded survey: Microsoft Playwright (Apache-2.0) for semantic/App Surface E2E, Radix Primitives (MIT) as finite-state UI-state design reference, and DOM Testing Library (MIT) as semantic-query guidance. No new runtime dependency or copied third-party code is introduced.

## Branch / PR

- **Branch:** `fix/fcm-010-13-11-unread-deselect-governance`
- **PR:** `#2558` — intentionally draft until a later exact head has required CI/package regression green and an independent no-finding review.
- **Base at branch creation:** `a504f50ef6c8520517e91157bcc5769527d6e30a`.
- **Exact review head:** read only after this current-round record commit; any later commit invalidates review and CI acceptance for queue purposes.

## Acceptance criteria

1. Production establishes non-assistant/list projection before exact `unread-none`.
2. Deterministic controller and packaged App Surface regressions cover actual assistant-active precondition, remount timing and fresh generation before invoke.
3. WBS, blockers, risks, status and changelog reconstruct current truth without obsolete blockers presented as current.
4. Required CI passes on one exact PR head, including Electron desktop quality / packaged App Surface regression.
5. A genuinely independent reviewer explicitly covers that same exact head and returns no P0/P1/P2/blocking findings; any head change invalidates it. Codex, Copilot code review or a genuine human reviewer can satisfy independence; self-review cannot.
6. Only after 4-5 pass may PR #2558 become ready/authorized and enter protected `main-merge-queue`; direct merge is unacceptable.
7. After merge, re-read exact canonical main SHA and require immutable desktop Release whose target/tag/macOS ZIP provenance binds to that SHA.
8. Production FCM-010.13.11 controller must automatically dispatch a brand-new frozen-source target; prior targets/artifacts remain diagnostic only.
9. One same fresh run must prove exact App-owned macOS device, all 27 categories PASS, real nonzero remote actions, exact `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller/target success and truthful evidence success.
10. Retain playable whole-session video, Playwright trace, screenshots, App/system/device-call logs, reports, artifact id/digest, Release/tag/source SHA, target run/attempt and exact device lineage.
11. Only then may FCM-010.13.11 and FCM-010.13 be marked passed; only after that closure may the CI latency observer be run for the next optimization round.

## Current blockers / risks

1. The post-`f47...` branch has not yet passed required Electron packaged validation.
2. No independent REVIEW-PASS exists on the eventual exact final head; Codex code-review quota is currently exhausted, and alternate GitHub reviewer evidence must be exact-head and no-finding.
3. No compliant protected-queue merge, new canonical SHA, new immutable Release or accepted fresh production target exists yet.

## Next action

Read the new PR #2558 exact head after this record commit, keep it draft, run required CI including Electron packaged App Surface regression, and obtain a genuinely independent no-finding review on that same SHA. Queue entry remains forbidden until both gates pass.