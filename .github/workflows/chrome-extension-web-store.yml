name: Chrome Extension Web Store (disabled by FCM-024)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Record disabled automatic-test policy
        run: |
          echo 'Disabled by FCM-024.'
          echo 'Automatic Chrome validation, Node test suites, Playwright journeys, and packaging are not permitted in the default CI path.'
          echo 'Test/beta releases must not run behavioral tests. Formal behavioral validation is performed only through a manually started Action runner controlled by the Fabushi official MCP.'
