# GBF-102 evidence — recursive source manifests

Both pinned historical source heads are recursively enumerated with Git mode/type/object/size/path. `summary.json` records the exact tree SHA, entry count, and SHA-256 for each TSV.
