# TFI-MACOS-INTERACTIVE-001 — macOS 1.2.35 release candidate

- Protected release-base main: `fedb2cc75e70c57de3248ce34188e8c3dc4f367e`; exact product/test baseline immediately before the workflow-only trigger repair: `d3fe6a9b7d9eff6ccdd307ef23123ab5eda67639`.
- Product repair: PR #2390 made profile/settings navigation stable; PR #2392 added production `data-agent-id="settings-logout"`; PR #2393 relocated the logout-id regression assertion into the already-existing account logout E2E after the low-level bridge test timed out.
- Excluded candidate: version 1.2.34 never published; release run `34012857131` was cancelled after the semantic-ID contract mismatch was proven, and no-release interactive runs were cancelled before device selection.
- Candidate governed macOS test version: `1.2.35`.
- Mobile release counters remain unchanged: Android `androidVersionCode=29`, iOS `iosBuildNumber=29`.

## Release and acceptance contract

Only a protected-main merge may publish `v1.2.35`, and the release target must equal the exact resulting canonical main SHA. Native recording must begin before installation. The installed 1.2.35 App must log the protected CI account and self-register a brand-new account-scoped macOS device. KRIS, old devices, pre-existing devices and runner-owned gateways remain forbidden.

The external acceptance must call all six `fabushi.app.status/snapshot/find/action/wait/assert` tools and `ci_session_status/note/finish`, execute the declared macOS journey, invoke the final logout through exact production `agentId="settings-logout"`, and always retain whole-session video, step screenshots, redacted device-call trace, Playwright report/trace/video/results, app/system logs, release metadata/digests, README/report and run/job/SHA/device identity on pass or fail.
