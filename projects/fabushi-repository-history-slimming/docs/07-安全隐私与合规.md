# 安全、隐私与合规

历史重写具有破坏性，采用最小权限、隔离镜像、显式 ref leases 和动作前确认。不得把 token、cookie、密码、私钥或个人数据写入项目记录、命令输出或 Git 历史。只对用户指定的 GitHub 仓库操作；不改变账号权限或协作者。发现疑似凭据时先停止并升级，不把它当普通大文件删除。

使用第三方工具时记录仓库、版本、许可证和适配理由；不复制其实现代码进入 Fabushi。
