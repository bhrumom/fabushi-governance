# GBF-502 Evidence

`BotMarkState` contains explicit Agent lifecycle semantics including idle, listening, thinking, tool-running, speaking, result and error. `profileForState` maps each to deterministic Fabushi motion behavior.
