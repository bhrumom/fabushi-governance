# 07 变更日志

## 2026-08-22
- Created canonical `mahayana-sovereign-runtime` project for the Codex + Grok Build convergence objective.
- Recorded PR #1971 as the merged implementation baseline rather than opening a duplicate implementation mainline.
- Defined Mahayana as product owner; Codex/Grok Build are upstream capability/provenance sources and optional compatibility references.
- Closed MSR-001 after PR #1989 / CI / protected merge / canonical-main verification.
- Advanced reviewed Codex baseline from `970b7f2d...` to `343074d4207d572809bd8cea15f4be1d09d98e0b`; retained Grok Build `19d42e35...` as the current reviewed baseline.
- Added `docs/08-upstream-capability-matrix.md` and routed explicit remaining gaps to MSR-103/201/202/301/302/401/501/601.
- Began MSR-103: introduced Mahayana-owned auth and encrypted-secrets crates, product dependency cutover, migration compatibility, source-boundary enforcement, and Actions coverage.

## 2026-08-24 — MSR-106 in progress
- Diagnosed background CPU drain to 1 ms Feature Host event polling.
- Added explicit bounded event long-poll while preserving non-blocking direct/test receive semantics.

## 2026-08-24 — MSR-106 renderer/GPU continuation

- Paused BotMark requestAnimationFrame and compositor CSS motion when Fabushi is hidden or unfocused.
- Capped focused shared BotMark motion dispatch to 30 FPS.
- Preserved reduced-motion and explicit per-mark pause behavior.
- 2026-08-28 — MSR-203: added native Mahayana live-web research (`web_search`, `web_fetch`) using TinyFish Search/Fetch as the first provider; preserved provider-neutral `WebSearch` capability and existing tool/policy event flow; added SSRF-oriented URL validation, secret redaction/bounds, mock provider tests and network-denial coverage.

## 2026-09-17 — MSR-403 opened
- Added source intake for standalone Mahayana CLI installation, automatic same-account device registration, and ephemeral GitHub Actions runner presence.
- Accepted ADR-0003: live same-account presence registry, stable local installation identity for normal machines, run-scoped/TTL identity for ephemeral runners, and no durable offline device inventory.
- Opened MSR-403; implementation/evidence remain pending.

## 2026-09-17 — MSR-403 implementation staged
- Implemented native Rust CLI device-agent lifecycle and same-account automatic registration.
- Changed gateway discovery to live-only semantics; GitHub Actions device registrations are deleted on disconnect.
- Added checksum-verifying macOS/Linux and Windows one-line installers plus no-test package and manual official-MCP live-device workflows.
- No local product build/test was performed; exact compile/package/runtime evidence remains pending in Actions and the official MCP.

## 2026-09-17 — MSR-403 branch device control proof
- GitHub Actions run `35172684311` on `e9a696941a05ca1b66a8a56b8a7557c6aec0f7f3` produced the first successful CLI-owned same-account device connection to the official Fabushi MCP.
- Official MCP verified status, remote shell execution, and text-file write against the live ephemeral runner.
- Production still returned the disconnected runner as offline; canonical gateway deployment remains required to activate the branch's live-only registry semantics.

## 2026-09-17 — MSR-403 canonical-main activation
- #2683 merged through protected merge queue as `c70eba65f38cd61c3cf6c5412868f7a1b92df4dd`.
- Activated canonical live-only device discovery on production official MCP with atomic rollback-safe deployment and health verification.
- Exact-main GitHub-hosted CLI device passed official MCP status/shell/write control and disappeared from `list_devices` after remote finish.
- Published exact canonical installer scripts; corrected the official-site legacy-host verifier to match the actual in-place custom-domain contract.
