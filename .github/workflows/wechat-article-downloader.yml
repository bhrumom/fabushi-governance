name: Retired automatic test - WeChat article downloader

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024. No automatic product tests are permitted.'
