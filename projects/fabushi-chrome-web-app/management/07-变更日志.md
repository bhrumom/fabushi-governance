# 变更日志（append-only）

## 2026-09-12
- Added independent FAB-P0011/CWA after canonical registry re-read.
- Selected compatible modules from codex/fabushi-chrome-userscripts; rejected wholesale merge.
- Preserved the 0.4.1 userscript runner, page bridge and bundled ChatGPT auto-confirm
  workbench; also package the smaller Task Queue companion through the same runner.
- Renamed active browser host to com.fabushi.browser_control; old name remains only for
  migration metadata/cleanup.
- Added package whitelist, 0.5.0 version gate, SHA/content manifest and 90-day target CI.
- Kept the old unpacked extension directory beside the new fabushi-extension staging path,
  rotated legacy metadata keys, and added an explicit published-ID gate plus a post-UI
  cleanup command so migration cannot overwrite or silently trust the old Bridge.
- Added a packaged Chrome journey that records labelled screenshots, video, trace, native
  events and an HTML/report manifest on an always-uploaded CI path.

## 2026-09-12 / CI corrections and evidence
- Kept MV3 worker startup synchronous; serialized first generation seeding so simultaneous tab
  events cannot mint conflicting claim generations.
- Registered the CI test host under Chrome for Testing's custom profile `NativeMessagingHosts`
  directory and used a CommonJS entrypoint; the packaged journey then connected successfully.
- Chrome workflow `34667146375` passed the complete PR packaged journey and uploaded the
  source-bound evidence bundle with 90-day retention. Canonical-main delivery and external
  Web Store/profile migration gates remain open.

## 2026-09-12 / CWA-007 scope
Persisted user request for independent Chrome account login and official same-account MCP direct control. Preserve all Bridge capabilities; no unrelated extension removal inferred from screenshot.

## 2026-09-14 / CWA-008 userscript recovery capability
- Added explicit userscript-to-host `tab-recovery` request/grant/release contract and a service-worker watchdog based on Chrome `tabs`/`alarms` lifecycle signals and content-free heartbeat metadata.
- Added safe recovery URL allow-list, bounded lease/cooldown, original-tab-first reload, one-time takeover fallback, and deliberate-close cleanup. Host storage never receives task goal, prompt, credentials or attachment bytes.
- Bundled source userscript v2.9.20 with attachment-preserving recovery, blocked-send recovery, unique current-route adoption and independent post-recovery confirmation window; final Work replies continue to the planner/acceptance prompt.
- This is an application-affecting Chrome extension change: required CI, protected main, canonical packaged E2E evidence and Release/Web Store delivery remain open.
