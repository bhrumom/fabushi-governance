# 架构与实现策略

迁移链路：远端 refs 快照 → 隔离 bare 镜像 → 只按审核路径过滤 → refs/path/对象审计 → 规则迁移窗口 → 原子 force-with-lease 推送 → GitHub 读回与完整性验证 → 立即恢复规则。

重写工具只运行在临时隔离目录，不改当前开发工作树。产品资产通过显式保留清单与 old/new tree 对照保护；生成规则通过 `.gitignore` 防回流。应用运行时架构不变，故不引入运行时依赖。
