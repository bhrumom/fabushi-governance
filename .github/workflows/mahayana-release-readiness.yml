name: Retired automatic test - Mahayana release readiness

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024. Formal validation is device-level through the Fabushi official MCP only.'
