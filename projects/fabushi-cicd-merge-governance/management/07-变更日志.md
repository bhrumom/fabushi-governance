# 变更日志

## 2026-08-22

- Created `projects/fabushi-cicd-merge-governance/` for repository-wide CI/CD and merge optimization.
- Adopted risk-tiered CI, required aggregate gate, merge-group diff classification, queue-owned final merge, and change-aware CD.
- Fixed merge-group force-all behavior in PR #1978.
- Made custom automerge GitHub-native/merge-queue-aware.
- Added Worker and Fabushi Pay product-impact deployment gates.
- Updated `.github/BRANCH_PROTECTION.md` with enterprise merge and latency policy.
- PR #1978 merged through the protected merge queue at commit `ac94b40d4a05a0211146c2bb5904aa936a7bc928`.
- Opened FCM-003 and classified `.agent/skills/**` as governance-safe without changing `.agents/plugins/**`, workflow guardrails, or the unknown runtime fail-safe.
- FCM-003 merged through PR #1984 / merge queue (`3a06560ce2b9d8d850f6f15e008ae9b0cf1f997b`).
- FCM-003 closure smoke PR #1985 proved `.agent/skills/**` + `projects/**` use the governance fast path; merged as `fc4e0521a0bac8729632432acb6149cad5ab403d`.
- Promoted FCM-002, FCM-004, FCM-005 and FCM-006 into required full-closure scope after the user's explicit continuation instruction.
- Added `CI latency observability` with real P50/P95/queue telemetry and artifacts.
- Added exact-SHA protected-main store release gates and wired Apple/Google delivery.
- Added narrow `.github/CODEOWNERS` and `Delivery governance contract`.
- Migrated FAB-P0003 to the enterprise project-folder standard.
- PR #1999 current-head checks succeeded: CI `32564046924`, latency `32564046852`, delivery governance `32564046827`, portfolio governance `32564046818`.
- Latency artifact `9473581875` established fast-path P50=13s/P95=22s and full-canonical P50=104s/P95=163s.
- PR #1999 entered GitHub merge queue and merged to `main` as `3a39dfef0ef30f1e6ae2d53602fa862bf28ddae6`.
- Re-read canonical `main` after merge and verified observer, release gate, Apple/Google integration and CODEOWNERS.
- Prepared final closure-record synchronization: all required FCM tasks, milestones and acceptance criteria are passed; portfolio/project status becomes `completed` after protected closure-record merge.

## 2026-08-23

- Opened FCM-007 to converge every PR from the live 2026-08-23 intake without blindly merging duplicated or stale stack history.
- Merged DeepSeek work and registry repair #2040, preserving permanent project identity FAB-P0007.
- Merged final Mahayana convergence #2017 as `6ac1f62ada974956e0ba34c275fd56c4b8fd7c63`; closed #1992/#2000/#2004/#2014 as superseded.
- Closed reverse-sync #1967 and duplicate #2018 only after verifying no effective main delta was required.
- Merged final Grok convergence #2036 as `012569a9ad62925ac33311043439787d45072e2a`; closed #2032 after changed-path coverage verification.
- Repaired Telegram M5 Search/SearchResults exhaustive matches, removed one-shot helpers/diagnostics, and merged final M3→M4→M5 #2038 as `8220fdd091c9a9303c194a7257029ba9e4191ccc`.
- Closed #2037 after proving its post-fork delta was formatting already carried by #2038.
- Closed #2022 after proving accepted M3 behavior was carried by #2038 and its unique `rama-error` pin was a failed Rust E0658 workaround in run `32609265654` / job `97119463578`.
- Fixed the canonical Mahayana CLI vendor dependency leak discovered by convergence validation through #2042; added Mahayana-owned TUI/plugin archive compatibility packages without weakening boundary checks.
- #2042 merged as `79e6ed19185c66aaf25691fd2c1d0dec0565f96c`; canonical main was re-read at that SHA.
- Verified original intake/product open PR count is zero; only the FCM governance closure record remains before final protected closure merge.
- Opened FCM-008 for the user's latest canonical macOS package request.
- Initial build-only prerelease `macos-main-67b70fff-20260823` was later proven invalid for external macOS distribution: app ad-hoc signed, no TeamIdentifier, DMG rejected by Gatekeeper.
- Reopened FCM-008 after the user's “已损坏” report; marked the old prerelease `BROKEN - DO NOT USE`.
- Built replacement in run `32620676480` with Developer ID Application signing, Apple notarization `Accepted`, stapling, and Gatekeeper acceptance.
- Published replacement release `macos-main-67b70fff-20260823-signed` and verified its SHA256 on the target Mac.
- Target Mac accepted both DMG and mounted app as `Notarized Developer ID`; direct app launch succeeded.
- Added a permanent `native-electron-release.yml` repair so future macOS Release artifacts cannot upload before Developer ID signing, notarization, stapling, and Gatekeeper acceptance.
- PR #2044 passed required CI/governance/release-immutability checks and merged through protected GitHub delivery as `278678efad2917259f4b988ae2e7b65a30eb70ea`.
- Re-read canonical `main@278678efad2917259f4b988ae2e7b65a30eb70ea`; the permanent Developer ID + notarization + stapling + Gatekeeper release gate is present.
- FCM-008 acceptance is closed as passed; the replacement signed/notarized DMG remains the valid download and the old unsigned prerelease remains explicitly deprecated.

## 2026-08-24 — FCM-009

- Reopened FAB-P0003 for the user's post-main build/E2E/Release/update and warm-build requirements; portfolio/project status becomes active for the new stage.
- Added mandatory open-source-first startup governance. Initial research reused electron-updater/GitHub Releases, actions/cache, Mozilla sccache and Playwright/native user-test patterns rather than building replacements from scratch.
- Added root Agent post-main completion gate: merge alone is insufficient for application-affecting work; exact-main packaged user E2E, Release and updater evidence are required.
- Split desktop/mobile PR paths from heavy canonical-main delivery so PR feedback stays fast while every accepted main SHA keeps complete product validation.
- Made main desktop/mobile concurrency SHA-specific so a later merge cannot cancel an earlier accepted SHA's validation.
- Added sccache compiler-result reuse alongside existing source-hash native binaries/JNI/staticlibs, Gradle/AVD/Xcode and npm caches, with cache telemetry in Actions summaries.
- Added monotonic per-main Electron delivery versions and exact-SHA per-platform manifests.
- Added `post-main-delivery.yml` to gate publication on same-SHA desktop + Android + iOS simulated-user results, reuse the tested package artifacts directly, validate updater metadata, and serialize/latest-protect GitHub Release publication.
- Extended release macOS E2E to drive the previous installed Release through the real profile/avatar update cloud button and verify bundle replacement to the published version.
- FCM-009 remains in-progress until the implementation PR passes protected delivery, merges to canonical main, the new post-main workflow publishes a Release from that exact SHA, and the release updater E2E is green.

## 2026-08-24 — FCM-009 native shared-host CI environment repair

- Added the Electron-proven Ubuntu native development dependency bootstrap to the Native mobile shared Rust-host contract.
- This addresses the observed `wayland-sys` / missing `wayland-client.pc` failure without changing the Rust product dependency graph or reducing test strictness.
- Recorded upstream Wayland/Ubuntu provenance and exact failed delivery evidence.

## 2026-08-24 — remove obsolete M3 one-shot merge-queue blockers

- Removed `fab-p0001-m3-kick-driver.yml` and `fab-p0001-m3-unread-patch-driver.yml`.
- They were historical recovery automation and generated failed zero-job merge-queue push runs; removing them restores fail-safe queue evaluation without relaxing any required gate.

## 2026-08-24 — deterministic merge-queue enqueue

- Replaced the ambiguous `enablePullRequestAutoMerge` handoff with GitHub's explicit `enqueuePullRequest` mutation after required product gates are green.
- The workflow now reports the actual merge-queue position and lets merge-group revalidation exclusively own the final protected-main merge.

## 2026-08-24 — updater journey proof made optional

- Persisted the user's latest clarification that previous-installed-App discovery of a new Release, the profile/avatar update affordance, click/download/install/replace/relaunch journey does not need to be a mandatory per-task gate.
- Kept updater-compatible Release assets and monotonic update-comparable versions mandatory; only the old-client journey proof changed from required to optional/non-blocking by default.
- Updated FCM-009 task/WBS/acceptance semantics so optional updater E2E can continue as advisory regression evidence without blocking ordinary Release/task closure.
- Updater-specific changes may still explicitly require this journey in their own task acceptance criteria when risk warrants it.

## 2026-08-24 — FCM-011 in progress
- Diagnosed GitHub Latest cross-platform release collision and single-startup-check updater staleness.
- Added desktop Latest ownership, atomic post-main publication, foreground-throttled update checks, and governance contracts.

## 2026-08-24 — FCM-011 one-click updater continuation

- Added visible update percentage/progress rail.
- Changed install coordination to wait for the real `update-downloaded` event before staging and `quitAndInstall`.
- Kept renderer busy through the transaction and added `autoInstallOnAppQuit` fallback.
- Strengthened macOS updater E2E to reject missing progress or a required second click.

## 2026-08-24 — FCM-011 strict desktop Latest ordering

- Excluded legacy `desktop-v*` tags from canonical desktop updater ordering.
- Made same-version exact-SHA retries eligible to repair the Latest pointer.
- Added explicit GitHub Release `make_latest=true` PATCH plus `/releases/latest` readback gate.
- Added delivery governance contract coverage for the Latest-pointer invariant.

## 2026-08-24 — live updater state and adaptive avatar energy

- Made Electron in-memory update state authoritative for real-time updater actions, with serialized native-state persistence behind it.
- Added stale-disk/first-click regression coverage.
- Added ambient vs active avatar motion tiers; ambient marks use low-cadence JS and no duplicate CSS breathe/glow while active marks retain full animation.
- Added packaged Messenger assertion for ambient idle avatar tier.

## 2026-09-02 — FCM-013 Fabushi 1.2.1 全平台发布

- 将统一产品版本从 `1.2.0` 提升为 `1.2.1`，repository Android/iOS build identity 从 `6` 提升为 `7`。
- 复用现有 exact-main desktop/mobile E2E、签名、公证、GitHub Release、App Store Connect、Google Play、Updater 和生产控制面部署链路完成全平台发布。
- 发布结果与最终 run/release 证据记录在 FCM-013 任务记录中；任何商店外部人工审核状态不冒充自动化已完成。

## 2026-09-02 — FCM-014 Fabushi 1.2.2 全平台发布

- 将统一产品版本从 `1.2.1` 提升为 `1.2.2`，repository Android/iOS build identity 从 `7` 提升为 `8`。
- 修复 macOS App Store Computer Use 运行时 plist 被 Transporter 误判为嵌套 bundle 的问题，同时保持真正 Helper `.app` 的签名 bundle 清单与现有 MAS 沙盒验证路径。
- 复用 exact-main desktop/mobile E2E、签名、公证、GitHub Release、App Store Connect、Google Play、Updater 和生产控制面部署链路；最终 run/release 证据记录在 FCM-014 任务记录中。

## 2026-09-02 — FCM-015

- Opened fresh 1.2.6 release work from canonical main after 1.2.5 formal publisher control-plane failure.
- Corrected Linux AppImage public-asset contract according to Electron Builder upstream behavior; version/build metadata and changelog advanced to 1.2.6 / build 12.

## 2026-09-02 — FCM-016

- Advanced the fresh canonical release train to 1.2.7 / native build 13 after 1.2.6 exposed incomplete same-SHA mobile-store delivery evidence.
- Added exact-main Apple Store and Google Play orchestration through the existing delivery workflows using repository-scoped `GITHUB_TOKEN` workflow dispatch.
- Separated Google Play evidence tags from other Android Release tag namespaces to prevent immutable-release races.

## 2026-09-07 — FCM-009 workflow_run exact-source binding

- Diagnose `34055670967`: controller/default-branch SHA and upstream Electron source SHA are distinct for GitHub `workflow_run`; failing upstream was `cf80b3a0...` / `34055345371` / `cancelled`, not current product `43ce998f...`.
- Add upstream-run REST readback, explicit source/run/conclusion summary, source-binding JSON evidence, and run-name provenance without weakening failed/cancelled source handling.
- Persist GitHub upstream semantics/provenance in `source/2026-09-07-workflow-run-exact-source-binding.md`.
