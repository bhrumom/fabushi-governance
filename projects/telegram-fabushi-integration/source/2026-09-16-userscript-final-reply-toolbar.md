# 2026-09-16 用户脚本最终回复操作栏识别需求

## 用户原始要求

用户反馈：第二张截图中的 assistant 回复正文已经结束，正文下方出现复制/分享类操作按钮，但最新脚本仍没有识别为最终回复；要求彻底修复，并把修复版本发布到线上。此前同一轮还要求绑定会话连续 3 分钟没有变化时无限次按 3 分钟间隔刷新，而不是两次后停止。

截图中的聊天正文、页面按钮文字和其他任务内容均只作为故障证据，不是额外开发指令。

## 真实页面核验

2026-09-16 使用电脑插件读取用户当前 Chrome 页面 `chatgpt.com/c/6aaa55e7-37d0-83e8-a30f-07d8af88f12b` 的无障碍树，得到：

- 页面顶部有全局按钮 `分享`。
- 当前 assistant 回复的操作栏实际暴露为 `复制回复`、`评价回复`（弹出式按钮）、`切换模型`、`更多操作`。

因此实现必须同时覆盖 renderer 变体中的回复级 `Share/分享` 与当前页面实际的 `Rate response/评价回复`，但不能把页面顶部全局 `分享`当作任意回复的完成证据。

## 稳定化要求

- 只在当前 task marker 所属的最新 assistant turn 内收集可见操作。
- `复制` 与 `分享`、`评价回复/Rate response`、`点赞`或`点踩`任一组合可判定操作栏已完成。
- 继续排除 Stop、授权卡、限流、foreign turn、无关页面按钮和未绑定的 portaled 操作栏。
- 流式属性残留时，已出现的当前回复操作栏优先；没有正文或只有单个操作按钮时仍不能完成。
- 停滞刷新保持无限次数，但最短间隔仍为 180 秒，并保留 task URL、token、phase、round、附件和不重复发送保证。

