# FCM-010.13.11 — Post-action App Surface semantic readiness

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010.13
- **Status:** in-progress
- **Intake:** `[Fabushi:acc40c75-64fe-440a-939c-bfcfa9c09aac]`; generation-race follow-up `[Fabushi:d3d20158-2ddd-46be-8233-a39cfe7ffada]`
- **Baseline:** `main@a0bdf707b24ac4b9e8b9d66046269b3d3503611c`
- **Branch:** `fix/fcm-010-13-11-generation-retry-budget`

## Proven before this task

- FCM-010.13.8-.10 remain passed and are out of scope.
- Frozen source stays `7ee12b790e18049d2b9509b0c29128dd2ace690b`; immutable Release tag stays `desktop-1.2.56-7ee12b790e18`.
- PR #2542 proved stable-agent stale-generation rebasing in production.
- PR #2543 proved query-only semantic re-resolution in production.
- PR #2544 added an explicit post-`新建频道` fail-closed wait rather than a blind sleep.
- PR #2545 corrected the channel form selectors to the App Surface accessible names `名称` and `描述`.
- PR #2546 added the created-channel projection readiness gate and merged to canonical `main@a0bdf707b24ac4b9e8b9d66046269b3d3503611c`.
- The production controller already contains the deterministic contract `find=95 -> action requires 96`, and retries stale App Surface actions by re-resolving their semantic target rather than reusing the stale generation/ref.

## 2026-09-12 generation-race round

Production controller run `34673090393` on `main@a0bdf707b24ac4b9e8b9d66046269b3d3503611c` passed its controller contract and protected-account production MCP preflight, then automatically dispatched exact frozen-source target run `34673135503` attempt `1`. The expected and actually discovered device was exactly `gha-34673135503-1-macos-app`; source/tag remained `7ee12b790e18049d2b9509b0c29128dd2ace690b` / `desktop-1.2.56-7ee12b790e18`.

The run proves MCP connectivity is restored and also isolates the current failure to sustained App Surface generation churn. The controller repeatedly re-resolved fresh semantic targets successfully: stable-agent `79 -> 84`, query `新建` `94 -> 100`, `名称` `109 -> 110 -> 111 -> 118`, `描述` `121 -> 123`, `创建频道` `126 -> 128 -> 134 -> 135 -> 136 -> 138`, then later query `新建频道` `173 -> 177 -> 178 -> 179 -> 180 -> 182 -> 186 -> 189`. The next action failed with `stale_app_surface_generation: expected 190, received 189` only because the helper's eighth attempt was its final bounded attempt. This is not stale-target reuse: every preceding stale attempt did re-find/rebase. It is a retry-budget exhaustion under real generation churn.

Failure controller evidence was retained as `fcm-010-13-11-external-controller-34673135503-1`, artifact ID `10291612205`, digest `sha256:3f3cd0d2c1e5c464adf5bcc0ee3008816af5107ddbb0b0510605ad68af325f63`.

## Minimal repair

1. Keep the existing fail-closed stale-generation algorithm and semantic re-resolution behavior unchanged.
2. Increase only the default bounded generation-sensitive action retry budget from `8` to `32`, so sustained churn can converge without introducing an unbounded loop.
3. Preserve the existing deterministic `95 -> 96` regression and add a second deterministic contract that produces eight consecutive stale generations, verifies a fresh `fabushi.app.find` on every retry, and proves the ninth action uses the newly resolved generation/ref rather than any previous lease.
4. Make the external-controller workflow trigger and syntax-check when `fcm-010-13-generation-sensitive-action.mjs` changes, preventing future helper-only edits from escaping controller contract CI.
5. Do not change device ID rules, frozen source SHA, immutable tag, 27 categories, production-account preflight, or `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout` ordering.

## Open-source-first startup record

Reviewed the established stale-target pattern in Playwright locators (actions re-resolve the current DOM target) and the W3C WebDriver stale-element model. The applicable design lesson is to reacquire a current target before retrying an action rather than hold a stale element/reference. No new dependency is warranted: Fabushi already has the required semantic `find`/snapshot/action abstraction, so this round adapts the proven re-resolution pattern inside the existing bounded fail-closed helper and does not copy third-party code.

## Atomic acceptance

1. Existing deterministic `find=95 -> action requires 96` contract remains green.
2. Sustained-churn regression proves more than eight consecutive stale generations each cause a new semantic resolution and eventually act on the latest generation/ref.
3. Required PR CI and protected merge queue merge the repair into canonical `main`.
4. Production FCM-010.13.11 automatically dispatches one new exact frozen-source target run after merge.
5. Acceptance requires exact `gha-<new-run>-1-macos-app`, all 27 categories PASS, real nonzero remote actions, `READY_FOR_LOGOUT PASS`, successful `ci_session_finish`, a fresh App snapshot, final exact `settings-logout`, target run `success`, truthful evidence gate `success`, and retained trace/log/screenshots/full-session video/artifact/digest.
6. Only after item 5 passes may FCM-010.13.11 and FCM-010.13 be marked passed; then update the obsolete HTTP-400/MCP blocker to connection-restored + generation-race outcome and rerun the CI latency observer.

## Current state

`in-progress / fail-closed`. Implementation branch exists, but no completion claim is valid until protected merge and fresh production evidence satisfy every acceptance item above.
