name: Retired automatic test - Telegram Rust

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024. Formal validation is performed through the Fabushi official MCP against an installed exact candidate.'
