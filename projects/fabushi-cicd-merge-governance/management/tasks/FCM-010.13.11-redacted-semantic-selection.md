# FCM-010.13.11 — Redacted semantic selection repair

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010.13 / FCM-010.13.11
- **Status:** IN_PROGRESS / FAIL_CLOSED
- **Source round:** `[Fabushi:6e069a44-1255-43d6-849f-289a7bdc7784]`
- **Branch:** `fix/fcm-010-13-11-redacted-find-selection`
- **Canonical source before repair:** `9e353c3973ea36b661ce31c9113ffc5184a1e758`
- **Fresh failed controller/target:** controller `34684421137`, target `34684462300`
- **Exact failed App-owned device:** `gha-34684462300-1-macos-app`
- **Immutable Release:** `desktop-1.2.56-9e353c3973ea`
- **Release source:** `9e353c3973ea36b661ce31c9113ffc5184a1e758`

## Production evidence and root cause

The post-merge production controller dispatched a genuinely fresh frozen-source target (`34684462300`), not either explicitly rejected prior run (`34680986090`, `34681709910`). The target installed the exact immutable Release, registered the exact App-owned device, passed packaged App Agent smoke and recorded real non-zero App actions. The truthful evidence gate correctly failed because the full journey exited before `READY_FOR_LOGOUT PASS`.

The exact failure was:

`semantic target not uniquely resolvable: {"text":"FCM-010.13.11 34684462300.1 receive-unread-probe"} count=0`

Production `fabushi.app.wait` and `fabushi.app.find` had already matched that text against the unredacted App Surface and returned the stable `message-actions:legacy:optimistic:chat-send-*` article. Returned `name`/`text` fields were intentionally `<redacted-ui-text>`. The journey then repeated the raw-text comparison client-side against those redacted fields, converting a valid server-side match into a false zero-match.

The same privacy-boundary mistake also made the old unread probe invalid: it parsed a trailing integer from the redacted assistant peer text. This would have failed immediately after the send fix.

## Atomic repair

1. Treat `fabushi.app.find` as authoritative for `text` and `name` matching; do not re-evaluate those criteria from redacted response fields.
2. Keep stable structural narrowing (`agentId`, `role`, stable ID prefixes) for message rows, self-hosted peers and forward targets.
3. Resolve the exact sent row from the server-filtered `find({text})` result using stable `message-actions:*` + `article` semantics.
4. Detect unread state from the bounded semantic snapshot structure: inside the exact assistant peer subtree, the existing `<b>` badge is rendered only for positive unread count. Do not parse redacted peer text.
5. Add deterministic regressions with `<redacted-ui-text>` returned for successful server-side text/name matches and structural unread-badge snapshots.
6. Preserve all existing fail-closed generation retry, find-limit, newest-message window, 27-category and logout/evidence gates.

## Acceptance

- Focused contract regression passes in GitHub Actions.
- Exact final PR head receives required CI success and a genuinely independent no-finding REVIEW-PASS bound to that exact head.
- Protected merge queue only; reread exact canonical main after merge.
- New canonical main produces a new immutable Release/source lineage.
- Production FCM-010.13.11 controller automatically dispatches another brand-new frozen-source target; no old run or artifact may substitute.
- A single fresh run must prove exact App-owned macOS device, all 27 categories PASS, real remote actions >0, `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller/target success, truthful evidence gate success, and playable whole-session video/trace/screenshots/logs/artifacts/digest/source lineage.
- Until that full same-run chain is green, FCM-010.13.11 and FCM-010.13 remain not passed and CI latency observer remains deferred.

## Evidence from failed fresh run

- target run: `34684462300`
- target job: `103528906858`
- App-owned device: `gha-34684462300-1-macos-app`
- source: `9e353c3973ea36b661ce31c9113ffc5184a1e758`
- Release: `desktop-1.2.56-9e353c3973ea`
- evidence artifact: `fabushi-macos-interactive-evidence-34684462300-1` / artifact id `10295246990`
- artifact digest: `sha256:0cfa377a826df08a82f2cf5c6fa403877425570e5d04eff2d1d289ce513bb122`
- successful `fabushi.app.action` count in report: `29`
- whole-session movie: playable; 84.238 seconds; one video track; first sample decoded
- final journey state: FAIL before READY/logout; this evidence is diagnostic only and is not accepted as final FCM-010.13.11 completion evidence.
