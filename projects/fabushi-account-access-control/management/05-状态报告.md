# 状态报告

## 2026-08-25 round 1
已定位 Web 管理白名单和 AI 月度 Token quota；实现账号级 super_admin/unlimitedUsage，正在进入测试、PR、CI 阶段。

## 2026-08-25 round 2
PR #2117 已通过全部 PR 门禁并进入 canonical `main` SHA `52b7c10889e585660b7d2a22a40781c22f31b7a1`。Worker production deploy 成功。`bhrum2` 的生产 AI 服务因发布快照旧于 current-main 模块布局，不能直接覆盖最新版 server；首次尝试发现缺少模块后已立即回滚恢复健康，随后基于实际生产 server 落地等价的最小兼容 hotfix。服务重启后 active，`/health` 正常；未认证 `username=bhrum108` 欺骗测试不会获得无限权限。请求的生产授权逻辑已部署，但严格仓库任务仍因 Electron packaged-user-journey 失败及尚未完成的 exact-SHA 平台门禁保持 in-progress。

## 2026-08-26 round 3
桌面登录发布闭环继续推进。已确认客户端 502 之外还有 Mahayana 平台 Worker 的独立 HTTP 500：`worker_api.rs` 中 developer-commerce method/path 被重复注册，而官方 `cloudflare/workers-rs` Router 对冲突插入直接 panic，导致包括 `/health` 在内的所有请求在路由构造阶段失败。AAC-002 已建立，正在删除重复路由并加入回归门禁；同时恢复 Windows CRLF-safe 桌面品牌预处理。完成标准保持为：合并 canonical main、平台真实 health/browser-auth 验证通过、main exact-SHA packaged E2E 通过并发布 Windows/macOS updater-compatible Release。

## 2026-08-26 round 4
用户在 macOS 1.0.941 复现 `refresh_token_reused`、Messenger 无退出登录及在线应用搜索空结果。生产 Marketplace 已独立验证可返回 9 个应用，客户端版本也是当时 Latest，因此根因收敛为 Rust session refresh 状态机 + 新 Messenger 迁移遗漏 + 市场请求错误耦合。已建立 AAC-003，实施终止型 session 自动清理、General 退出登录、账号级本地缓存清理、公开 Marketplace 与 auth refresh 解耦，以及 browse/installed 独立 settle。Renderer build 已通过；本机 Cargo 受 crates.io 镜像缺少 lockfile 指定 `futures 0.3.34` 阻塞，Rust 编译测试将由 GitHub clean CI 验证，不能通过降级 lockfile 绕过。任务保持 in-progress，直到 protected main、exact-main packaged E2E 与新 Release 全部闭环。

## 2026-08-26 round 5
AAC-003 已通过 PR #2147 并合入 main（a10079f3cd315ddfc55a4698652a91469a6978e7）。exact-main Electron run 32962607644 的 Linux 打包前真实 Host 用户旅程发现退出后 `ElectronMahayanaHostTransport::close()` 会把已经清空的 conversation journal 重新持久化为空壳 `{version:1,conversations:{}}`。Projection 与 Draft 均已正确清空且 UI 已回登录页，但严格账号缓存清理验收未满足。跟进修复将空 conversation journal 视为无缓存并删除 key，而不是在 transport close 时重建空壳；原 E2E 断言保持严格，不放宽。

## 2026-08-26 round 6
对 #2150 后的 logout 生命周期继续做真实数据审计：仅“空 journal 不落盘”仍不足以覆盖已有历史对话的用户。新增显式 `MAHAYANA_ACCOUNT_SESSION_RESET_EVENT`：Desktop 在 logout/terminal revoked-session 清理前通知所有活跃 Electron Mahayana transports，transport 立即取消待持久化 timer、清空内存 journal、删除 localStorage，并在随后 React unmount/`close()` 时禁止重新 flush 旧账号 journal。E2E 不再手工伪造 journal，而是先向真实 Messenger 会话发送消息并确认 journal 非空，再退出并要求 projection/draft/journal 三项全部不存在。

## 2026-09-06 — AAC consumer update
- FAB-P0001 Global Dharma Desktop Round B consumes AAC Host session through a bounded Mini App projection only; no bearer/refresh credential is exposed.
- New `fabushi.miniapp.execution.v1:*` durable keys are account-scoped and cleared on session reset.
- Cross-project acceptance is pending protected PR and canonical-main packaged Electron evidence.
## 2026-09-06 — AAC-004 Mini App controlled account session

State: `IN_PROGRESS`. Web/service implementation now requires stable authenticated account for Global Dharma synchronized runtime and never serializes the raw credential into runtime/tool payload. Same-account sessions share one account namespace and another account is isolated in the new integration contract. Automatic-login closure is **not** claimed yet: the repo has Electron credential gateway and a five-minute `auth.requestToken` issuer, but no verified packaged consumer path for the plugin token was found. Host bootstrap + logout/revoke E2E remain required.

## 2026-09-07 — AAC-004 current-head contract evidence

Web/service branch is synchronized to canonical `main@8595a50196309c8ebb91c3f8077125d7dc9e3ffa` at head `a53b576ab99f0c3fbeed65e4e3937424d9abd3c6`. Actions run `34047757146` backend account/runtime contract is SUCCESS with artifact `9993622901`. Stable account namespace and token-redaction contracts are therefore current-head green. Packaged Host automatic credential bootstrap plus logout/revoke proof remain open; desktop #2448 has not produced a passing packaged Global Dharma journey, so AAC-004 stays IN_PROGRESS and no packaged video is claimed.

## 2026-09-07 — AAC-004 protected merge/package evidence

#2445 delegated credential closure is protected-merged as `c82b29cd6404c2f19b93d8479b2e2cae45469249`. Desktop #2448 pre-package Host journey is green at `1655ea8070e07ad7dd8ab8e9347fbcb43f6ddf8f` / run `34051925481` with video, 12 screenshots, trace and logout cleanup. Android `1.2.52@380b6ed5a96a5b6d1295267e07d9c8dc45fa84ab` is released, but interactive `34051316405` ends `failed-timeout` after App-owned refresh failure, so AAC-004 remains IN_PROGRESS pending accepted-main package + Android terminal proof.
