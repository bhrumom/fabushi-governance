name: Retired automatic test - legacy production deploy

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by FCM-024 because this legacy path coupled deployment to automatic regression/E2E. Use the formal release orchestrator only after official MCP validation.'
