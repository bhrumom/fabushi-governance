# FCM-010.13 — Exact-source macOS release dispatch

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010
- **Status:** in-progress / fail-closed
- **Intake:** `[Fabushi:e12da10d-edb2-406d-b5fe-a7029e9bf640]`
- **Current round intake:** `[Fabushi:d3d20158-2ddd-46be-8233-a39cfe7ffada]`
- **Canonical evidence product SHA:** `7ee12b790e18049d2b9509b0c29128dd2ace690b`
- **Immutable Release tag:** `desktop-1.2.56-7ee12b790e18`
- **Generation-race controller repair canonical main:** `26348bde9091a8bb8cffa864bb911ad70046953e`
- **Evidence:** `evidence/FCM-010.13/2026-09-12-canonical-release-chain.md`

## Objective

Close the release-to-packaged-E2E control-plane gap for the measured `macos-packaged-interactive` bottleneck: every canonical macOS Release producer must trigger the installed-App interactive lane on the exact immutable Release source, and completion requires truthful App-owned end-to-end evidence.

## Atomic acceptance

| ID | Acceptance | State |
|---|---|---|
| FCM-010.13.1 | Release bridge consumes successful `Native Electron macOS test release` and `Post-main E2E Release delivery` completions | implemented |
| FCM-010.13.2 | Bridge binds to upstream exact `workflow_run.head_sha` | implemented |
| FCM-010.13.3 | Bridge resolves only a published Release targeting that SHA with an installable macOS ZIP | implemented |
| FCM-010.13.4 | Resolved Release tag dereferences to exact source SHA | implemented |
| FCM-010.13.5 | Interactive workflow dispatch uses immutable Release tag, never moving `main` | implemented |
| FCM-010.13.6 | Active/successful exact-source runs suppress duplicate dispatch | implemented |
| FCM-010.13.7 | Required lightweight CI locks exact-source bridge invariants | implemented |
| FCM-010.13.8 | PR required Actions pass | **passed** — PR #2530 head `ed0b608717ae585cf73bf6ca3b164cd018750b2f`; `CI result` run `34661029319` / check `103463385262` success |
| FCM-010.13.9 | Protected merge + canonical same-SHA desktop/native gates + published exact-source Release are verified | **passed** — source `7ee12b790e18049d2b9509b0c29128dd2ace690b`; Electron `34663960416`; native `34663960421`; post-main `34664490101`; Release `desktop-1.2.56-7ee12b790e18` |
| FCM-010.13.10 | Canonical Release path automatically creates exact-source macOS interactive run using immutable tag | **passed** — release-chain `34664577213`, dispatch job `103473806024` success/not-skipped; downstream immutable-tag dispatch proven |
| FCM-010.13.11 | Exact-source installed App-owned full journey succeeds and truthful logs/trace/screenshots/video/artifact gate passes | **failed / blocking** — generation-race repair #2547 is production-verified, but fresh target `34673991774/1` next failed on missing legacy-assistant semantic target; no READY/logout success path and whole-session MOV is absent |

## FCM-010.13.11 generation-race closure result

The historical HTTP-400 / MCP connectivity diagnosis is closed. Production controller preflight now authenticates successfully, discovers the exact run-owned device and performs real remote calls.

A live production run before the repair (`34673090393` -> target `34673135503/1`) proved the controller was already re-finding current semantic targets but exhausted the old eight-attempt budget after sustained generation churn (`173 -> 177 -> 178 -> 179 -> 180 -> 182 -> 186 -> 189`, then `expected 190, received 189`).

PR #2547 implemented the minimal bounded repair: default generation-sensitive retries `8 -> 32`, preserving fail-closed semantic re-resolution; retained the deterministic `find=95 -> expected 96` contract; added a sustained eight-stale regression proving each retry re-runs `find` and eventually acts with the newest generation/ref; and made helper changes trigger controller contract CI. Required CI passed and protected merge queue landed #2547 as `main@26348bde9091a8bb8cffa864bb911ad70046953e`.

Post-merge controller run `34673949330` passed controller contract, protected-account MCP preflight and immutable Release provenance, then automatically dispatched exact target `34673991774/1`:

- source `7ee12b790e18049d2b9509b0c29128dd2ace690b`
- tag `desktop-1.2.56-7ee12b790e18`
- device `gha-34673991774-1-macos-app`
- install/login/App-owned registration/Action-owned semantic smoke: success
- secondary packaged App Surface Playwright: success
- 56 successful real remote device actions recorded
- generation-sensitive stale retries successfully crossed the former failure point; both self-hosted test channels were created and there was no retry-budget exhaustion.

The next failure is semantically distinct and outside the generation-race repair scope:

`semantic target not found: {"agentId":"test:peer-legacy:conversation:mahayana-ai:agent:assistant"}`

The controller failed closed, wrote a FAIL note and called `ci_session_finish`. Because the full journey was incomplete, it did not emit `READY_FOR_LOGOUT PASS`, did not execute the success-path fresh snapshot, and did not invoke exact `settings-logout`. Target `34673991774` and the truthful evidence gate both therefore correctly finished `failure`.

Failure target artifact `fabushi-macos-interactive-evidence-34673991774-1` is ID `10291553529`, 160229449 bytes, digest `sha256:fde3ea98c320218c4716a049bffeeb48a5a27020e23b5b3e0cf155414bf5b0e8`, 112 files. It contains trace/log/screenshots/Playwright report and trace, but does **not** contain `macos-session.mov`; `screencapture.log` reports `IOServiceMatchingfailed for: AppleM2ScalerParavirtDriver`.

## Current blocker

FCM-010.13.8-.10 stay passed and are not rerun merely for this controller repair. The generation race requested in the current round is fixed and verified in production. FCM-010.13.11 remains blocked by two newly isolated acceptance gaps:

1. missing `test:peer-legacy:conversation:mahayana-ai:agent:assistant` semantic projection during the full journey;
2. missing whole-session video output from the macOS Actions recorder.

Neither gap is silently folded into the completed generation-race code change. FCM-010.13 remains `in-progress / fail-closed` until a new production run satisfies every FCM-010.13.11 acceptance gate. The CI latency observer / bottleneck-compression round remains sequenced **after** that closure and has not been run early.
