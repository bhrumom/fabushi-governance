# GBF-505 Evidence

CI guard rejects `GrokBotMarkEngine`, `grok-bot-mark-engine`, and production Grok vendor runtime paths. Current search only finds those terms inside anti-regression guards/project evidence.
