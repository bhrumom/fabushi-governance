# FCM-010.13.11 exact-head independent review — PR #2561

- Repository: `bhrumom/fabushi`
- Project: `FAB-P0003 / FCM`
- Stable atomic task ID: `FCM-010.13.11`
- Canonical task: `projects/fabushi-cicd-merge-governance/management/tasks/FCM-010.13.11-unread-deselect-governance-repair.md`
- Canonical task slug: `FCM-010.13.11-unread-deselect-governance-repair`
- Product PR: `#2561` (`fix(fcm): restore production assistant unread transition`)
- Exact reviewed base: `9785d8e1b71e0d61cf31541af28b910d5be58bd9`
- Exact reviewed head: `3565c9d483cb29e7729e3003e2b844b46124e18f`
- Review date: `2026-09-13`
- Review disposition: **REVIEW-FAIL / BLOCKED**
- Findings: `P0=0`, `P1=2`, `P2=0`, other blocking findings `=0`

This is a records-only independent reviewer artifact. It does not modify PR #2561's product head, does not mark the product PR ready, does not merge, release, dispatch, rerun CI, or authorize production acceptance. Old reviews, old reviewer records, old handoffs, and old Codex findings were treated only as navigation clues; the findings below were independently re-established from current GitHub/repository truth.

## Start live lock

At the start of this fresh review, GitHub live-read showed PR #2561:

- state: `open`
- draft: `true`
- merged: `false`
- mergeable: `true`
- actual head: `3565c9d483cb29e7729e3003e2b844b46124e18f`
- actual base: `9785d8e1b71e0d61cf31541af28b910d5be58bd9`

The head and base strictly matched the requested review lock, so review proceeded. The PR contains 17 commits and 13 changed files. The complete PR diff, all 17 commits, changed paths, current reviews/comments/threads, current check/workflow state, root `AGENTS.md`, canonical task/spec/WBS/risk/blocker/status/changelog, reviewer-record precedent, active main ruleset, automerge changed-path logic, and post-main controller/evidence contracts were re-read.

## Exact specification binding

Per the repository reviewer-record precedent, `spec_digest` is SHA-256 over this UTF-8 manifest, lexicographically sorted by path and serialized as `<path>\t<git_blob_sha>\n`. Every blob SHA below was read from GitHub at exact product head `3565c9d483cb29e7729e3003e2b844b46124e18f`, then re-read again immediately before the verdict; both reads matched.

```text
projects/fabushi-cicd-merge-governance/SOURCE_OF_TRUTH.md	a0f02dc787d9005e4b2ea781766243a962ab6872
projects/fabushi-cicd-merge-governance/docs/02-需求与成功指标.md	98b9fbbe00bdeafd87a3b767eb730405213a1bae
projects/fabushi-cicd-merge-governance/docs/19-完成定义与验收.md	16255a2e702da2a8b04519aa46f0425da5e861af
projects/fabushi-cicd-merge-governance/management/01-WBS原子任务.md	7e5068102bf72a61c9e2dfe7884d90f71d6774db
projects/fabushi-cicd-merge-governance/management/03-验收追踪矩阵.md	fad9e0d4384bd960015bd8b66692f3c07f0ee6b2
projects/fabushi-cicd-merge-governance/management/04-风险登记.md	8ee79e9fb39f6ca03531a0329ba97f8ae24c2845
projects/fabushi-cicd-merge-governance/management/05-状态报告.md	9d1e21fa81f3016f4e80393c68cb5c1d30701637
projects/fabushi-cicd-merge-governance/management/06-依赖与阻塞.md	03f9c7dea1a13b16bea819030d3b70ea017f64f6
projects/fabushi-cicd-merge-governance/management/07-变更日志.md	9d238fca52b459410090a6302e1b22c3acbb0086
projects/fabushi-cicd-merge-governance/management/tasks/FCM-010.13.11-unread-deselect-governance-repair.md	85c4fd7bf2325fc69e0002b5bcde9f0feb775d86
```

- `spec_digest = 391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307`

`review_key` follows the repository reviewer-record formula:

`SHA256(repository + pr_number + exact_head_sha + exact_base_sha + atomic_task_id + spec_digest)`

using the stable atomic task ID `FCM-010.13.11` (the full canonical task slug/path is recorded separately above), with raw UTF-8 concatenation and no separators:

- repository: `bhrumom/fabushi`
- pr_number: `2561`
- exact_head_sha: `3565c9d483cb29e7729e3003e2b844b46124e18f`
- exact_base_sha: `9785d8e1b71e0d61cf31541af28b910d5be58bd9`
- atomic_task_id: `FCM-010.13.11`
- spec_digest: `391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307`

- `review_key = 06092478ac0acac864bb04b06e395b84b87208ce6583b003766f4d79d88f4e5a`

Any change to the product head/base or any bound specification blob invalidates this review.

## Code and semantic review

No product-code P0/P1/P2 finding was found in the unread repair itself.

The reviewed production behavior is consistent with the task's intended transition:

1. persisted visible history initializes as read (`unread=0`);
2. only visible assistant completions can increase unread (`0 -> positive`);
3. user messages do not increase unread;
4. read-through boundaries are stored per conversation, so opening a shared-provider non-assistant conversation does not clear the assistant's unread state;
5. an explicit assistant `conversation.open` uses the 200-history contract and clears only that conversation's unread boundary (`positive -> 0`);
6. FeatureHost background indexing requests 2000 history items, Runtime clamps that request to 500, and that background 500 path does not acknowledge unread;
7. hidden/background completions do not persist visible history or create unread, while the runtime completion event remains emitted so the completion contract is preserved;
8. reset clears both history and read-boundary state; persisted-history initialization/reset tests cover the lifecycle;
9. Electron requests authoritative `conversation.list` refresh after a non-Mini-App open and after assistant `chat.message`; the transport does not manufacture a local unread value;
10. the change introduces no new external dependency version and only test-support/dev dependency wiring for already-owned crates.

The async/error/privacy review found no new blocking deadlock, reentrancy, stale-generation, hidden-content persistence, or privacy regression in the changed code. Runtime state mutation precedes the completion event that triggers the Electron refresh, and explicit open marks the read boundary before its accepted path returns.

## Regression execution evidence

Existing exact-head CI was only re-read; nothing was rerun.

- `CI` run `34704136588`: success; `CI result` success.
- `Electron desktop quality gate` run `34704136680`: success; Linux real Rust Host pre-package user journey actually executed and succeeded. PR-only package/post-main jobs were correctly skipped and are not counted as production acceptance.
- `Host fast E2E` run `34704136558`: success; complete Host user journey executed.
- `Mahayana fast checks` run `34704136567`: success; direct Host and feature-host gates executed.
- `Global Dharma Rust` run `34704136681`: success; its `mahayana-test` job actually ran `cargo test --locked ... --package mahayana-runtime`, covering the added runtime unread/read-boundary unit regressions.
- `Computer control security gate` run `34704136551`: success; Linux/macOS/Windows `rust-contracts` all actually ran `cargo test --locked ... -p mahayana-feature-host`. Because `mahayana-feature-host` default features are `production + local-only`, the new production cross-layer test `fcm_010_13_11_production_adapter_keeps_read_boundary_conversation_scoped` is included in that executed test surface.

The exact-head production adapter test exercises FeatureHost -> MahayanaHost -> Runtime -> Kernel/provider behavior, verifies initial `0`, visible assistant completion to `1`, unrelated conversation open preserving `1`, background 2000 -> Runtime 500 preserving `1`, hidden completion preserving history/unread, and explicit assistant open returning to `0`.

## Downstream source/Release/device/evidence invariants

PR #2561 does not modify the post-main controller, macOS target workflow, release workflow, or evidence-gate scripts. Those exact-head contracts were directly re-read rather than inferred from the PR description.

The unchanged `FCM-010.13.11 macOS external controller` still requires an immutable exact-source desktop Release, exact tag/source identity, a digest-bearing macOS ZIP, a fresh target run bound to that source/tag, the exact App-owned device ID, controller success, exact target success, and a digest-bearing target evidence artifact.

The unchanged macOS interactive target still enumerates exactly 27 required categories, requires real successful `fabushi.app.action` calls with `remoteActionCount > 0`, writes `READY_FOR_LOGOUT PASS` only after all categories, then requires `ci_session_finish`, a fresh App snapshot containing exact `settings-logout`, and the exact logout invoke. It also requires whole-session recording verification, screenshots, device-call trace, remote notes, logs, and truthful evidence reporting. No old run/artifact is promoted by this PR.

## P1-1 — Durable canonical task/status/change records are stale for the reviewed exact head

**Severity: P1 / blocking.**

This finding was independently reproduced from the exact-head files; it is not inherited from an old reviewer conclusion.

Root `AGENTS.md` requires each durable task record to retain current branch/commit/PR, status, verification/check evidence, blockers/risks, next action, and timestamps, and requires progress/material changes to be synchronized into project status/changelog/risk/dependency records.

At exact head `3565c9d483cb29e7729e3003e2b844b46124e18f`:

- the canonical task record still describes an earlier implementation point and a pending exact-head CI/re-review state rather than the final `3565c9d...` commit and its completed check chain;
- WBS/risk/dependency text still describes the old repair/re-review transition instead of the final exact-head check truth;
- `management/05-状态报告.md` does not record the final PR #2561 exact head/check state;
- `management/07-变更日志.md` does not contain the current PR #2561/final runtime-unread repair transition;
- therefore the durable canonical records cannot, by themselves, reconstruct the exact task/spec/head/base/check/review chain required before protected-queue authorization.

The green exact-head Actions state does not cure this record-truth mismatch; the repository's own governance requires the durable records to match GitHub truth.

## P1-2 — Required changed-path `Messaging Product Gate` has no successful run for the exact head

**Severity: P1 / blocking.**

This finding is derived from the repository's current automerge contract plus the live exact-head workflow list.

`.github/workflows/automerge.yml` defines `Messaging Product Gate` as a required product workflow whenever a changed path matches its messaging path prefixes. PR #2561 changes, among other files:

- `third_party/mahayana/mahayana-rs/mahayana-feature-host/Cargo.toml`
- `third_party/mahayana/mahayana-rs/mahayana-feature-host/src/implementation.rs`

Those paths match the automerge messaging prefix `third_party/mahayana/mahayana-rs/mahayana-feature-host/`, so the automerge contract requires `Messaging Product Gate` in addition to base `CI` and the Electron gate.

The complete current pull-request workflow-run list for exact head `3565c9d...` contains successful `CI`, `Electron desktop quality gate`, `Fabushi self-hosted messaging`, `Mahayana fast checks`, `Computer control security gate`, and other workflows, but contains **no** `Messaging Product Gate` run. `Fabushi self-hosted messaging` is not a valid substitute because the automerge implementation checks the required workflow name exactly.

The exact-head `.github/workflows/messaging-product-gate.yml` currently has only a `workflow_dispatch` trigger and one placeholder job that echoes `Automatic messaging gate paused for 2026-09-05 Mac test release.` It does not naturally run on this PR. This review did not dispatch it, as required by the review boundary, and a skipped/missing/old/different-named run cannot be substituted for a real successful required gate.

Therefore the required changed-path gate set is not green for this exact head and protected-queue authorization is not valid.

## Protected merge truth

The live repository ruleset `main-merge-queue` (`15857448`) is active for `refs/heads/main`, requires merge queue with `ALLGREEN`, requires `CI result`, and has no bypass actors. The current `Explicit automerge` workflow success on this draft PR is not an authorization: the workflow explicitly exits early when a PR is draft, before it verifies/enqueues the product workflow set. PR #2561 remains draft and has not entered the protected queue.

## Verdict

**REVIEW-FAIL / BLOCKED** for exact product head `3565c9d483cb29e7729e3003e2b844b46124e18f` against exact review base `9785d8e1b71e0d61cf31541af28b910d5be58bd9`.

- P0: `0`
- P1: `2`
- P2: `0`
- other blocking: `0`
- `spec_digest`: `391e7a3ee4bda068398ff166f45371391a877d728ebc16e4fe96802a4a310307`
- `review_key`: `06092478ac0acac864bb04b06e395b84b87208ce6583b003766f4d79d88f4e5a`

This record does **not** authorize ready/merge/release/dispatch. The only valid next step is for the execution/governance owner to repair the two P1 blockers without weakening product gates: synchronize the durable canonical task/WBS/risk/dependency/status/changelog truth to the final exact-head/check state, and restore a real PR-triggered (or otherwise repository-approved, exact-head, substantive) successful `Messaging Product Gate` for the changed messaging surface. Any resulting product/spec/head/base change invalidates this review and requires a brand-new independent exact-head review.