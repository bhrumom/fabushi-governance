# 05 状态报告

## 2026-08-22 — MSR-001 Round 1
- Located no existing project folder for the exact Codex + Grok Build -> sovereign Mahayana objective.
- Verified PR #1971 already implemented the first convergence baseline and merged to main as `5dcfaee4b8fb12896f9ac92c6dbc51317d10b942`.
- Identified #1963/#1968 as superseded history and #1967 as obsolete reverse-sync work against a superseded branch.
- Verified Fabushi `SOURCES.lock` pins Codex/Grok Build provenance and the source-boundary rule.
- Created the enterprise project baseline on branch `docs/mahayana-sovereign-runtime-project`.
- Acceptance remained pending until protected merge.

## 2026-08-22 — MSR-001 Round 2 / MSR-101-103 Round 1
- PR #1989 passed CI run `32559149040`, merged through protected main as `88db63c328c3cba39971f3942509cb0b582502bc`, and the canonical project folder was re-read from `main`; MSR-001 is passed.
- Re-audited upstreams: Codex advanced to reviewed commit `343074d4207d572809bd8cea15f4be1d09d98e0b`; Grok Build remains reviewed at `19d42e35c07a9c9244f03f6df0c4c353f970d4f9`.
- Added `docs/08-upstream-capability-matrix.md`, including Codex runtime MCP status / unfinished-turn suspension and Grok Build goal/oracle/permission/loop/workflow capabilities.
- Source audit confirmed many Grok-derived primitives already exist natively in `mahayana-kernel`, `mahayana-workspace-engine`, and `mahayana-orchestrator`; these remain `partial` until production-path and cross-surface evidence exists.
- Opened MSR-103 implementation on `feat/mahayana-native-auth-boundary`.
- Added first-party `mahayana-auth` and `mahayana-secrets` crates.
- Rewired `mahayana-product` login/secrets dependency resolution to those Mahayana packages while keeping six private source aliases temporarily to minimize the high-risk product-client diff.
- Added encrypted-file compatibility, dedicated Mahayana keyring services, lazy migration of historical managed-secret encryption keys, atomic writes, and Unix 0600/0700 hardening.
- Extended `check-mahayana-source-boundary.py` so native auth/secrets cannot import vendor types and the product aliases cannot point back to vendor source paths.
- Extended `Mahayana fast checks` to test the native crates and compile/test the product client against them.
- CI/merge evidence for MSR-101/102/103 is pending; none of those tasks is represented as passed yet.

Next: open the implementation PR, repair all Actions failures, merge/re-verify; then execute the remaining matrix gaps in MSR-201/202/301/302/401/501/601.

## 2026-08-24 — MSR-106 Round 1 — desktop idle energy diagnosis

- Target Mac hidden/background baseline isolated the main drain: GPU/renderer fell near zero, while `mahayana-app-host` remained around 21.6% CPU and the Electron main process around 0.9%.
- `sample` captured `feature_receive -> crossbeam_channel::Receiver::recv_timeout`; source inspection found a 1 ms production receive timeout followed by the Electron 10 ms retry loop, creating continuous idle wakeups.
- Repair keeps existing `crossbeam-channel` and adds an explicit bounded `timeoutMs` boundary: Electron requests a 500 ms long poll, direct/test callers remain non-blocking, and queued events drain with zero additional wait after wakeup.
- Task remains in-progress pending CI, protected merge, exact-main package and target-Mac post-fix energy measurement.

## 2026-08-24 — MSR-106 Round 2 — renderer/GPU background energy

- Target-Mac packaged `1.0.798` with Fabushi unfocused but still open isolated a second energy source: GPU about 60.9% CPU and renderer about 22.5%, while ChatGPT on the same machine sampled near idle.
- Root cause is continuous BotMark JS/CSS motion surviving application blur because intersection visibility is not equivalent to foreground focus.
- Shared motion now suspends on hidden/unfocused documents, resumes on focus, and dispatches focused organic motion at 30 FPS; CSS BotMark animations follow the same lifecycle.
- MSR-106 remains in-progress until the packaged post-merge build is re-measured on the target Mac.

## 2026-08-28 — MSR-203 Round 1
- User clarified `.agent/skills/bb-browser` is an unused external MCP/browser component and must not be counted as a native Bot capability.
- Re-audited OpenAI Codex current main (`41d3dc56a0e1de47e30a9585c1b49253c082f8f7`): live web search is treated as an Agent runtime tool, not as a browser-MCP dependency.
- Audited TinyFish official Search/Fetch contracts and MIT-licensed cookbook; selected direct TinyFish provider instead of Monid CLI/runtime indirection.
- Implemented provider-neutral Mahayana `web_search` and `web_fetch` tools behind the existing `Capability::WebSearch`, tool-loop, Hook, permission and network-policy boundaries.
- Added runtime-only `TINYFISH_API_KEY` loading, optional endpoint overrides, public-URL validation, bounded page text, sanitized provider failures and no secret propagation into model/tool output.
- Local verification on Rust 1.98.0: `cargo fmt --all -- --check` passed; `cargo check -p mahayana-native-engine` passed; `cargo test -p mahayana-native-engine` passed with 12 unit tests + 2 capability-boundary integration tests. Search/Fetch tests use local mock providers and no live TinyFish credential.
- Task remains `in-progress`: protected PR CI/main merge and canonical-main delivery evidence are still required. Production activation additionally requires a TinyFish key in the trusted runtime secret boundary.

## 2026-09-17 — MSR-403 Round 1
- User required Mahayana CLI to become an install-and-login controllable Fabushi device, with Codex-style one-line installers and automatic same-account discovery through the official Fabushi MCP.
- Official MCP authentication is currently healthy for the CI test account, while `list_devices` returned zero devices before CLI installation/repair.
- Reused the existing `mahayana-sovereign-runtime` project rather than creating a parallel project; added MSR-403 and ADR-0003.
- Acceptance remains in-progress pending implementation, Actions evidence, exact artifact installation, same-account login, and official MCP control proof.

## 2026-09-17 — MSR-403 Round 2 implementation staged
- Added a native `mahayana device start|status|stop|serve` agent inside the standalone Rust CLI. The agent uses the existing Fabushi account session, connects only to `wss://fabushi-mcp.ombhrum.com/agent`, exports bounded tool descriptors, and removes login-only credential environment variables before daemonizing.
- Password and Alipay CLI login now bind a stable CLI device id and automatically start the device agent; logout requests agent shutdown. Older/test sessions can use the explicit CLI `DEVICE_ID` fallback without exporting refresh credentials.
- Normal machines persist only a local stable installation id. GitHub Actions uses run-scoped `gha-*` identity and advertises `kind=github-actions`; the gateway now returns only live sockets and immediately removes disconnected GitHub-runner registrations.
- Added official-site one-line installers at `/mahayana/install.sh` and `/mahayana/install.ps1`, checksum verification, automatic background-agent start, a manual no-test multi-platform packaging workflow, and a manual live-device workflow that logs into the dedicated CI test account and waits for official MCP validation.
- Added gateway lifecycle regression coverage, but did not run it locally. `git diff --check` passes. A local `cargo metadata --locked --no-deps` attempt could not load the intentionally sparse worktree because `third_party/mahayana/codex-rs` is not expanded; compile/package verification is therefore delegated to GitHub Actions as required by repository policy.
- Acceptance remains in-progress until Actions compiles the exact branch, an exact CLI artifact registers under the official MCP account, `vps_status` is called through the official MCP, `ci_session_finish` ends the runner, and a second `list_devices` proves the ephemeral entry disappeared.

## 2026-09-17 — MSR-403 Round 3 — branch runtime proof passed, production lifecycle gap confirmed
- Manual live-device Action run `35172684311` on exact branch source `e9a696941a05ca1b66a8a56b8a7557c6aec0f7f3` built the CLI, authenticated the dedicated CI test account, and registered CLI-owned device `gha-35172684311-1-interactive` with the official Fabushi MCP.
- Official MCP independently described and called `vps_status`; a harmless `run_shell_command` returned `MAHAYANA_REMOTE_OK`; `write_text_file` also completed successfully.
- The runner workflow completed successfully after the remote validation window was finished.
- The currently deployed production MCP gateway still exposes the stopped runner as `offline`, proving the old deployed `list_devices` semantics do not yet satisfy the user's ephemeral-device requirement. The branch implementation already changes discovery to live-only and deletes GitHub Actions registrations on disconnect.
- MSR-403 remains in-progress until #2683 is merged to canonical main, the official MCP gateway is atomically redeployed from that exact main lineage, an exact-main runner is revalidated, and the stopped runner disappears from `list_devices`.

## 2026-09-17 — MSR-403 Round 4 — canonical-main device lifecycle passed
- Protected merge queue produced canonical main `c70eba65f38cd61c3cf6c5412868f7a1b92df4dd` for PR #2683; merge-group CI `35173253639` passed.
- Production `fabushi-remote-mcp` was atomically redeployed on `bhrum2` with canonical-main live-only gateway/server files; local and public health passed and the existing ChatGPT OAuth account remained connected.
- Exact-main manual live-device run `35173406319` registered `gha-35173406319-1-interactive`; official MCP successfully described status, called status, ran a harmless shell command, wrote a bounded proof file, and invoked `ci_session_finish`.
- After disconnect, production `list_devices` returned no devices. MSR-403.C/D live-only and ephemeral cleanup behavior is therefore proven on canonical main.
- Public installer scripts are already live at `ombhrum.com/mahayana/` and byte-identical to canonical main. Multi-platform package/latest-channel and a corrected green site-verifier run remain before final completion.

## 2026-09-17 — MSR-403 Round 5 — final released-installer closure passed
- Windows startup was closed without changing desktop App behavior: PR #2692 moved CLI liveness to native Windows process queries and PR #2693 removed redundant pre-login device startup from both public installers so account-scoped registration begins only after successful login.
- Exact CLI source `38eb8312776ee35d18a5807a5e799aa22cc7a764` passed no-test five-platform package run `35186579174` and immutable channel `mahayana-cli-main-38eb8312776e`; later control-main `aa1063891ebc66111042c40b0602856add6c9bc0` has no `third_party/mahayana` diff from that payload.
- Official-site run `35194816512` passed; live shell/PowerShell installers matched canonical main byte-for-byte.
- Final manual released-installer run `35195155342` succeeded on Linux, macOS and Windows. Official MCP independently discovered all three CLI-owned ephemeral devices, described the live schema, executed native status/shell controls (including Windows `MSR403_38EB_WINDOWS_OK`), and wrote a bounded Windows proof file.
- `ci_session_finish` succeeded for all three runners, the Action finished green, and the subsequent official-MCP `list_devices` returned an empty array.
- MSR-403.A-F and traceability item MSR-R10 are now passed; D-06 is satisfied. No automated product E2E/test gate was introduced or used.

## 2026-09-18 — MSR-105 first-turn readiness continuation
- User clarified that cached UI/optimistic indicators are insufficient: the real Mahayana runtime/provider session must already be usable before the first message is sent.
- Source inspection proved `KernelConversationProvider` deferred `EngineBackend::open_session` until `send_message`; compatibility backends therefore deferred real `start_thread` work to the first user turn.
- Branch `fix/msr-105-first-turn-readiness` adds provider-neutral idempotent warmup, wires it through Runtime/Host, and binds it to every authenticated account boundary so restored sessions and fresh password/browser/OAuth login complete readiness before `hostReady` enables sending.
- Added a Rust regression that warms twice, sends the first visible prompt, and requires exactly one Agent thread creation.
- Open-source review reused the lifecycle principles from Codex session startup and Grok Build initialize/session-new-before-prompt; no upstream code was copied.
- No behavioral/E2E run was requested. Exact-head compile/quality CI, protected merge and canonical-main readback remain pending; MSR-105 stays in progress.
