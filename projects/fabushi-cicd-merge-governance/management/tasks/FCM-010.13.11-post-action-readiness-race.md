# FCM-010.13.11 — Post-action App Surface readiness race

- **Project:** FAB-P0003 / FCM
- **Parent:** FCM-010.13
- **Status:** in-progress
- **Intake:** `[Fabushi:acc40c75-64fe-440a-939c-bfcfa9c09aac]`
- **Baseline:** `main@02c8609c1b7afcb6960e3a07319fbffe5bfc90a0`
- **Branch:** `fix/fcm-010-13-11-post-action-readiness`

## Proven before this task

- FCM-010.13.8-.10 remain passed and are out of scope.
- Frozen source stays `7ee12b790e18049d2b9509b0c29128dd2ace690b`; immutable Release tag stays `desktop-1.2.56-7ee12b790e18`.
- PR #2542 proved stable-agent stale-generation rebasing in production.
- PR #2543 proved query-only semantic re-resolution in production. In controller run `34671836710` against target `34672126158`, `test:profile-navigation-chats` rebased generation `76 -> 82`, and query-only `新建频道` rebased `90 -> 92` and the action completed.

## Newly exposed race

Immediately after the successful `新建频道` action, the controller queried the expected `role=textbox,name=频道名称` target before the dialog semantic surface became visible. `fabushi.app.find` returned generation `95` with no matching target and the journey failed `semantic target not found`.

This is a post-action surface-readiness race in the same controller chain, not an MCP connectivity regression and not a reason to weaken generation, source, tag, device or evidence gates.

## Atomic acceptance

1. After invoking `新建频道`, explicitly wait fail-closed for the unique `频道名称` textbox to become visible before attempting `setValue`.
2. Add a controller contract assertion that locks the invoke -> wait-visible -> setValue order.
3. Do not change the frozen source/tag, exact `gha-<run>-<attempt>-macos-app` rule, 27 required categories, or `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh snapshot -> exact settings-logout` order.
4. Merge only through required CI and the protected merge queue.
5. A new production controller run must auto-dispatch a new exact frozen-source target and produce complete green evidence before FCM-010.13.11 or FCM-010.13 can be passed.

## Completion evidence required

Exact new App-owned device, all 27 categories PASS, nonzero real remote actions, `READY_FOR_LOGOUT PASS`, successful `ci_session_finish`, fresh App snapshot, final exact `settings-logout`, target run success, truthful evidence gate success, and retained trace/log/screenshots/full-session video/artifact/digest.
