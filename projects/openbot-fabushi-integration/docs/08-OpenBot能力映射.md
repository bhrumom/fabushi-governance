# OpenBot feature -> Fabushi implementation -> evidence -> gap matrix

Pinned upstream: `CopilotKit/OpenBot@19fdcbb7fd5c072d5c2aa95800fcbbbf72aa202c` (MIT). This round initially opened from `main@9365b5e13f1fb5ac7b31f00049e487ab2174edbc`; while the round was executing, canonical main advanced one commit and the OBF branch was synchronized so its current canonical base/merge-base is `main@c41e9305dd2c2af4f48139dadfefe71eef50068f`. Reference screenshot: user task `Fabushi:82655b0d-e929-44c7-a4f4-fdcfd0a13c8a`, original `2048x1280`, SHA-256 `555f8ae43517b2f6b2a1e545a16c0eb95e596585a1c9adfd211cad6a217c475b`; app-controlled comparison crop `(176,196,1671,937)` remains the visual acceptance surface defined in `docs/09-视觉回归规范.md`.

Classification:

- `EQUIVALENT`: an existing Fabushi capability is the canonical implementation; OBF must reuse it.
- `GAP-CLOSED`: a verified missing surface/contract was implemented in OBF-001; CI / packaged acceptance can still be pending.
- `NOT-FUSED`: upstream deployment semantics conflict with Fabushi architecture or duplicate an existing source of truth; the reason is explicit.
- `EVIDENCE-PENDING`: implementation exists, but the exact fresh packaged reference run is not yet proven.

| OpenBot feature | Fabushi corresponding implementation | Evidence/source on canonical main or OBF delta | Disposition / remaining gap |
|---|---|---|---|
| Matte dark workspace | Messenger + authoritative OpenBot parity layer | `desktop/src/openbot-ui-parity.css`; merged UI/reply baseline PR #2580 | `EQUIVALENT`; fresh packaged pixel evidence pending |
| Coworker roster/search | Messenger peer list + search | `desktop/src/messaging-shell-v2.tsx`; `desktop/src/messaging-shell.module.css` | `EQUIVALENT`; Chief/Research/Builder/Launch packaged scene pending |
| Geometric coworker avatar | canonical deterministic `BotMark` | `frontend/apps/web/src/app/host/bot-mark.tsx`; OBF parity removes roster-position shape overrides | `GAP-CLOSED`: roster/header/transcript use canonical Bot identity; restart proof is reserved for the real packaged acceptance |
| One channel per coworker | Messenger bot/conversation projection | `desktop/src/messaging-shell-v2.tsx`; existing Mahayana conversation transport | `EQUIVALENT`; no second OpenBot channel or transcript store added |
| Reply thinking | Mahayana operation lifecycle -> live thinking row | `desktop/src/messaging-shell-v2.tsx` `agentThinkingRow`; `desktop/src/openbot-ui-parity.css` | `GAP-CLOSED`: live row, not static transcript prose; packaged event proof pending |
| Reply action/tool/result | Mahayana workbench/step lifecycle -> action rows | `desktop/src/messaging-shell-v2.tsx`; `desktop/src/mahayana-agent-workbench.tsx`; `agent-step[data-status]` | `GAP-CLOSED`: running/completed/failed must be observed in a real packaged run |
| Final assistant completion | ordinary Mahayana assistant message/completion projection | `desktop/src/messaging-shell-v2.tsx`; `desktop/e2e/openbot-packaged-acceptance.spec.ts` uses the normal composer and waits for a completed real run | `EQUIVALENT`; exact fresh provider/runtime completion proof pending |
| Component / structured result | runtime-authored Markdown heading/table renderer | `StructuredMessageBody` / `parseStructuredMessage` in `desktop/src/messaging-shell-v2.tsx` | `GAP-CLOSED`: no hard-coded Final launch brief transcript |
| Final launch brief 3-column table | generic structured result table | `assistant-result-table`; packaged test requires exactly `Workstream | Owner | Status` and three runtime-authored rows | `GAP-CLOSED`; fresh packaged proof pending |
| Source files / attachments | Messenger file messages + generic `Source files:` chip projection | `messageFile`; `assistant-source-files`; packaged test attaches real `launch-brief.md` and `launch-metrics.csv` | `GAP-CLOSED`; same-run artifact proof pending |
| Hover reply/copy/more | existing reply/context operations exposed as hover toolbar | `message-hover-actions`; existing `MessageContextMenu` | `GAP-CLOSED`; visual placement/diff pending |
| Composer | existing Messenger attachment/text/send composer | `desktop/src/messaging-shell-v2.tsx`; `messenger-input` / `messenger-send` | `EQUIVALENT`; packaged geometry/diff pending |
| Per-coworker computer | Fabushi registered App-owned/user computer runtime, routed by agent/host identity | `packages/mahayana-core/src/host/host_contract.dart`; `host_adapter.dart`; `action_dispatcher.dart`; `services/chatgpt-vps-control/remote-computer-peer/remote_peer.py` carry `agentId` for `computer.screenshot` / `computer.action` | `EQUIVALENT` computer routing; `NOT-FUSED` for OpenBot's mandatory container-per-coworker machine because Fabushi's source of truth is the registered/App-owned computer |
| Per-coworker browser | browser automation on the assigned Fabushi computer | `chatgpt-vps-control/lib/browser-session.js`; Chrome/CDP integration in `chatgpt-vps-control`; Mahayana unified tool bus described by `docs/mahayana-sovereign-kernel.md` | `EQUIVALENT` browser capability; `NOT-FUSED` for mandatory isolated OpenBot browser profile per coworker until Fabushi adopts that tenancy requirement |
| Per-coworker files | Mahayana workspace/file tools + Messenger documents | Mahayana workspace/tool runtime; `MessagingMediaRef` / file message path; sovereign-runtime workspace capability records | `EQUIVALENT` file capability; `NOT-FUSED` for a mandatory independent OpenBot `/workspace` filesystem per coworker because Fabushi does not use that container tenancy model |
| Per-coworker tools / MCP | Mahayana unified Tool Bus, MCP, plugins and Mini Apps | `docs/mahayana-sovereign-kernel.md` (unified tool bus); `frontend/packages/mcp-app-sdk/src/webmcp.ts`; existing plugin/Mini App contracts | `EQUIVALENT`; do not import a second OpenBot MCP gateway/catalogue |
| Shell through the same policy gate | existing Mahayana/local execution path | ToolHost/local execution path plus Host policy in `packages/mahayana-core/src/host/host_contract.dart` / `action_dispatcher.dart` | `EQUIVALENT`; existing policy boundary remains authoritative |
| Live screen | App-owned snapshot/live remote channel + semantic/device control | `computer.screenshot` host contract/adapter/dispatcher; remote-computer peer; `fabushi.app.snapshot`; FAB-P0009 device-control project | `EQUIVALENT` underlying capability; OpenBot panel placement is visual/product evidence only and must not create a second screen stack |
| Human take-over | existing remote computer/session control | `computer.action` with `agentId`; remote-computer peer; existing device-control/RDF session semantics | `EQUIVALENT` control capability; exact OpenBot `help_requested/control_taken/control_released` protocol vocabulary is `NOT-FUSED` because Fabushi has its own audited session protocol; packaged lockout/audit proof pending |
| Policy before action | Mahayana Host policy/approval | `HostPolicy.localToolPermission`, `screenshotsAllowed`, scopes/allowed tools in `packages/mahayana-core/src/host/host_contract.dart`; enforcement in `action_dispatcher.dart` | `EQUIVALENT`; packaged allow/deny evidence pending |
| Audit after action | existing Host audit list + run/evidence journals | `packages/mahayana-core/src/host/host_adapter.dart` handles `audit.list` / `audit.clear` with agent filtering; `frontend/apps/web/src/app/host/host-client.tsx`; workbench/event journals | `EQUIVALENT`; OpenBot `/admin/audit` route identity is `NOT-FUSED` because Fabushi already has a Host/admin surface |
| Secrets not exposed in transcript | existing sensitive-input/credential boundaries | credential gateway + sensitive-input policy | `EQUIVALENT`; no OpenBot secret store is copied |
| Durable threads / restart | Mahayana persistence + Messenger projection | existing Host/client persistence; packaged acceptance restarts the same app-data directory and rechecks Chief identity | `EQUIVALENT`; fresh packaged restart evidence pending |
| Memory | Fabushi/Mahayana conversation/workspace memory semantics | existing host/runtime persistence | `EQUIVALENT`; no OpenBot memory store imported |
| Components instead of prose | Mini Apps/WebMCP + generic ordinary-message structured result | `frontend/packages/mcp-app-sdk/src/webmcp.ts`; `StructuredMessageBody` | `GAP-CLOSED` for the reference ordinary reply table; Mini Apps remain the richer canonical component system |
| Skills | existing Skills/plugin/Mini App ecosystem | marketplace/plugin/tool contracts | `EQUIVALENT`; tenant-specific OpenBot skills are data, not a fusion target |
| Routines/scheduled work | existing Fabushi automation/workflow runtime | MSR/automation surfaces | `EQUIVALENT`; outside this screenshot journey |
| Admin computers/boundaries/components/audit pages | Fabushi settings/Host/admin surfaces | remote computer/settings/host client | `NOT-FUSED` at route/UI identity level; functions map to existing Fabushi-native control planes instead of duplicating `/admin/*` |

OBF-001 therefore changes only presentation/projection and acceptance contracts. It does **not** add an OpenBot executor, container supervisor, gateway, store, transcript database, MCP gateway, audit database, browser tenancy service, or remote-control protocol. Any row marked `EVIDENCE-PENDING` remains non-complete until the same fresh packaged run produces the required video/trace/log/screenshot evidence.