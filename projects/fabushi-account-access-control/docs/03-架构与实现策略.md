# 架构与实现策略

角色与 entitlement 分离：Web 使用 `isAdminUser()` 判断角色，`hasUnlimitedUsage()` 判断无限额度；AI 后端使用 `resolveAccountEntitlements()` 生成 `role/isAdmin/unlimitedUsage`。内建 `bhrum108`，同时保留环境变量扩展。参考 Keycloak 的角色分离思想与 OpenMeter 的 entitlement/metering 模式，但不引入新运行时依赖。
