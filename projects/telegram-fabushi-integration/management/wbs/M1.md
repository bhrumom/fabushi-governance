# WBS 原子任务 — M1 Rust Core 骨架

### M1.T01 — fabushi-protocol

- **交付物**：canonical Messaging Protocol v2
- **验收标准**：Rust 单测通过；可本地创建会话/消息；Electron 能通过 bridge 获取本地会话列表
- **客观验证**：current `native/mahayana-messaging/src/protocol.rs` + all-target messaging tests + Electron contract
- **来源**：源计划 M1
- **状态**：IMPLEMENTED
- **阻塞**：待 M1 acceptance reconciliation 将 current-head evidence 绑定到本任务
- **证据位置**：PR #1961；`../../../native/mahayana-messaging/src/protocol.rs`
- **下一步**：使用 current Messaging Product Gate 关闭 protocol/model 基线验收。

### M1.T02 — fabushi-storage

- **交付物**：production local-first storage
- **验收标准**：Rust 单测通过；可恢复会话/消息状态；production server 默认使用 durable store
- **客观验证**：M1.T06 SQLite foundation + M1.T02 production cutover PR #1990
- **来源**：源计划 M1
- **状态**：IN_PROGRESS
- **阻塞**：依赖 M1.T06 先进入 main；#1990 CI/merge pending
- **证据位置**：`../tasks/M1-T02-production-storage.md`（active branch）；PR #1990
- **下一步**：通过 #1990 gate，完成 production SQLite + legacy JSON one-time migration。

### M1.T03 — fabushi-messaging

- **交付物**：canonical Rust messaging state machine/service
- **验收标准**：Rust 单测通过；可本地创建会话/消息；Electron bridge 可消费同一消息核心
- **客观验证**：`engine.rs` / `service.rs` / integration tests / Messaging Product Gate
- **来源**：源计划 M1
- **状态**：IMPLEMENTED
- **阻塞**：待 project-scoped acceptance reconciliation
- **证据位置**：PR #1961；`../../../native/mahayana-messaging/`
- **下一步**：把 current-head all-target test evidence绑定到 M1 验收记录。

### M1.T04 — fabushi-sync

- **交付物**：cursor / delta sync foundation
- **验收标准**：Rust 单测通过；状态可通过统一事件/同步接口恢复
- **客观验证**：service/realtime/device sync paths + current Rust tests
- **来源**：源计划 M1
- **状态**：IMPLEMENTED
- **阻塞**：待 sync-specific acceptance reconciliation
- **证据位置**：PR #1961；messaging service/device/realtime modules
- **下一步**：增加/绑定 reconnect、cursor、idempotency 验收证据。

### M1.T05 — fabushi-client-sdk

- **交付物**：Electron/Host client bridge foundation
- **验收标准**：Electron 能通过 bridge 获取/操作同一 messaging state；客户端不得复制第二套状态机
- **客观验证**：Feature Host bridge + Electron Messenger contract + Native Edge parity
- **来源**：源计划 M1
- **状态**：IMPLEMENTED
- **阻塞**：M11 原生移动绑定仍需独立跨端验证
- **证据位置**：PR #1961；Messaging Product Gate Electron contract
- **下一步**：M1 先关闭 Electron/Host bridge 基线，移动端完整共享验收归 M11。

### M1.T06 — SQLite schema

- **交付物**：版本化 SQLite schema + `SqliteStateStore`
- **验收标准**：Rust 单测通过；snapshot 可在进程重开后恢复；schema 版本不兼容可显式拒绝；Electron 仍可通过现有 bridge 使用同一消息核心
- **客观验证**：PR #1988；Messaging Product Gate run `32559222693`; Mahayana fast checks `32559222679`
- **来源**：源计划 M1
- **状态**：TESTED
- **阻塞**：protected merge queue / canonical main verification pending
- **证据位置**：`../tasks/M1-T06-sqlite-storage.md`; `../../evidence/M1-T06/README.md`; PR #1988
- **下一步**：merge queue 后在 main 关闭 landed evidence；production default 由 M1.T02/#1990 完成。

### M1.T07 — Conversation/Message/Participant 基础模型

- **交付物**：Actor/Conversation/Message 基础模型
- **验收标准**：Rust 单测通过；可创建会话/消息；Electron bridge 使用同一模型
- **客观验证**：actor/conversation/message modules + messaging contract tests + Electron contract
- **来源**：源计划 M1
- **状态**：IMPLEMENTED
- **阻塞**：待 project-scoped acceptance reconciliation
- **证据位置**：PR #1961；`../../../native/mahayana-messaging/src/{actor,conversation,message}.rs`
- **下一步**：把 current-head tests/bridge evidence 回填为 M1 acceptance proof。
